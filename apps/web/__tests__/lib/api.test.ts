import { describe, test } from 'vitest';

describe('apiFetch', () => {
  test.todo('sends credentials: include');
  test.todo('retries on 401 after refresh');
  test.todo('redirects to /login on refresh failure');
});
