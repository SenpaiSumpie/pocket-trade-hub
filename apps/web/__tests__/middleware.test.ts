import { describe, test } from 'vitest';

describe('Auth middleware', () => {
  test.todo('redirects unauthenticated user to /login');
  test.todo('allows authenticated user to access /cards');
  test.todo('redirects authenticated user from /login to /cards');
});
