---
phase: 01-foundation-and-auth
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - apps/mobile/package.json
  - apps/mobile/app.json
  - apps/mobile/tsconfig.json
  - apps/mobile/app/_layout.tsx
  - apps/mobile/app/(auth)/_layout.tsx
  - apps/mobile/app/(auth)/login.tsx
  - apps/mobile/app/(auth)/signup.tsx
  - apps/mobile/app/(auth)/reset-password.tsx
  - apps/mobile/app/(tabs)/_layout.tsx
  - apps/mobile/app/(tabs)/index.tsx
  - apps/mobile/app/(tabs)/cards.tsx
  - apps/mobile/app/(tabs)/trades.tsx
  - apps/mobile/app/(tabs)/profile.tsx
  - apps/mobile/app/onboarding.tsx
  - apps/mobile/app/user/[id].tsx
  - apps/mobile/app/edit-profile.tsx
  - apps/mobile/src/stores/auth.ts
  - apps/mobile/src/hooks/useApi.ts
  - apps/mobile/src/constants/avatars.ts
  - apps/mobile/src/constants/theme.ts
  - apps/mobile/src/components/FriendCodeBadge.tsx
  - apps/mobile/src/components/AvatarPicker.tsx
  - apps/mobile/src/components/SetupChecklist.tsx
autonomous: false
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - PROF-01
  - PROF-02
  - PROF-03
  - PROF-04

must_haves:
  truths:
    - "User can sign up with email and password from the mobile app"
    - "User can log in and stay logged in across app restarts"
    - "User can log out from any screen via the profile tab"
    - "User can request a password reset from the login screen"
    - "User can set display name, avatar, and friend code on onboarding or edit profile"
    - "User can view another user's profile with avatar, name, friend code, and join date"
    - "User can tap a friend code to copy it to clipboard with a Copied toast"
    - "Home tab shows setup checklist and coming-soon previews"
    - "Cards and Trades tabs show Coming soon placeholder screens"
  artifacts:
    - path: "apps/mobile/app/_layout.tsx"
      provides: "Root layout with Stack.Protected auth guards"
      contains: "Stack.Protected"
    - path: "apps/mobile/src/stores/auth.ts"
      provides: "Zustand auth store with SecureStore hydration"
      exports: ["useAuthStore"]
    - path: "apps/mobile/app/(auth)/login.tsx"
      provides: "Branded login screen with email/password form"
      min_lines: 50
    - path: "apps/mobile/app/(tabs)/_layout.tsx"
      provides: "Bottom tab bar with 4 tabs: Home, Cards, Trades, Profile"
      contains: "Tabs.Screen"
    - path: "apps/mobile/app/user/[id].tsx"
      provides: "Other user profile view with friend code copy"
      min_lines: 40
    - path: "apps/mobile/app/onboarding.tsx"
      provides: "Post-signup onboarding (name, avatar, friend code)"
      min_lines: 40
  key_links:
    - from: "apps/mobile/src/stores/auth.ts"
      to: "expo-secure-store"
      via: "Token persistence"
      pattern: "SecureStore\\.(setItemAsync|getItemAsync|deleteItemAsync)"
    - from: "apps/mobile/app/(auth)/login.tsx"
      to: "apps/api/src/routes/auth.ts"
      via: "POST /auth/login fetch call"
      pattern: "fetch.*auth/login"
    - from: "apps/mobile/app/(auth)/signup.tsx"
      to: "apps/api/src/routes/auth.ts"
      via: "POST /auth/signup fetch call"
      pattern: "fetch.*auth/signup"
    - from: "apps/mobile/app/user/[id].tsx"
      to: "apps/api/src/routes/users.ts"
      via: "GET /users/:id fetch call"
      pattern: "fetch.*users/"
    - from: "apps/mobile/app/_layout.tsx"
      to: "apps/mobile/src/stores/auth.ts"
      via: "Auth state drives route protection"
      pattern: "useAuthStore"
---

<objective>
Build the Expo mobile app with branded auth screens, tab navigation, profile management, onboarding flow, and all UI for the authentication and profile features.

Purpose: Delivers the complete user-facing mobile experience -- from sign up through profile management -- consuming the API endpoints built in Plan 01.
Output: Working Expo app with auth flow, tab navigation, profile screens, and onboarding.
</objective>

<execution_context>
@C:/Users/romec/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/romec/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-auth/01-RESEARCH.md
@.planning/phases/01-foundation-and-auth/01-CONTEXT.md
@.planning/phases/01-foundation-and-auth/01-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 that this plan consumes -->

From packages/shared/src/schemas/auth.ts:
```typescript
export const signupSchema = z.object({
  email: z.string().email(),
  password: z.string().min(8),
  confirmPassword: z.string(),
}).refine(data => data.password === data.confirmPassword, ...);
export const loginSchema = z.object({ email: z.string().email(), password: z.string().min(1) });
export const resetRequestSchema = z.object({ email: z.string().email() });
export const resetConfirmSchema = z.object({ token: z.string(), newPassword: z.string().min(8) });
export type SignupInput = z.infer<typeof signupSchema>;
export type LoginInput = z.infer<typeof loginSchema>;
```

From packages/shared/src/schemas/user.ts:
```typescript
export const friendCodeSchema = z.string().regex(/^\d{4}-\d{4}-\d{4}-\d{4}$/);
export const updateProfileSchema = z.object({
  displayName: z.string().min(1).max(30).optional(),
  avatarId: z.string().optional(),
  friendCode: friendCodeSchema.optional(),
});
```

API Endpoints (from Plan 01):
- POST /auth/signup -> 201 {accessToken, refreshToken, user: {id, email}}
- POST /auth/login -> 200 {accessToken, refreshToken, user: {id, email, displayName, avatarId, friendCode}}
- POST /auth/refresh -> 200 {accessToken, refreshToken}
- POST /auth/logout -> 200 (requires Authorization header)
- POST /auth/reset-request -> 200
- POST /auth/reset-confirm -> 200
- GET /users/me -> 200 {id, email, displayName, avatarId, friendCode, createdAt}
- GET /users/:id -> 200 {id, displayName, avatarId, friendCode, createdAt} | 404
- PATCH /users/me -> 200 {updated user} (requires Authorization header)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expo app scaffolding, auth store, API hook, theme, and auth screens</name>
  <files>
    apps/mobile/package.json, apps/mobile/app.json, apps/mobile/tsconfig.json,
    apps/mobile/app/_layout.tsx, apps/mobile/app/(auth)/_layout.tsx,
    apps/mobile/app/(auth)/login.tsx, apps/mobile/app/(auth)/signup.tsx,
    apps/mobile/app/(auth)/reset-password.tsx,
    apps/mobile/app/(tabs)/_layout.tsx, apps/mobile/app/(tabs)/index.tsx,
    apps/mobile/app/(tabs)/cards.tsx, apps/mobile/app/(tabs)/trades.tsx,
    apps/mobile/app/(tabs)/profile.tsx,
    apps/mobile/src/stores/auth.ts, apps/mobile/src/hooks/useApi.ts,
    apps/mobile/src/constants/theme.ts
  </files>
  <action>
    **Step 1: Create Expo app**
    - Run `npx create-expo-app@latest apps/mobile --template tabs` from project root
    - Install dependencies: `npx expo install expo-secure-store expo-clipboard @react-native-async-storage/async-storage expo-splash-screen`
    - Install: `npm install zustand zod react-hook-form @hookform/resolvers react-native-toast-message burnt`
    - Ensure the app's package.json has name "mobile" (for pnpm filter) and can import @pocket-trade-hub/shared

    **Step 2: Theme constants**
    - Create `src/constants/theme.ts` with dark theme colors per user decision:
      - Background: deep dark (#0f0f1a or similar dark blue-black)
      - Card/surface: slightly lighter dark (#1a1a2e)
      - Primary accent: Pokemon-inspired gold/yellow (#f0c040)
      - Text: white/light gray
      - Error: red, success: green
      - Typography scale: heading, body, caption sizes

    **Step 3: Auth store (Zustand + SecureStore)**
    - Create `src/stores/auth.ts` per research Pattern 2:
      - State: accessToken, refreshToken, user (id, email, displayName, avatarId, friendCode), isLoggedIn, isHydrated
      - Actions: login(access, refresh, user) -> saves tokens to SecureStore + sets state; logout() -> deletes from SecureStore + clears state; hydrate() -> reads from SecureStore, if accessToken exists fetch /users/me to get user data, sets isHydrated true
      - On hydrate failure (expired token): attempt refresh via /auth/refresh; if that fails, clear tokens

    **Step 4: API hook**
    - Create `src/hooks/useApi.ts`:
      - Base URL from env or constant (e.g., http://localhost:3000 for dev)
      - `apiFetch(path, options)`: adds Authorization: Bearer header from auth store, handles 401 by attempting token refresh, retries original request once
      - Returns typed response or throws error

    **Step 5: Root layout with auth guards**
    - Create `app/_layout.tsx` per research Pattern 1:
      - Keep splash screen visible until auth store isHydrated (prevents hydration flash -- research pitfall 1)
      - Call authStore.hydrate() on mount
      - Use Stack.Protected: guard={!isLoggedIn} wraps (auth) group; guard={isLoggedIn} wraps (tabs), onboarding, user/[id], edit-profile
      - Per research pitfall 2: every route group folder must have its own _layout.tsx

    **Step 6: Auth screens**
    - `app/(auth)/_layout.tsx`: Stack layout, headerShown: false
    - `app/(auth)/login.tsx`: Branded, immersive login screen per user decision:
      - Dark background with subtle Pokemon card art/pattern imagery
      - App logo/name prominently at top
      - Email + password fields with react-hook-form + zod (loginSchema from shared)
      - "Log In" button with loading spinner on press
      - "Don't have an account? Sign Up" link to signup
      - "Forgot Password?" link to reset-password
      - Inline field validation errors, toast for network errors
    - `app/(auth)/signup.tsx`: Branded signup screen matching login style:
      - Email, password, confirm password fields (signupSchema from shared)
      - "Create Account" button
      - "Already have an account? Log In" link
      - On success: store tokens via auth store, navigate to onboarding
    - `app/(auth)/reset-password.tsx`:
      - Two-step flow: (1) Enter email, submit to /auth/reset-request, show success message (2) Enter code/token + new password (since we use email link in prod, for dev show a text field for the token)
      - Success message: "Check your email for reset instructions"

    **Step 7: Tab navigation shell**
    - `app/(tabs)/_layout.tsx`: Bottom tab bar with 4 tabs per user decision:
      - Home (Ionicons "home"), Cards (Ionicons "albums"), Trades (Ionicons "swap-horizontal"), Profile (Ionicons "person")
      - Dark-themed tab bar matching app theme
    - `app/(tabs)/cards.tsx`: "Coming soon" placeholder with card icon and message per user decision
    - `app/(tabs)/trades.tsx`: "Coming soon" placeholder with trade icon and message per user decision
    - `app/(tabs)/profile.tsx`: Own profile screen showing avatar, display name, friend code, join date, email. "Edit Profile" button navigates to edit-profile screen. "Log Out" button calls auth store logout (AUTH-03: logout from any screen -- it's accessible from the Profile tab which is always visible).
    - `app/(tabs)/index.tsx`: Home tab placeholder (will be built out in Task 2 with setup checklist)
  </action>
  <verify>
    <automated>cd C:/Users/romec/OneDrive/Desktop/CodingProjects/pocket-trade-hub && npx expo export --platform web --output-dir /tmp/expo-test-build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Expo app boots without crashes
    - Auth store hydrates from SecureStore on launch (no login flash)
    - Login screen shows branded UI with email/password form
    - Signup screen creates account and navigates to onboarding
    - Reset password screen sends reset request
    - Tab bar shows 4 tabs with icons
    - Cards and Trades tabs show "Coming soon" placeholders
    - Profile tab shows user info and logout button
    - Logging out redirects to login screen
  </done>
</task>

<task type="auto">
  <name>Task 2: Onboarding, edit profile, user profile view, friend code copy, and home screen</name>
  <files>
    apps/mobile/app/onboarding.tsx,
    apps/mobile/app/edit-profile.tsx,
    apps/mobile/app/user/[id].tsx,
    apps/mobile/app/(tabs)/index.tsx,
    apps/mobile/src/constants/avatars.ts,
    apps/mobile/src/components/FriendCodeBadge.tsx,
    apps/mobile/src/components/AvatarPicker.tsx,
    apps/mobile/src/components/SetupChecklist.tsx
  </files>
  <action>
    **Step 1: Avatar presets**
    - Create `src/constants/avatars.ts`:
      - Define 12-16 preset avatars based on Pokemon types (fire, water, grass, electric, psychic, dark, dragon, fairy, fighting, ghost, steel, ice)
      - Each avatar has: id (string like "fire", "water"), label, emoji or simple icon representation (use Ionicons/MaterialCommunityIcons as placeholder -- actual custom art can be swapped in later)
      - Export AVATARS array and getAvatarById helper

    **Step 2: Reusable components**
    - `src/components/FriendCodeBadge.tsx`:
      - Displays friend code in formatted style (XXXX-XXXX-XXXX-XXXX)
      - Tappable: on press, copies to clipboard using expo-clipboard, shows "Copied!" toast (use burnt for native iOS toast, ToastAndroid for Android per research)
      - Visual: monospace font, subtle card-like background, copy icon indicator
    - `src/components/AvatarPicker.tsx`:
      - Grid of preset avatar icons from constants
      - Selected avatar highlighted with accent border
      - Props: selectedId, onSelect(id)
    - `src/components/SetupChecklist.tsx`:
      - Shows checklist items: "Set display name" (check if displayName exists), "Choose avatar" (check if avatarId exists), "Add friend code" (check if friendCode exists)
      - Each item shows checkmark when complete, tap navigates to edit-profile
      - Props: user object from auth store

    **Step 3: Onboarding screen**
    - `app/onboarding.tsx`: Presented as modal per research architecture
      - Welcoming headline: "Welcome to Pocket Trade Hub!"
      - AvatarPicker component for selecting avatar
      - Display name text input (max 30 chars)
      - Friend code input with XXXX-XXXX-XXXX-XXXX placeholder, validated with friendCodeSchema
      - "Get Started" button -- calls PATCH /users/me with selections, updates auth store user, navigates to (tabs)
      - "Skip for now" button -- navigates directly to (tabs)
      - All fields optional (skippable per user decision)

    **Step 4: Edit Profile screen**
    - `app/edit-profile.tsx`:
      - Dedicated edit screen per user decision (not inline editing)
      - Pre-populated with current values from auth store
      - AvatarPicker, display name input, friend code input (same components as onboarding)
      - "Save" button: validates with updateProfileSchema, calls PATCH /users/me, updates auth store, navigates back
      - Loading state on save button

    **Step 5: Other user profile view**
    - `app/user/[id].tsx`:
      - Full-screen view per discretion recommendation
      - Fetches GET /users/:id on mount
      - Shows: large avatar icon, display name, friend code (via FriendCodeBadge -- tappable to copy), "Member since" join date formatted nicely
      - Trade history count shows "0 trades" (placeholder for Phase 5 -- PROF-03 says "with trade history count", initialized to 0)
      - Loading skeleton while fetching
      - 404 handling: "User not found" message

    **Step 6: Home tab with setup checklist**
    - Update `app/(tabs)/index.tsx`:
      - Welcome message: "Welcome, {displayName || 'Trainer'}!"
      - SetupChecklist component if profile incomplete
      - "Coming soon" preview cards for future features (Cards Database, Trade Matching, Trade Proposals) with brief descriptions and icons
      - Dark themed, consistent with overall app aesthetic
  </action>
  <verify>
    <automated>cd C:/Users/romec/OneDrive/Desktop/CodingProjects/pocket-trade-hub && npx expo export --platform web --output-dir /tmp/expo-test-build-2 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Onboarding screen appears after signup with avatar picker, name input, friend code input
    - Onboarding is skippable
    - Edit Profile screen loads current values and saves changes via API
    - User profile view (user/[id]) shows avatar, name, friend code, join date
    - Tapping friend code copies to clipboard with "Copied!" toast
    - Home tab shows welcome message with setup checklist
    - Home tab shows "coming soon" previews for future features
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete auth and profile flow end-to-end</name>
  <action>
    Human verifies the full auth flow and profile management by running through the app manually.
    All automated work was completed in Tasks 1 and 2. This checkpoint confirms visual correctness,
    interaction quality, and end-to-end data flow between the mobile app and API.
  </action>
  <verify>Human walkthrough confirms all flows work correctly</verify>
  <done>User approves the complete auth and profile experience</done>
  <what-built>Complete auth flow and profile management in the Expo mobile app connected to the Fastify API</what-built>
  <how-to-verify>
    Prerequisites: Ensure PostgreSQL is running, API server is running (`cd apps/api && pnpm dev`), Expo dev server is running (`cd apps/mobile && npx expo start`)

    1. **Signup flow:** Open the app. You should see a branded, dark-themed login screen with Pokemon imagery. Tap "Sign Up". Enter an email, password (8+ chars), confirm password. Tap "Create Account". You should be taken to the onboarding screen.

    2. **Onboarding:** Select an avatar, enter a display name, enter a friend code (format: 1234-5678-9012-3456). Tap "Get Started". You should land on the Home tab.

    3. **Home tab:** Verify welcome message shows your display name. Check that setup checklist items are checked off. See "coming soon" preview cards.

    4. **Tab navigation:** Tap Cards tab -- see "Coming soon" placeholder. Tap Trades tab -- see "Coming soon" placeholder. Tap Profile tab -- see your avatar, display name, friend code, join date, and email.

    5. **Edit profile:** From Profile tab, tap "Edit Profile". Change your display name. Tap Save. Verify the change appears on the Profile tab.

    6. **Friend code copy:** On your own profile (or navigate to another user's profile), tap the friend code. Verify "Copied!" toast appears. Paste somewhere to confirm clipboard content.

    7. **Logout:** Tap "Log Out" on the Profile tab. Verify you're returned to the login screen.

    8. **Login persistence:** Log in again. Force-close the app. Reopen it. Verify you're still logged in (no login screen flash).

    9. **Password reset:** Log out. On login screen, tap "Forgot Password?". Enter your email. Verify success message appears.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
Full verification of Phase 1:
1. API tests all pass: `pnpm turbo test`
2. Expo app builds without errors
3. Manual walkthrough of all auth and profile flows per checkpoint
</verification>

<success_criteria>
- User can sign up, log in, stay logged in across restarts, log out, and reset password
- User can set display name, avatar, and friend code via onboarding or edit profile
- User can view another user's profile and copy their friend code
- Tab bar with 4 tabs visible, Cards and Trades show placeholders
- Home tab shows setup checklist and coming-soon previews
- Dark themed, branded UI throughout
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-auth/01-02-SUMMARY.md`
</output>
