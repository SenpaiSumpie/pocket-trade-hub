---
phase: 10-internationalization
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - apps/mobile/app/(tabs)/_layout.tsx
  - apps/mobile/app/(tabs)/index.tsx
  - apps/mobile/app/(tabs)/cards.tsx
  - apps/mobile/app/(tabs)/market.tsx
  - apps/mobile/app/(tabs)/trades.tsx
  - apps/mobile/app/(tabs)/profile.tsx
  - apps/mobile/app/(auth)/login.tsx
  - apps/mobile/app/(auth)/signup.tsx
  - apps/mobile/app/(auth)/reset-password.tsx
  - apps/mobile/app/edit-profile.tsx
  - apps/mobile/app/onboarding.tsx
  - apps/mobile/app/_layout.tsx
  - apps/mobile/src/components/LanguageSelector.tsx
  - apps/mobile/src/components/cards/CardDetailModal.tsx
  - apps/mobile/src/components/cards/CardGrid.tsx
  - apps/mobile/src/components/cards/SetPicker.tsx
  - apps/mobile/src/components/collection/CollectionSummary.tsx
  - apps/mobile/src/components/market/CreatePostModal.tsx
  - apps/mobile/src/components/market/PostDetailModal.tsx
  - apps/mobile/src/components/market/PostCard.tsx
  - apps/mobile/src/components/trades/ProposalCard.tsx
  - apps/mobile/src/components/trades/ProposalCreationModal.tsx
  - apps/mobile/src/components/trades/RatingModal.tsx
  - apps/mobile/src/components/premium/PremiumBadge.tsx
  - apps/mobile/src/components/premium/PaywallModal.tsx
  - apps/mobile/src/components/export/CardExport.tsx
  - apps/mobile/src/components/export/CollectionExport.tsx
  - apps/mobile/src/components/export/PostExport.tsx
  - apps/mobile/src/components/export/WantedExport.tsx
  - apps/mobile/src/components/promo/RedeemCodeForm.tsx
  - apps/mobile/src/components/notifications/NotificationList.tsx
  - apps/mobile/src/stores/auth.ts
autonomous: true
requirements:
  - PLAT-03
  - PLAT-04

must_haves:
  truths:
    - "All visible UI text in the mobile app comes from translation keys, not hardcoded strings"
    - "User can open language selector on profile screen and see 10 languages with native names"
    - "Selecting a language instantly updates all visible UI text without app restart"
    - "Language preference persists across app sessions via server sync"
    - "App defaults to device language on first launch"
  artifacts:
    - path: "apps/mobile/src/components/LanguageSelector.tsx"
      provides: "Scrollable language picker showing native names with text codes"
      min_lines: 50
    - path: "apps/mobile/app/(tabs)/profile.tsx"
      provides: "Language section in profile with selector trigger"
      contains: "useTranslation"
    - path: "apps/mobile/app/(tabs)/_layout.tsx"
      provides: "Tab labels using translation keys"
      contains: "t('tabs."
  key_links:
    - from: "apps/mobile/src/components/LanguageSelector.tsx"
      to: "apps/mobile/src/stores/language.ts"
      via: "setLanguage() call on selection"
      pattern: "setLanguage"
    - from: "apps/mobile/app/_layout.tsx"
      to: "apps/mobile/src/i18n/index.ts"
      via: "i18n import to ensure initialization"
      pattern: "import.*i18n"
    - from: "apps/mobile/src/stores/auth.ts"
      to: "apps/mobile/src/stores/language.ts"
      via: "initLanguage called on auth hydration with user.uiLanguage"
      pattern: "initLanguage"
---

<objective>
Replace all hardcoded strings across the mobile app with i18n translation calls and build the language selector UI.

Purpose: This is the core i18n integration that makes PLAT-03 and PLAT-04 visible to users. Every screen, modal, and component gets wired to use `t()` calls instead of hardcoded English strings.

Output: Fully i18n-wired mobile app with working language selector on profile screen.
</objective>

<execution_context>
@C:/Users/romec/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/romec/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-internationalization/10-CONTEXT.md
@.planning/phases/10-internationalization/10-RESEARCH.md
@.planning/phases/10-internationalization/10-01-SUMMARY.md

@apps/mobile/src/i18n/locales/en.json
@apps/mobile/src/i18n/index.ts
@apps/mobile/src/stores/language.ts
@packages/shared/src/schemas/i18n.ts

<interfaces>
<!-- From Plan 01 outputs -->

From packages/shared/src/schemas/i18n.ts:
```typescript
export const supportedUILanguages = ['en', 'de', 'es', 'fr', 'it', 'ja', 'ko', 'pt', 'zh', 'th'] as const;
export type UILanguage = typeof supportedUILanguages[number];
export const UI_LANGUAGES: { code: UILanguage; name: string; nativeName: string }[];
```

From apps/mobile/src/stores/language.ts:
```typescript
export const useLanguageStore: {
  currentLanguage: string;
  isLoading: boolean;
  initLanguage: (serverLanguage?: string) => void;
  setLanguage: (lang: string) => Promise<void>;
};
```

From apps/mobile/src/i18n/index.ts:
```typescript
// i18n instance configured with i18next + react-i18next
// Usage: import { useTranslation } from 'react-i18next'; const { t } = useTranslation();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire i18n into app root, replace strings across all screens and components</name>
  <files>
    apps/mobile/app/_layout.tsx,
    apps/mobile/app/(tabs)/_layout.tsx,
    apps/mobile/app/(tabs)/index.tsx,
    apps/mobile/app/(tabs)/cards.tsx,
    apps/mobile/app/(tabs)/market.tsx,
    apps/mobile/app/(tabs)/trades.tsx,
    apps/mobile/app/(tabs)/profile.tsx,
    apps/mobile/app/(auth)/login.tsx,
    apps/mobile/app/(auth)/signup.tsx,
    apps/mobile/app/(auth)/reset-password.tsx,
    apps/mobile/app/edit-profile.tsx,
    apps/mobile/app/onboarding.tsx,
    apps/mobile/src/stores/auth.ts
  </files>
  <action>
1. In `apps/mobile/app/_layout.tsx` (root layout):
   - Add `import '../src/i18n'` at the TOP of imports (before any component code) to ensure i18n initializes first
   - Import language store and call `initLanguage(user?.uiLanguage)` during auth hydration flow

2. In `apps/mobile/src/stores/auth.ts`:
   - On successful login/signup/hydration, call `useLanguageStore.getState().initLanguage(user.uiLanguage)` to sync language from server profile
   - This ensures the app switches to the user's saved language preference on login

3. In `apps/mobile/app/(tabs)/_layout.tsx`:
   - Add `import { useTranslation } from 'react-i18next'`
   - Replace all hardcoded tab titles with `t('tabs.home')`, `t('tabs.cards')`, `t('tabs.market')`, `t('tabs.trades')`, `t('tabs.profile')`

4. For EVERY screen and component file listed below, add `const { t } = useTranslation()` and replace ALL hardcoded English strings with `t('key')` calls using the keys from en.json:
   - `(tabs)/index.tsx` — home screen: setup checklist labels, collection summary labels, welcome text
   - `(tabs)/cards.tsx` — browse/collection/wanted labels, search placeholder, filter labels, empty states
   - `(tabs)/market.tsx` — market title, post type labels, filter labels, empty states
   - `(tabs)/trades.tsx` — trade status labels, empty states, proposal action labels
   - `(tabs)/profile.tsx` — all profile section labels, logout confirmation, member since, friend code labels, linked accounts, premium status, redeem code labels. Add a new "Language" section showing current language name + chevron that opens the LanguageSelector (Task 2)
   - `(auth)/login.tsx` — login/signup labels, error messages, OAuth button labels
   - `(auth)/signup.tsx` — signup labels, validation messages
   - `(auth)/reset-password.tsx` — reset password labels, success/error messages
   - `edit-profile.tsx` — edit profile form labels, save/cancel, validation messages
   - `onboarding.tsx` — onboarding step labels, avatar picker labels, friend code labels

5. Replace hardcoded date formatting: Find all `toLocaleDateString('en-US', ...)` or `formatDate` calls and replace the locale parameter with the current i18n language. Use `i18n.language` or pass it from the language store.

6. For Toast.show() calls, Alert.alert() calls, and any other user-facing strings — replace with t() calls. Keep console.log/error messages in English (those are developer-facing).

IMPORTANT: Do NOT create the LanguageSelector component in this task — that's Task 2. In profile.tsx, add the Language section row that will trigger the selector (show current language name, tappable to open selector). Use a placeholder onPress that will be wired in Task 2.
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - Every user-facing string in all screen files uses t() calls
    - Tab labels are translated via useTranslation hook
    - Auth screens (login, signup, reset-password) use translation keys
    - Profile screen has a "Language" section showing current language
    - Date formatting uses current i18n locale instead of hardcoded 'en-US'
    - Auth store calls initLanguage on hydration/login
    - Root layout imports i18n initialization module
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Build LanguageSelector component and wire remaining component translations</name>
  <files>
    apps/mobile/src/components/LanguageSelector.tsx,
    apps/mobile/src/components/cards/CardDetailModal.tsx,
    apps/mobile/src/components/cards/CardGrid.tsx,
    apps/mobile/src/components/cards/SetPicker.tsx,
    apps/mobile/src/components/collection/CollectionSummary.tsx,
    apps/mobile/src/components/market/CreatePostModal.tsx,
    apps/mobile/src/components/market/PostDetailModal.tsx,
    apps/mobile/src/components/market/PostCard.tsx,
    apps/mobile/src/components/trades/ProposalCard.tsx,
    apps/mobile/src/components/trades/ProposalCreationModal.tsx,
    apps/mobile/src/components/trades/RatingModal.tsx,
    apps/mobile/src/components/premium/PremiumBadge.tsx,
    apps/mobile/src/components/premium/PaywallModal.tsx,
    apps/mobile/src/components/export/CardExport.tsx,
    apps/mobile/src/components/export/CollectionExport.tsx,
    apps/mobile/src/components/export/PostExport.tsx,
    apps/mobile/src/components/export/WantedExport.tsx,
    apps/mobile/src/components/promo/RedeemCodeForm.tsx,
    apps/mobile/src/components/notifications/NotificationList.tsx,
    apps/mobile/app/(tabs)/profile.tsx
  </files>
  <action>
1. Create `apps/mobile/src/components/LanguageSelector.tsx`:
   - A bottom sheet or Modal component showing a scrollable list of all 10 languages
   - Import `UI_LANGUAGES` from `@pocket-trade-hub/shared` for the language list
   - Each row shows: native name + text code, e.g., "Deutsch (DE)", "日本語 (JA)", "ไทย (TH)"
   - Current language has a checkmark indicator
   - On tap: call `useLanguageStore.getState().setLanguage(code)` — this triggers i18n.changeLanguage() and server sync
   - Close the selector after selection
   - Props: `visible: boolean`, `onClose: () => void`
   - Style consistent with existing bottom sheets/modals in the app (dark theme, rounded corners, etc.)

2. Wire LanguageSelector into `apps/mobile/app/(tabs)/profile.tsx`:
   - Add state for selector visibility: `const [languageVisible, setLanguageVisible] = useState(false)`
   - The Language section row (added in Task 1) gets its onPress wired to `setLanguageVisible(true)`
   - Render `<LanguageSelector visible={languageVisible} onClose={() => setLanguageVisible(false)} />`

3. For EVERY component file listed, add `const { t } = useTranslation()` and replace ALL hardcoded strings with t() calls:
   - `CardDetailModal.tsx` — card detail labels, action buttons, luck calculator labels, pull rate text
   - `CardGrid.tsx` — empty state messages, loading text
   - `SetPicker.tsx` — set labels, progress text
   - `CollectionSummary.tsx` — summary labels, stats text
   - `CreatePostModal.tsx` — form labels, post type selection, card selection labels, submit button
   - `PostDetailModal.tsx` — post detail labels, action buttons, proposal button
   - `PostCard.tsx` — post card labels, status badges, expiry text
   - `ProposalCard.tsx` — proposal status labels, action buttons
   - `ProposalCreationModal.tsx` — creation form labels, card selection
   - `RatingModal.tsx` — rating labels, star descriptions, submit button
   - `PremiumBadge.tsx` — premium label text
   - `PaywallModal.tsx` — paywall description, feature list, upgrade button
   - `CardExport.tsx`, `CollectionExport.tsx`, `PostExport.tsx`, `WantedExport.tsx` — export template text, share labels
   - `RedeemCodeForm.tsx` — code input label, redeem button, success/error messages
   - `NotificationList.tsx` — notification labels, empty state, mark all read

4. If any strings were missed in en.json from Plan 01, add them now. The en.json file is the source of truth — if a component has a string not in en.json, add the key.

IMPORTANT: For export template components (CardExport, CollectionExport, etc.), these render as images via react-native-view-shot. The t() calls work fine here since the component renders as React Native views before capture.
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - LanguageSelector shows all 10 languages with native names and text codes
    - Selecting a language in the selector instantly updates all UI text (hot-swap)
    - Language preference syncs to server via PATCH /users/me
    - All component files use t() calls — no hardcoded English strings remain in user-facing text
    - Export templates use translated text
    - Promo code form uses translated labels and messages
    - Notification list uses translated labels
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
- `cd apps/mobile && npx tsc --noEmit` passes with zero errors
- Grep for remaining hardcoded strings: `grep -r "title:" apps/mobile/app/(tabs)/_layout.tsx` shows t() calls
- No English string literals in component render returns (except technical strings like URLs, IDs)
</verification>

<success_criteria>
- Every screen and component in the mobile app uses useTranslation() and t() calls
- Language selector accessible from profile screen, shows 10 languages
- Language change is instant (hot-swap), no app restart needed
- Language syncs to server on change, initializes from server on login
- Date/number formatting respects current language locale
</success_criteria>

<output>
After completion, create `.planning/phases/10-internationalization/10-02-SUMMARY.md`
</output>
