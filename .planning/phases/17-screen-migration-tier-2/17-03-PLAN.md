---
phase: 17-screen-migration-tier-2
plan: 03
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - apps/mobile/app/(tabs)/profile.tsx
  - apps/mobile/src/components/premium/PaywallCard.tsx
autonomous: true
requirements: [SCR-06]
must_haves:
  truths:
    - "Profile header has glassmorphism backdrop with BlurView intensity 40 and gold overlay at 8% opacity"
    - "Info sections (friend code, linked accounts, member since, language) use Card primitive"
    - "Edit Profile button uses Button primary variant, Logout uses Button destructive variant"
    - "Link/Unlink account buttons use Button secondary variant"
    - "Premium badge next to display name uses Badge premium variant"
    - "PaywallCard uses Card primitive with 2px gold gradient top-border accent"
    - "Profile tab shows ProfileHeaderSkeleton during initial load"
    - "ActivityIndicator loading states replaced with ShimmerText inline shimmer"
    - "Profile tab has gold pull-to-refresh and staggered entrance on info sections"
    - "Profile tab actions trigger toast notifications (profile update, account link/unlink)"
    - "Reputation stars keep their current visual unchanged"
  artifacts:
    - path: "apps/mobile/app/(tabs)/profile.tsx"
      provides: "Glassmorphism header, Card sections, Button actions, Badge premium, shimmer loading, stagger, PTR, toasts"
      contains: "BlurView"
    - path: "apps/mobile/src/components/premium/PaywallCard.tsx"
      provides: "Card primitive PaywallCard with gold gradient top-border"
      contains: "LinearGradient"
  key_links:
    - from: "apps/mobile/app/(tabs)/profile.tsx"
      to: "expo-blur"
      via: "import BlurView from expo-blur"
      pattern: "BlurView.*expo-blur"
    - from: "apps/mobile/app/(tabs)/profile.tsx"
      to: "apps/mobile/src/components/ui/Badge.tsx"
      via: "import Badge premium variant for display name"
      pattern: "variant.*premium"
    - from: "apps/mobile/src/components/premium/PaywallCard.tsx"
      to: "react-native-svg"
      via: "SVG LinearGradient for top-border accent"
      pattern: "LinearGradient"
    - from: "apps/mobile/app/(tabs)/profile.tsx"
      to: "apps/mobile/src/hooks/useToast.ts"
      via: "import useToast replacing Toast.show"
      pattern: "useToast"
---

<objective>
Migrate the Profile tab with glassmorphism header (expo-blur BlurView + gold overlay), Card primitive info sections, Button variant actions, Badge premium for display name, PaywallCard gold top-border accent, shimmer loading replacements, ProfileHeaderSkeleton, staggered entrance, gold pull-to-refresh, and toast migration from react-native-toast-message to useToast.

Purpose: Complete SCR-06 — Profile tab features glassmorphism backdrop effects and a redesigned settings screen using new primitives.
Output: Migrated profile.tsx and PaywallCard.tsx
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-screen-migration-tier-2/17-CONTEXT.md
@.planning/phases/17-screen-migration-tier-2/17-UI-SPEC.md
@.planning/phases/17-screen-migration-tier-2/17-RESEARCH.md
@.planning/phases/17-screen-migration-tier-2/17-01-SUMMARY.md

@apps/mobile/app/(tabs)/profile.tsx
@apps/mobile/src/components/premium/PaywallCard.tsx

<interfaces>
<!-- BlurView from expo-blur (installed in Plan 01): -->
```typescript
import { BlurView } from 'expo-blur';

<BlurView intensity={40} tint="dark" style={styles.glassHeader}>
  <View style={styles.goldOverlay} pointerEvents="none" />
  {/* content */}
</BlurView>
```

<!-- Badge premium variant (from Plan 01): -->
```typescript
<Badge variant="premium" label="PRO" />
// Gold background (#f0c040), dark text (#0c0c18)
```

<!-- SVG LinearGradient for top-border (horizontal): -->
```typescript
import Svg, { Defs, LinearGradient, Stop, Rect } from 'react-native-svg';

// 2px horizontal gold gradient at top of card
<View style={{ position: 'absolute', top: 0, left: 0, right: 0, height: 2 }}>
  <Svg width="100%" height={2}>
    <Defs>
      <LinearGradient id="paywallGrad" x1="0" y1="0" x2="1" y2="0">
        <Stop offset="0" stopColor="#f5d060" stopOpacity="1" />
        <Stop offset="1" stopColor="#c9a020" stopOpacity="1" />
      </LinearGradient>
    </Defs>
    <Rect x="0" y="0" width="100%" height={2} fill="url(#paywallGrad)" />
  </Svg>
</View>
```

<!-- Shimmer inline loading (Phase 15): -->
```typescript
import { ShimmerText } from '@/src/components/animation/ShimmerText';
import { Shimmer } from '@/src/components/animation/Shimmer';
// Replace ActivityIndicator with:
<Shimmer><ShimmerText width="60%" /></Shimmer>
```

<!-- ProfileHeaderSkeleton (created in Plan 01): -->
```typescript
import { ProfileHeaderSkeleton } from '@/src/components/skeleton/ProfileHeaderSkeleton';
```

<!-- Toast migration pattern: -->
```typescript
// OLD (react-native-toast-message):
import Toast from 'react-native-toast-message';
Toast.show({ type: 'success', text1: 'Linked!' });

// NEW (Phase 16 Zustand system):
import { useToast } from '@/src/hooks/useToast';
const toast = useToast();
toast.success('Account linked');
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Profile tab migration — glassmorphism header, Card sections, Button actions, Badge premium, shimmer, stagger, PTR, toasts</name>
  <files>
    apps/mobile/app/(tabs)/profile.tsx
  </files>
  <read_first>
    apps/mobile/app/(tabs)/profile.tsx,
    apps/mobile/src/components/ui/Card.tsx,
    apps/mobile/src/components/ui/Button.tsx,
    apps/mobile/src/components/ui/Badge.tsx,
    apps/mobile/src/components/ui/Text.tsx,
    apps/mobile/src/components/animation/ShimmerText.tsx,
    apps/mobile/src/components/animation/Shimmer.tsx,
    apps/mobile/src/components/skeleton/ProfileHeaderSkeleton.tsx,
    apps/mobile/src/hooks/useStaggeredList.ts,
    apps/mobile/src/hooks/useToast.ts,
    apps/mobile/src/hooks/usePremium.ts,
    apps/mobile/app/(tabs)/cards.tsx
  </read_first>
  <action>
**Glassmorphism header (per D-18, D-19):**

1. Import `BlurView` from `expo-blur`.

2. Wrap the avatar + display name + premium badge area in a glassmorphism container:
   ```typescript
   <BlurView intensity={40} tint="dark" style={styles.glassHeader}>
     <View style={styles.goldOverlay} pointerEvents="none" />
     <View style={styles.headerContent}>
       {/* Avatar Image */}
       {/* Display Name row */}
       {/* Premium Badge (if premium) */}
       {/* Email / subtitle */}
       {/* Reputation stars */}
     </View>
   </BlurView>
   ```

3. Styles for glassmorphism:
   ```typescript
   glassHeader: {
     borderRadius: 16,         // borderRadius.lg
     overflow: 'hidden',        // REQUIRED for BlurView border radius to clip on Android
     marginHorizontal: 16,     // spacing.md
     marginBottom: 32,          // spacing.xl
   },
   goldOverlay: {
     ...StyleSheet.absoluteFillObject,
     backgroundColor: 'rgba(240, 192, 64, 0.08)',
   },
   headerContent: {
     alignItems: 'center',
     paddingVertical: 24,      // spacing.lg
     paddingHorizontal: 16,    // spacing.md
   },
   ```

4. **Premium Badge next to display name (per D-23):** If user is premium, render Badge `premium` variant next to the display name:
   ```typescript
   <View style={{ flexDirection: 'row', alignItems: 'center', gap: 8 }}>
     <Text variant="heading">{user.displayName}</Text>
     {isPremium && <Badge variant="premium" label="PRO" />}
   </View>
   ```

5. **Reputation stars (per D-25):** Leave the existing 5-point star system unchanged. Do NOT modify the star rendering logic.

**Info sections -> Card primitive (per D-20, D-28):**

6. Wrap each info section (friend code, linked accounts, member since, language) in `Card` from `@/src/components/ui/Card`:
   ```typescript
   <Card>
     <Text variant="label">Friend Code</Text>
     <Text variant="body">{user.friendCode}</Text>
   </Card>
   ```
   Same sections, same order — no restructuring (D-28).

**Button actions (per D-21, D-22):**

7. Replace Edit Profile TouchableOpacity/Pressable with:
   ```typescript
   <Button variant="primary" label="Edit Profile" onPress={handleEditProfile} />
   ```

8. Replace Logout TouchableOpacity/Pressable with:
   ```typescript
   <Button variant="destructive" label="Log Out" onPress={handleLogout} />
   ```
   Note: The logout handler should show Alert.alert confirmation first:
   - Title: "Log Out?"
   - Body: "You will be signed out of your account."
   - Confirm: "Log Out" (destructive style)
   - Cancel: "Cancel"

9. Replace Link/Unlink account buttons with Button `secondary` variant:
   ```typescript
   <Button variant="secondary" label="Link Account" onPress={() => handleLink(provider)} />
   <Button variant="secondary" label="Unlink Account" onPress={() => handleUnlink(provider)} />
   ```
   Unlink should show Alert.alert confirmation:
   - Title: "Unlink Account?"
   - Body: "You can re-link it later from your profile."
   - Confirm: "Unlink Account" (destructive style)
   - Cancel: "Cancel"

**Shimmer loading replacements (per D-26):**

10. Replace ALL `ActivityIndicator` loading states with inline shimmer:
    ```typescript
    import { ShimmerText } from '@/src/components/animation/ShimmerText';
    import { Shimmer } from '@/src/components/animation/Shimmer';

    // Where a text field is loading:
    {loading ? (
      <Shimmer><ShimmerText width="60%" /></Shimmer>
    ) : (
      <Text variant="body">{value}</Text>
    )}
    ```

**ProfileHeaderSkeleton for initial load (per D-27):**

11. For the full-screen initial loading state (before any user data is available), render:
    ```typescript
    import { ProfileHeaderSkeleton } from '@/src/components/skeleton/ProfileHeaderSkeleton';

    if (!user && loading) return <ProfileHeaderSkeleton />;
    ```

**Staggered entrance (per D-30):**

12. Add `useStaggeredList` for info section cards:
    ```typescript
    const sectionCount = loading ? 0 : infoSections.length;
    const { onLayout, getItemStyle } = useStaggeredList(sectionCount);
    ```
    Wrap each info Card section in `<Animated.View style={getItemStyle(index)}>`.
    Import `Animated` from `react-native-reanimated`.

**Gold pull-to-refresh (per D-29):**

13. Add gold RefreshControl to the ScrollView:
    ```typescript
    <ScrollView
      refreshControl={
        <RefreshControl
          refreshing={refreshing}
          onRefresh={handleRefresh}
          tintColor="#f0c040"
          colors={["#f0c040"]}
        />
      }
    >
    ```

**Toast migration (per D-31, Pitfall 4 from RESEARCH):**

14. Replace ALL `Toast.show` calls from `react-native-toast-message` with `useToast()`:
    ```typescript
    const toast = useToast();

    // Profile updated:
    toast.success('Profile updated');
    // Profile update failed:
    toast.error('Could not update profile. Please try again.');
    // Account linked:
    toast.success('Account linked');
    // Account unlinked:
    toast.success('Account unlinked');
    // Link/unlink failed:
    toast.error('Could not update account link. Please try again.');
    ```

15. Remove `import Toast from 'react-native-toast-message'` from profile.tsx if no other usages remain after migration.

**Text primitive adoption:**

16. Replace inline `<Text style={styles.xxx}>` with `<Text variant="heading">` for the screen title, `<Text variant="subheading">` for section headers, `<Text variant="body">` for content, `<Text variant="label">` for metadata labels (member since, language, etc.).
  </action>
  <verify>
    <automated>cd apps/mobile && grep -q "BlurView" app/\(tabs\)/profile.tsx && grep -q "expo-blur" app/\(tabs\)/profile.tsx && grep -q "rgba(240, 192, 64, 0.08)" app/\(tabs\)/profile.tsx && grep -q "overflow.*hidden" app/\(tabs\)/profile.tsx && grep -q 'variant="premium"' app/\(tabs\)/profile.tsx && grep -q "Card" app/\(tabs\)/profile.tsx && grep -q 'variant="primary"' app/\(tabs\)/profile.tsx && grep -q 'variant="destructive"' app/\(tabs\)/profile.tsx && grep -q "useStaggeredList" app/\(tabs\)/profile.tsx && grep -q "tintColor.*f0c040" app/\(tabs\)/profile.tsx && grep -q "useToast" app/\(tabs\)/profile.tsx && grep -q "ProfileHeaderSkeleton" app/\(tabs\)/profile.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - profile.tsx imports `BlurView` from `expo-blur`
    - profile.tsx contains `intensity={40}` and `tint="dark"` on BlurView
    - profile.tsx contains `rgba(240, 192, 64, 0.08)` for gold overlay
    - profile.tsx contains `overflow: 'hidden'` on the BlurView container style
    - profile.tsx contains `variant="premium"` Badge for display name premium indicator
    - profile.tsx imports Card from `@/src/components/ui/Card` and wraps info sections
    - profile.tsx contains `variant="primary"` for Edit Profile Button
    - profile.tsx contains `variant="destructive"` for Logout Button
    - profile.tsx contains `variant="secondary"` for Link/Unlink account Buttons
    - profile.tsx imports and calls `useStaggeredList` with loading gate
    - profile.tsx contains `tintColor="#f0c040"` in RefreshControl
    - profile.tsx imports `useToast` from `@/src/hooks/useToast`
    - profile.tsx contains `toast.success('Profile updated')` and `toast.success('Account linked')`
    - profile.tsx does NOT contain `import Toast from 'react-native-toast-message'` (migrated away)
    - profile.tsx does NOT contain `ActivityIndicator` (replaced with shimmer)
    - profile.tsx imports ProfileHeaderSkeleton for initial load
    - profile.tsx reputation stars remain unchanged (no modifications to star rendering)
    - Alert.alert called for Logout confirmation with "Log Out?" title
    - Alert.alert called for Unlink confirmation with "Unlink Account?" title
  </acceptance_criteria>
  <done>Profile tab has glassmorphism BlurView header with gold overlay, Card info sections, Button primary/destructive/secondary for actions, Badge premium, shimmer loading, ProfileHeaderSkeleton, staggered entrance, gold PTR, toast migration from react-native-toast-message to useToast, and Alert dialogs for destructive actions</done>
</task>

<task type="auto">
  <name>Task 2: PaywallCard migration — Card primitive with gold gradient top-border accent</name>
  <files>
    apps/mobile/src/components/premium/PaywallCard.tsx
  </files>
  <read_first>
    apps/mobile/src/components/premium/PaywallCard.tsx,
    apps/mobile/src/components/ui/Card.tsx,
    apps/mobile/src/components/ui/Text.tsx,
    apps/mobile/src/components/ui/Button.tsx,
    apps/mobile/src/components/animation/Shimmer.tsx
  </read_first>
  <action>
**PaywallCard.tsx migration (per D-24):**

1. Replace the outer View/container with `Card` primitive from `@/src/components/ui/Card`. If the PaywallCard has an onPress, pass it to Card's onPress.

2. Add a 2px gold gradient top-border accent using SVG LinearGradient (horizontal direction):
   ```typescript
   import Svg, { Defs, LinearGradient, Stop, Rect } from 'react-native-svg';

   // Inside the Card, add an absolutely positioned gradient strip at top:
   <View style={styles.topBorderAccent}>
     <Svg width="100%" height={2}>
       <Defs>
         <LinearGradient id="paywallTopGrad" x1="0" y1="0" x2="1" y2="0">
           <Stop offset="0" stopColor="#f5d060" stopOpacity="1" />
           <Stop offset="1" stopColor="#c9a020" stopOpacity="1" />
         </LinearGradient>
       </Defs>
       <Rect x="0" y="0" width="100%" height={2} fill="url(#paywallTopGrad)" />
     </Svg>
   </View>
   ```

3. Style for the top border:
   ```typescript
   topBorderAccent: {
     position: 'absolute',
     top: 0,
     left: 0,
     right: 0,
     height: 2,
     borderTopLeftRadius: 16,    // borderRadius.lg — match Card corners
     borderTopRightRadius: 16,
     overflow: 'hidden',
   },
   ```

4. The gradient ID `paywallTopGrad` is safe as a static string since there's only ever one PaywallCard on screen (not rendered in a list).

5. Replace any inline Text elements with Text primitive using appropriate variants (`subheading` for title, `body` for description).

6. Replace any CTA button with Button primitive `primary` variant if applicable.
  </action>
  <verify>
    <automated>cd apps/mobile && grep -q "Card" src/components/premium/PaywallCard.tsx && grep -q "LinearGradient" src/components/premium/PaywallCard.tsx && grep -q "f5d060" src/components/premium/PaywallCard.tsx && grep -q "c9a020" src/components/premium/PaywallCard.tsx && grep -q "paywallTopGrad" src/components/premium/PaywallCard.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - PaywallCard.tsx imports Card from `@/src/components/ui/Card`
    - PaywallCard.tsx imports Svg, Defs, LinearGradient, Stop, Rect from react-native-svg
    - PaywallCard.tsx contains `id="paywallTopGrad"` gradient definition
    - PaywallCard.tsx contains gradient stops `#f5d060` (gold[400]) and `#c9a020` (gold[600])
    - PaywallCard.tsx contains `height={2}` for the top border accent (or `height: 2`)
    - PaywallCard.tsx gradient direction is horizontal: `x1="0" y1="0" x2="1" y2="0"`
    - PaywallCard.tsx top border View has `position: 'absolute'` and `top: 0`
  </acceptance_criteria>
  <done>PaywallCard wraps in Card primitive with a 2px horizontal gold gradient top-border accent using SVG LinearGradient</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes from apps/mobile
- profile.tsx has BlurView glassmorphism header with gold overlay, Card sections, Button variants, Badge premium, shimmer, stagger, PTR, toasts
- PaywallCard.tsx has Card primitive + SVG gold gradient top border
- No ActivityIndicator remains in profile.tsx
- No react-native-toast-message import in profile.tsx
- Reputation stars unchanged
- Settings sections same order, no restructuring (D-28)
</verification>

<success_criteria>
Profile tab is fully refreshed: glassmorphism BlurView header with gold overlay wraps avatar and name area, info sections use Card primitives, Edit Profile/Logout/Link/Unlink use correct Button variants, premium users see gold Badge next to name, PaywallCard has gold gradient top border, all ActivityIndicator replaced with shimmer, ProfileHeaderSkeleton on initial load, staggered entrance on info sections, gold PTR, all toasts migrated from react-native-toast-message to useToast, destructive actions have Alert dialogs.
</success_criteria>

<output>
After completion, create `.planning/phases/17-screen-migration-tier-2/17-03-SUMMARY.md`
</output>
