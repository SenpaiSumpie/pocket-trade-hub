---
phase: 17-screen-migration-tier-2
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/src/components/market/PostCreationModal.tsx
  - apps/mobile/app/(tabs)/market.tsx
  - apps/mobile/app/(tabs)/profile.tsx
  - apps/mobile/app/edit-profile.tsx
autonomous: true
gap_closure: true
requirements: [SCR-04, SCR-05, SCR-06]

must_haves:
  truths:
    - "Market tab shows toast.error when post creation fails"
    - "Profile tab has no dead/unused imports"
    - "Edit profile success uses the new Zustand toast system for consistency"
  artifacts:
    - path: "apps/mobile/src/components/market/PostCreationModal.tsx"
      provides: "onError callback prop for creation failure"
      contains: "onError"
    - path: "apps/mobile/app/(tabs)/market.tsx"
      provides: "toast.error wired on post creation failure"
      contains: "toast.error"
    - path: "apps/mobile/app/(tabs)/profile.tsx"
      provides: "Clean imports with no dead Shimmer/ShimmerText"
    - path: "apps/mobile/app/edit-profile.tsx"
      provides: "Profile updated toast via new Zustand toast system"
      contains: "useToast"
  key_links:
    - from: "apps/mobile/app/(tabs)/market.tsx"
      to: "PostCreationModal.tsx"
      via: "onError callback prop"
      pattern: "onError.*toast\\.error"
    - from: "apps/mobile/app/edit-profile.tsx"
      to: "useToast.ts"
      via: "import useToast"
      pattern: "toast\\.success"
---

<objective>
Close three verification gaps from Phase 17: (1) wire toast.error for market post creation failure via a new onError prop on PostCreationModal, (2) remove dead ShimmerText/Shimmer imports from profile.tsx, (3) migrate edit-profile.tsx success toast from react-native-toast-message to the new Zustand toast system.

Purpose: All three Phase 17 plan acceptance criteria are met with zero dead code and consistent toast usage.
Output: Four modified files closing all three verification gaps.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-screen-migration-tier-2/17-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add onError prop to PostCreationModal and wire toast.error in market.tsx</name>
  <files>apps/mobile/src/components/market/PostCreationModal.tsx, apps/mobile/app/(tabs)/market.tsx</files>
  <read_first>
    - apps/mobile/src/components/market/PostCreationModal.tsx (current interface and handleCreate logic)
    - apps/mobile/app/(tabs)/market.tsx (current PostCreationModal usage)
  </read_first>
  <action>
In PostCreationModal.tsx:
1. Add `onError?: () => void` to the `PostCreationModalProps` interface (line 21-25), making it optional so existing callers are not broken:
   ```
   interface PostCreationModalProps {
     visible: boolean;
     onClose: () => void;
     onCreated: () => void;
     onError?: () => void;
   }
   ```
2. Destructure `onError` in the component function signature (line 38):
   ```
   export function PostCreationModal({ visible, onClose, onCreated, onError }: PostCreationModalProps) {
   ```
3. In `handleCreate` (around line 130-135), after `const result = await createPost(input);`, add an else branch that calls `onError?.()` and resets creating state:
   ```
   const result = await createPost(input);
   setCreating(false);
   if (result) {
     resetState();
     onCreated();
   } else {
     onError?.();
   }
   ```

In market.tsx:
4. Add `onError` prop to the PostCreationModal JSX (around line 160-168):
   ```
   <PostCreationModal
     visible={showCreate}
     onClose={() => setShowCreate(false)}
     onCreated={() => {
       setShowCreate(false);
       refresh();
       toast.success('Post created');
     }}
     onError={() => {
       toast.error('Could not create post. Please try again.');
     }}
   />
   ```
  </action>
  <verify>
    <automated>cd C:/Users/romec/OneDrive/Desktop/CodingProjects/pocket-trade-hub && grep -n "onError" apps/mobile/src/components/market/PostCreationModal.tsx && grep -n "toast.error" apps/mobile/app/(tabs)/market.tsx</automated>
  </verify>
  <acceptance_criteria>
    - PostCreationModal.tsx contains `onError?: () => void` in PostCreationModalProps interface
    - PostCreationModal.tsx contains `onError?.()` call in the else branch of handleCreate
    - market.tsx contains `onError={() =>` on the PostCreationModal JSX
    - market.tsx contains `toast.error('Could not create post. Please try again.')`
  </acceptance_criteria>
  <done>PostCreationModal exposes onError callback, market.tsx wires it to toast.error with the message "Could not create post. Please try again."</done>
</task>

<task type="auto">
  <name>Task 2: Remove dead imports from profile.tsx and migrate edit-profile toast to Zustand system</name>
  <files>apps/mobile/app/(tabs)/profile.tsx, apps/mobile/app/edit-profile.tsx</files>
  <read_first>
    - apps/mobile/app/(tabs)/profile.tsx (lines 25-26 for dead imports)
    - apps/mobile/app/edit-profile.tsx (full file for Toast.show usage and imports)
    - apps/mobile/src/hooks/useToast.ts (API reference for the new toast hook)
  </read_first>
  <action>
In profile.tsx:
1. Remove line 25 (`import { ShimmerText } from '@/src/components/animation/ShimmerText';`)
2. Remove line 26 (`import { Shimmer } from '@/src/components/animation/Shimmer';`)
No other changes — the Button `loading` prop is the accepted pattern for inline loading states.

In edit-profile.tsx:
3. Remove the `import Toast from 'react-native-toast-message';` import (line 14)
4. Add `import { useToast } from '@/src/hooks/useToast';` in the imports section
5. Inside the component function, add `const toast = useToast();` near the top (after existing useState/store hooks)
6. Replace all `Toast.show({ type: 'error', text1: ..., text2: ... })` calls with `toast.error(...)`:
   - Validation error (around line 68-73): Replace `Toast.show({ type: 'error', text1: t('profile.validationError'), text2: firstError?.message || t('profile.checkInput') })` with `toast.error(firstError?.message || t('profile.checkInput'))`
7. Replace `Toast.show({ type: 'success', text1: t('profile.profileUpdated'), visibilityTime: 1500 })` (around line 90-94) with `toast.success(t('profile.profileUpdated'))`
8. Check for any other `Toast.show` calls in the file and migrate them to the new `toast.error()` / `toast.success()` pattern
  </action>
  <verify>
    <automated>cd C:/Users/romec/OneDrive/Desktop/CodingProjects/pocket-trade-hub && grep -c "ShimmerText\|Shimmer" apps/mobile/app/\(tabs\)/profile.tsx && grep -n "useToast\|toast\." apps/mobile/app/edit-profile.tsx && grep -c "Toast.show\|react-native-toast-message" apps/mobile/app/edit-profile.tsx</automated>
  </verify>
  <acceptance_criteria>
    - profile.tsx does NOT contain `import { ShimmerText }` or `import { Shimmer }`
    - profile.tsx grep for "ShimmerText" and "Shimmer" returns 0 matches
    - edit-profile.tsx does NOT contain `import Toast from 'react-native-toast-message'`
    - edit-profile.tsx contains `import { useToast } from '@/src/hooks/useToast'`
    - edit-profile.tsx contains `const toast = useToast()`
    - edit-profile.tsx contains `toast.success(t('profile.profileUpdated'))` (or equivalent)
    - edit-profile.tsx grep for "Toast.show" returns 0 matches
  </acceptance_criteria>
  <done>Dead ShimmerText/Shimmer imports removed from profile.tsx. edit-profile.tsx migrated from react-native-toast-message to useToast Zustand system for all success/error toasts.</done>
</task>

</tasks>

<verification>
1. `grep -rn "ShimmerText\|from.*Shimmer" apps/mobile/app/(tabs)/profile.tsx` returns no matches
2. `grep -n "onError" apps/mobile/src/components/market/PostCreationModal.tsx` shows prop definition and invocation
3. `grep -n "toast.error" apps/mobile/app/(tabs)/market.tsx` shows post creation error toast
4. `grep -n "useToast\|toast\.\(success\|error\)" apps/mobile/app/edit-profile.tsx` shows new toast system usage
5. `grep -c "Toast.show\|react-native-toast-message" apps/mobile/app/edit-profile.tsx` returns 0
6. `npx tsc --noEmit` passes without type errors on modified files
</verification>

<success_criteria>
- All three verification gaps from 17-VERIFICATION.md are closed
- Gap 1: market.tsx wires toast.error via PostCreationModal onError callback
- Gap 2: profile.tsx has zero dead imports (no ShimmerText, no Shimmer)
- Gap 3: edit-profile.tsx uses Zustand toast system for "Profile updated" success message
- No anti-patterns: zero dead imports, consistent toast system usage
</success_criteria>

<output>
After completion, create `.planning/phases/17-screen-migration-tier-2/17-04-SUMMARY.md`
</output>
