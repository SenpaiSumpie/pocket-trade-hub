---
phase: 15-animation-utilities-and-motion-system
plan: 05
type: execute
wave: 2
depends_on: [04]
files_modified:
  - apps/mobile/src/components/cards/CardDetailModal.tsx
  - apps/mobile/src/components/market/PostDetailModal.tsx
  - apps/mobile/src/components/meta/DeckDetailModal.tsx
  - apps/mobile/src/components/trades/MatchDetailModal.tsx
  - apps/mobile/src/components/trades/MyPostDetailModal.tsx
  - apps/mobile/src/components/trades/ProposalDetailModal.tsx
autonomous: true
requirements: [MOT-04]

must_haves:
  truths:
    - "All 6 detail modals use DetailSheet instead of React Native Modal"
    - "All 6 modals still open and close via visible/onClose props (API unchanged for consumers)"
    - "Modal content is scrollable inside BottomSheetScrollView"
    - "Close button (X) is removed since DetailSheet provides drag-to-dismiss and backdrop tap"
    - "Creation modals and small dialogs are NOT converted (PostCreationModal, ProposalCreationModal, RatingModal, AddToCollectionModal, LinkAccountModal)"
  artifacts:
    - path: "apps/mobile/src/components/cards/CardDetailModal.tsx"
      provides: "Card detail as bottom sheet"
      contains: "DetailSheet"
    - path: "apps/mobile/src/components/market/PostDetailModal.tsx"
      provides: "Post detail as bottom sheet"
      contains: "DetailSheet"
    - path: "apps/mobile/src/components/meta/DeckDetailModal.tsx"
      provides: "Deck detail as bottom sheet"
      contains: "DetailSheet"
    - path: "apps/mobile/src/components/trades/MatchDetailModal.tsx"
      provides: "Match detail as bottom sheet"
      contains: "DetailSheet"
    - path: "apps/mobile/src/components/trades/MyPostDetailModal.tsx"
      provides: "My post detail as bottom sheet"
      contains: "DetailSheet"
    - path: "apps/mobile/src/components/trades/ProposalDetailModal.tsx"
      provides: "Proposal detail as bottom sheet"
      contains: "DetailSheet"
  key_links:
    - from: "apps/mobile/src/components/cards/CardDetailModal.tsx"
      to: "apps/mobile/src/components/animation/DetailSheet.tsx"
      via: "import DetailSheet"
      pattern: "import.*DetailSheet.*from.*animation/DetailSheet"
    - from: "apps/mobile/src/components/trades/ProposalDetailModal.tsx"
      to: "apps/mobile/src/components/animation/DetailSheet.tsx"
      via: "import DetailSheet"
      pattern: "import.*DetailSheet.*from.*animation/DetailSheet"
---

<objective>
Convert 6 detail modals from React Native Modal to DetailSheet bottom sheets.

Purpose: Replace static full-screen modals with gesture-driven bottom sheets (drag-to-dismiss, backdrop tap dismiss) per D-01. This is the largest single change in the phase but follows a consistent mechanical pattern for each modal.
Output: All 6 detail modals using DetailSheet wrapper instead of React Native Modal
</objective>

<execution_context>
@C:/Users/romec/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/romec/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-animation-utilities-and-motion-system/15-UI-SPEC.md
@.planning/phases/15-animation-utilities-and-motion-system/15-04-SUMMARY.md

<interfaces>
From apps/mobile/src/components/animation/DetailSheet.tsx (created by Plan 04):
```typescript
interface DetailSheetProps {
  visible: boolean;
  onDismiss: () => void;
  children: React.ReactNode;
  initialSnap?: number; // default 0 (60% peek)
}

export function DetailSheet({ visible, onDismiss, children, initialSnap }: DetailSheetProps) {
  // BottomSheet with snap points ['60%', '92%'], enablePanDownToClose, backdrop with pressBehavior="close"
  // Children rendered inside BottomSheetScrollView
}
```

Modal prop patterns found across all 6 files:
```typescript
// All 6 modals use: visible: boolean, onClose: () => void
// DetailSheet uses: visible: boolean, onDismiss: () => void
// Migration: pass onClose as onDismiss prop
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert CardDetailModal, PostDetailModal, and DeckDetailModal to DetailSheet</name>
  <files>apps/mobile/src/components/cards/CardDetailModal.tsx, apps/mobile/src/components/market/PostDetailModal.tsx, apps/mobile/src/components/meta/DeckDetailModal.tsx</files>
  <read_first>
    - apps/mobile/src/components/animation/DetailSheet.tsx (DetailSheet API — visible, onDismiss, children)
    - apps/mobile/src/components/cards/CardDetailModal.tsx (full file — 1106 lines, largest modal)
    - apps/mobile/src/components/market/PostDetailModal.tsx (full file — 432 lines)
    - apps/mobile/src/components/meta/DeckDetailModal.tsx (full file — 430 lines)
  </read_first>
  <action>
For EACH of the 3 modals, apply this consistent conversion pattern:

**1. Add DetailSheet import, remove Modal import:**
```typescript
// REMOVE: import { Modal } from 'react-native';
// ADD:
import { DetailSheet } from '@/src/components/animation/DetailSheet';
```

Remove `Modal` from the `react-native` import destructuring. Keep all other RN imports (View, Text, Pressable, ScrollView, etc.). If `ScrollView` was only used as the modal's scroll container, it can be removed too since DetailSheet uses BottomSheetScrollView internally.

**2. Replace `<Modal>` wrapper with `<DetailSheet>`:**
```typescript
// BEFORE:
<Modal visible={visible} animationType="slide" transparent onRequestClose={onClose}>
  <View style={styles.overlay}>
    <View style={styles.container}>
      <ScrollView>
        {/* content */}
      </ScrollView>
    </View>
  </View>
</Modal>

// AFTER:
<DetailSheet visible={visible} onDismiss={onClose}>
  {/* content — NO outer overlay View, NO container View, NO ScrollView wrapper */}
</DetailSheet>
```

**3. Remove modal chrome elements:**
- Remove the close button (`X` icon Pressable at the top) — DetailSheet provides drag-to-dismiss and backdrop tap
- Remove the overlay View (semi-transparent background) — DetailSheet has its own backdrop
- Remove the container View with borderRadius and backgroundColor — DetailSheet has its own background
- Remove ScrollView wrapper — DetailSheet wraps children in BottomSheetScrollView
- Keep ALL content inside the modal (cards, lists, buttons, text) unchanged

**4. Remove modal-specific styles:**
- Remove `overlay` style (the semi-transparent background)
- Remove `container` style (the white/dark card with border radius)
- Remove `closeBtn` / `closeButton` style
- Keep all content-related styles unchanged

**5. Do NOT change:**
- The component's props interface (visible, onClose, other props stay the same)
- The component's export name
- Any internal state or business logic
- Any child components rendered inside the modal
- Any other modals within the file (e.g., CardDetailModal has an internal LuckCalculator modal that stays as Modal)

**Specific notes per file:**

**CardDetailModal.tsx (1106 lines):**
- Has an internal `<LuckCalculator>` modal — DO NOT convert this, it stays as `<Modal>`
- Has a complex multi-tab content area — keep all tabs and content intact
- The `onClose` prop maps to `onDismiss` on DetailSheet
- Remove the `<Pressable onPress={onClose} hitSlop={12} style={styles.closeBtn}>` close button

**PostDetailModal.tsx (432 lines):**
- Has action buttons (propose trade, etc.) — keep them
- Remove `<Pressable onPress={onClose} style={styles.closeButton}>` close button

**DeckDetailModal.tsx (430 lines):**
- Has a card list/grid — keep it
- Remove `<Pressable style={styles.closeButton} onPress={onClose}>` close button
  </action>
  <verify>
    <automated>grep -q "DetailSheet" apps/mobile/src/components/cards/CardDetailModal.tsx && grep -q "DetailSheet" apps/mobile/src/components/market/PostDetailModal.tsx && grep -q "DetailSheet" apps/mobile/src/components/meta/DeckDetailModal.tsx && ! grep -q "<Modal visible={visible}" apps/mobile/src/components/cards/CardDetailModal.tsx && ! grep -q "<Modal visible={visible}" apps/mobile/src/components/market/PostDetailModal.tsx && ! grep -q "<Modal visible={visible}" apps/mobile/src/components/meta/DeckDetailModal.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - apps/mobile/src/components/cards/CardDetailModal.tsx contains `import { DetailSheet }` or `import { DetailSheet } from`
    - apps/mobile/src/components/cards/CardDetailModal.tsx contains `<DetailSheet` with `onDismiss`
    - apps/mobile/src/components/cards/CardDetailModal.tsx does NOT contain `<Modal visible={visible}` as the outer modal (internal LuckCalculator Modal may remain)
    - apps/mobile/src/components/cards/CardDetailModal.tsx does NOT contain `closeBtn` or close X button Pressable for the main modal
    - apps/mobile/src/components/market/PostDetailModal.tsx contains `<DetailSheet` with `onDismiss`
    - apps/mobile/src/components/market/PostDetailModal.tsx does NOT contain `<Modal`
    - apps/mobile/src/components/meta/DeckDetailModal.tsx contains `<DetailSheet` with `onDismiss`
    - apps/mobile/src/components/meta/DeckDetailModal.tsx does NOT contain `<Modal`
    - All 3 files still export the same named function (CardDetailModal, PostDetailModal, DeckDetailModal)
    - All 3 files still accept `visible` and `onClose` in their props interface
  </acceptance_criteria>
  <done>CardDetailModal, PostDetailModal, and DeckDetailModal use DetailSheet instead of Modal, with close buttons and overlay chrome removed, all content and business logic preserved</done>
</task>

<task type="auto">
  <name>Task 2: Convert MatchDetailModal, MyPostDetailModal, and ProposalDetailModal to DetailSheet</name>
  <files>apps/mobile/src/components/trades/MatchDetailModal.tsx, apps/mobile/src/components/trades/MyPostDetailModal.tsx, apps/mobile/src/components/trades/ProposalDetailModal.tsx</files>
  <read_first>
    - apps/mobile/src/components/animation/DetailSheet.tsx (DetailSheet API)
    - apps/mobile/src/components/trades/MatchDetailModal.tsx (full file — 277 lines)
    - apps/mobile/src/components/trades/MyPostDetailModal.tsx (full file — 320 lines)
    - apps/mobile/src/components/trades/ProposalDetailModal.tsx (full file — 669 lines)
  </read_first>
  <action>
Apply the same conversion pattern as Task 1 to these 3 trade-related modals:

**For each modal:**

1. Add `import { DetailSheet } from '@/src/components/animation/DetailSheet';`
2. Remove `Modal` from `react-native` imports
3. Replace `<Modal visible={visible} animationType="slide" transparent onRequestClose={onClose}>` with `<DetailSheet visible={visible} onDismiss={onClose}>`
4. Remove overlay View, container View, ScrollView wrapper, close button
5. Keep all content, business logic, child components, props interface, and export name

**Specific notes per file:**

**MatchDetailModal.tsx (277 lines):**
- Has an internal `ProposalCreationModal` — DO NOT convert that, it stays as `<Modal>` per D-02 (creation modals stay)
- Has `<TouchableOpacity style={styles.closeButton} onPress={onClose}>` — remove this close button
- The `visible` and `onClose` props map to DetailSheet's `visible` and `onDismiss`

**MyPostDetailModal.tsx (320 lines):**
- Has delete and close post actions — keep all action logic
- Has `<TouchableOpacity style={styles.closeButton} onPress={onClose}>` — remove this close button
- Uses `animationType="slide"` — remove since DetailSheet handles its own animation

**ProposalDetailModal.tsx (669 lines):**
- Second-largest modal after CardDetailModal
- Has accept/reject/counter proposal actions — keep all of them
- Has internal modals (if any for rating) — keep those as `<Modal>`
- Remove the close button and modal chrome
  </action>
  <verify>
    <automated>grep -q "DetailSheet" apps/mobile/src/components/trades/MatchDetailModal.tsx && grep -q "DetailSheet" apps/mobile/src/components/trades/MyPostDetailModal.tsx && grep -q "DetailSheet" apps/mobile/src/components/trades/ProposalDetailModal.tsx && ! grep -q "animationType" apps/mobile/src/components/trades/MatchDetailModal.tsx && ! grep -q "animationType" apps/mobile/src/components/trades/MyPostDetailModal.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - apps/mobile/src/components/trades/MatchDetailModal.tsx contains `<DetailSheet` with `onDismiss`
    - apps/mobile/src/components/trades/MatchDetailModal.tsx does NOT contain `<Modal visible={visible}` as the outer modal (internal ProposalCreationModal Modal may remain)
    - apps/mobile/src/components/trades/MyPostDetailModal.tsx contains `<DetailSheet` with `onDismiss`
    - apps/mobile/src/components/trades/MyPostDetailModal.tsx does NOT contain `<Modal`
    - apps/mobile/src/components/trades/ProposalDetailModal.tsx contains `<DetailSheet` with `onDismiss`
    - apps/mobile/src/components/trades/ProposalDetailModal.tsx does NOT contain `<Modal visible={visible}` as the outer modal
    - All 3 files still export the same named function
    - All 3 files still accept `visible` and `onClose` in their props interface
    - No `animationType` prop remains in any of the 3 files
    - MatchDetailModal still contains ProposalCreationModal (internal creation modal preserved as Modal per D-02)
  </acceptance_criteria>
  <done>MatchDetailModal, MyPostDetailModal, and ProposalDetailModal use DetailSheet instead of Modal, with close buttons and overlay chrome removed, all content and business logic preserved, internal creation/dialog modals kept as Modal per D-02</done>
</task>

</tasks>

<verification>
- `grep -r "DetailSheet" apps/mobile/src/components/cards/ apps/mobile/src/components/market/ apps/mobile/src/components/meta/ apps/mobile/src/components/trades/` shows all 6 detail modals import DetailSheet
- `grep -rn "<Modal" apps/mobile/src/components/cards/CardDetailModal.tsx apps/mobile/src/components/market/PostDetailModal.tsx apps/mobile/src/components/meta/DeckDetailModal.tsx apps/mobile/src/components/trades/MatchDetailModal.tsx apps/mobile/src/components/trades/MyPostDetailModal.tsx apps/mobile/src/components/trades/ProposalDetailModal.tsx` — only internal child modals should remain (LuckCalculator, ProposalCreationModal), not the outer detail modal
- Creation modals NOT touched: `grep -l "Modal" apps/mobile/src/components/market/PostCreationModal.tsx apps/mobile/src/components/trades/ProposalCreationModal.tsx` should still show Modal usage
</verification>

<success_criteria>
- All 6 detail modals (CardDetail, PostDetail, DeckDetail, MatchDetail, MyPostDetail, ProposalDetail) use DetailSheet
- No outer `<Modal>` wrapper remains in any of the 6 files (internal child modals OK)
- Close buttons removed from all 6 modals (DetailSheet handles dismissal)
- All content, business logic, and child components preserved
- Props interfaces unchanged (visible, onClose still accepted by all)
- Creation modals and small dialogs NOT converted (D-02 respected)
</success_criteria>

<output>
After completion, create `.planning/phases/15-animation-utilities-and-motion-system/15-05-SUMMARY.md`
</output>
