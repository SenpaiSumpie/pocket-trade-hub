---
phase: 16-screen-migration-tier-1
plan: 04
type: execute
wave: 2
depends_on: [16-01, 16-02]
files_modified:
  - apps/mobile/src/components/trades/MyPostCard.tsx
  - apps/mobile/src/components/trades/ProposalCard.tsx
  - apps/mobile/src/components/trades/MatchCard.tsx
  - apps/mobile/app/(tabs)/trades.tsx
autonomous: true
requirements: [SCR-03, CL-06]

must_haves:
  truths:
    - "MyPostCard, ProposalCard, and MatchCard use Card primitive with animated press and haptics"
    - "Trades tab shows PostListSkeleton or ProposalListSkeleton shimmer while loading (not ActivityIndicator)"
    - "Trades tab shows contextual EmptyState with CTA per segment (no posts, no proposals, no matches)"
    - "Post and proposal list items stagger-animate on initial load"
    - "Segment tabs and filter chips use consistent Button/Badge styling"
    - "Pull-to-refresh spinner is gold-tinted"
  artifacts:
    - path: "apps/mobile/src/components/trades/MyPostCard.tsx"
      provides: "Post card using Card primitive with animated press"
      contains: "Card"
    - path: "apps/mobile/src/components/trades/ProposalCard.tsx"
      provides: "Proposal card using Card primitive with animated press"
      contains: "Card"
    - path: "apps/mobile/src/components/trades/MatchCard.tsx"
      provides: "Match card using Card primitive with animated press"
      contains: "Card"
    - path: "apps/mobile/app/(tabs)/trades.tsx"
      provides: "Trades tab with skeletons, empty states, staggered list, gold refresh"
      contains: "useStaggeredList"
  key_links:
    - from: "apps/mobile/src/components/trades/MyPostCard.tsx"
      to: "apps/mobile/src/components/ui/Card.tsx"
      via: "Card primitive wrapper"
      pattern: "import.*Card.*from.*ui"
    - from: "apps/mobile/app/(tabs)/trades.tsx"
      to: "apps/mobile/src/components/skeleton/PostListSkeleton.tsx"
      via: "Skeleton in loading state"
      pattern: "PostListSkeleton"
    - from: "apps/mobile/app/(tabs)/trades.tsx"
      to: "apps/mobile/src/hooks/useStaggeredList.ts"
      via: "Staggered list entrance"
      pattern: "useStaggeredList"
---

<objective>
Migrate the Trades tab — update MyPostCard, ProposalCard, and MatchCard to use Card primitive with animated press, replace loading indicators with skeleton compositions, add contextual empty states with CTAs, apply staggered list animation and gold pull-to-refresh.

Purpose: Trades tab is the core trading experience — animated cards and proper loading/empty states make the workflow feel responsive and guide users toward actions.

Output: 3 migrated trade card components + fully updated trades.tsx.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-screen-migration-tier-1/16-CONTEXT.md
@.planning/phases/16-screen-migration-tier-1/16-UI-SPEC.md
@.planning/phases/16-screen-migration-tier-1/16-01-SUMMARY.md
@.planning/phases/16-screen-migration-tier-1/16-02-SUMMARY.md

<interfaces>
<!-- Primitives from Plan 01 -->
From apps/mobile/src/components/ui/index.ts:
```typescript
export { Button, ButtonProps } from './Button';
export { Card, CardProps } from './Card';
export { Text, TextProps } from './Text';
export { Badge, BadgeProps } from './Badge';
export { EmptyState, EmptyStateProps } from './EmptyState';
```

<!-- Skeletons from Plan 02 -->
```typescript
export function PostListSkeleton(): JSX.Element;    // 3 avatar+text shimmer rows
export function ProposalListSkeleton(): JSX.Element; // 3 avatar+preview+text shimmer rows
```

<!-- Phase 15 hooks -->
```typescript
export function useStaggeredList(itemCount: number): {
  onLayout: () => void;
  getItemStyle: (index: number) => object;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate MyPostCard, ProposalCard, and MatchCard to Card primitive</name>
  <files>
    apps/mobile/src/components/trades/MyPostCard.tsx
    apps/mobile/src/components/trades/ProposalCard.tsx
    apps/mobile/src/components/trades/MatchCard.tsx
  </files>
  <read_first>
    apps/mobile/src/components/trades/MyPostCard.tsx
    apps/mobile/src/components/trades/ProposalCard.tsx
    apps/mobile/src/components/trades/MatchCard.tsx
    apps/mobile/src/components/ui/Card.tsx (from Plan 01)
    apps/mobile/src/components/ui/Text.tsx (from Plan 01)
    apps/mobile/src/components/ui/Badge.tsx (from Plan 01)
    apps/mobile/src/constants/theme.ts
  </read_first>
  <action>
Migrate all 3 trade card components to use the new UI primitives (per D-39):

**MyPostCard.tsx:**
1. Replace the outer `TouchableOpacity` with the `Card` primitive: `import { Card, Text, Badge } from '@/src/components/ui'`
2. Pass the existing `onPress` handler to `Card`'s `onPress` prop — this gives animated scale (0.97) + haptic feedback automatically
3. Remove `activeOpacity={0.7}` — Card primitive handles press feedback via Reanimated
4. Replace inline-styled `Text` elements with the `Text` primitive:
   - Post title/description: `<Text preset="body">{post.title}</Text>`
   - Secondary info (date, card count): `<Text preset="label">{formattedDate}</Text>`
5. Replace any inline badge/status styling with `Badge` primitive:
   - Post status badges: use `variant="success"` for active, `variant="warning"` for pending, etc.
6. Keep internal layout structure (flexDirection, spacing) — only replace outer container + text/badge styling
7. Remove the old `TouchableOpacity` import if no longer used

**ProposalCard.tsx:**
1. Same Card primitive wrapping as MyPostCard — replace outer `TouchableOpacity` with `<Card onPress={onPress}>`
2. Remove `activeOpacity={0.7}`
3. Replace inline Text elements with `Text` primitive using appropriate presets
4. Replace status indicator styling with `Badge` primitive:
   - Proposal status: `variant="success"` for accepted, `variant="error"` for declined, `variant="warning"` for pending, `variant="default"` for countered
5. Keep card thumbnail previews and partner info layout intact

**MatchCard.tsx:**
1. Same Card primitive wrapping — replace outer touchable with `<Card onPress={onPress}>`
2. Remove `activeOpacity={0.7}`
3. Replace inline Text with `Text` primitive:
   - Partner name: `<Text preset="subheading">{partner.name}</Text>`
   - Match details: `<Text preset="body">{matchDescription}</Text>`
   - Card counts: `<Text preset="label">{cardCount} cards</Text>`
4. Keep existing card thumbnail/avatar rendering intact

**For all 3 components:** Remove old `StyleSheet.create` entries that are now handled by Card/Text/Badge primitives (backgroundColor, borderRadius, elevation). Keep layout-specific styles (flexDirection, flex, marginLeft, etc.) that control internal arrangement.
  </action>
  <verify>
    <automated>grep "import.*Card.*from.*@/src/components/ui" apps/mobile/src/components/trades/MyPostCard.tsx apps/mobile/src/components/trades/ProposalCard.tsx apps/mobile/src/components/trades/MatchCard.tsx && ! grep "TouchableOpacity" apps/mobile/src/components/trades/MyPostCard.tsx && ! grep "TouchableOpacity" apps/mobile/src/components/trades/ProposalCard.tsx && ! grep "TouchableOpacity" apps/mobile/src/components/trades/MatchCard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - MyPostCard.tsx contains `import { Card` from `@/src/components/ui`
    - MyPostCard.tsx contains `<Card onPress=` (Card primitive wrapping content)
    - MyPostCard.tsx does NOT contain `TouchableOpacity` or `activeOpacity`
    - ProposalCard.tsx contains `import { Card` from `@/src/components/ui`
    - ProposalCard.tsx contains `<Card onPress=`
    - ProposalCard.tsx does NOT contain `TouchableOpacity` or `activeOpacity`
    - MatchCard.tsx contains `import { Card` from `@/src/components/ui`
    - MatchCard.tsx contains `<Card onPress=`
    - MatchCard.tsx does NOT contain `TouchableOpacity` or `activeOpacity`
    - All 3 files import `Text` or `Badge` from `@/src/components/ui`
  </acceptance_criteria>
  <done>MyPostCard, ProposalCard, and MatchCard all use Card primitive (animated press + haptics), Text primitive for typography, and Badge primitive for status indicators — no TouchableOpacity remains</done>
</task>

<task type="auto">
  <name>Task 2: Migrate trades.tsx with skeletons, empty states, staggered list, and gold refresh</name>
  <files>
    apps/mobile/app/(tabs)/trades.tsx
  </files>
  <read_first>
    apps/mobile/app/(tabs)/trades.tsx
    apps/mobile/src/hooks/useStaggeredList.ts
    apps/mobile/src/components/skeleton/PostListSkeleton.tsx (from Plan 02)
    apps/mobile/src/components/skeleton/ProposalListSkeleton.tsx (from Plan 02)
    apps/mobile/src/components/ui/EmptyState.tsx (from Plan 01)
    apps/mobile/src/components/ui/Badge.tsx (from Plan 01)
    apps/mobile/src/components/ui/Button.tsx (from Plan 01)
    .planning/phases/16-screen-migration-tier-1/16-UI-SPEC.md (lines 249-257 for Trades empty state copy)
  </read_first>
  <action>
Migrate trades.tsx tab screen (per D-39 through D-43):

1. **Add imports:**
   ```typescript
   import { Badge, EmptyState, Text, Button } from '@/src/components/ui';
   import { PostListSkeleton } from '@/src/components/skeleton/PostListSkeleton';
   import { ProposalListSkeleton } from '@/src/components/skeleton/ProposalListSkeleton';
   import { useStaggeredList } from '@/src/hooks/useStaggeredList';
   import Animated from 'react-native-reanimated';
   import { RefreshControl } from 'react-native';
   import { Tag, ArrowsLeftRight, Sparkle } from 'phosphor-react-native';
   ```

2. **Skeleton loading states** (D-42):
   - Find existing loading state rendering (likely `ActivityIndicator` or conditional rendering)
   - When in "posts" segment and loading: render `<PostListSkeleton />`
   - When in "proposals" segment and loading: render `<ProposalListSkeleton />`
   - Remove any `ActivityIndicator` imports and usage for list loading

3. **Empty states with contextual CTAs** (D-43, UI-SPEC copy):
   - Posts segment empty: `<EmptyState icon={Tag} title="No posts yet" subtitle="Create a post to start trading with other players" ctaLabel="Create Post" onCta={() => { /* navigate to post creation */ }} />`
   - Proposals segment empty: `<EmptyState icon={ArrowsLeftRight} title="No proposals yet" subtitle="When other players propose trades, they'll appear here" />`
   - Matches segment empty (if applicable): `<EmptyState icon={Sparkle} title="No matches found" subtitle="Add more cards to your wishlist to find trade partners" ctaLabel="Go to Wishlist" onCta={() => { /* navigate to wishlist */ }} />`
   - Replace any existing inline empty state markup (referenced in RESEARCH.md as trades.tsx lines 271-299) with the EmptyState component

4. **Segment tabs and filter chips styling** (D-40):
   - Find existing segment tab rendering (posts/proposals toggle)
   - Style selected segment like a primary Button look (gold background, dark text) and unselected like ghost variant
   - Style direction filter chips using Badge primitive: `<Badge variant="default" label={filterLabel} />` for unselected, highlighted variant for selected
   - Keep the onPress handlers and functionality — only update the visual rendering

5. **Staggered list animation** (D-41):
   - Call `useStaggeredList(items.length)` where `items` is the current segment's data (posts or proposals)
   - In FlashList `renderItem`, wrap each item in `<Animated.View style={getItemStyle(index)}>...</Animated.View>`
   - Add `onLayout` to a container above the FlashList or to `ListHeaderComponent`
   - IMPORTANT: Per Pitfall 4 from RESEARCH.md — gate staggeredList count behind loaded data. Pass 0 or don't trigger animation while loading.

6. **Gold pull-to-refresh** (D-22, D-23, D-24):
   - Find existing `refreshing` / `onRefresh` on FlashList
   - Replace with:
     ```typescript
     refreshControl={
       <RefreshControl
         refreshing={refreshing}
         onRefresh={handleRefresh}
         tintColor="#f0c040"
         colors={["#f0c040"]}
       />
     }
     ```
   - Remove inline `refreshing` and `onRefresh` props from FlashList (they conflict)

7. **Text primitive adoption**: Replace major inline-styled RN Text elements with the `Text` primitive where applicable (section titles, headers). Keep highly custom text formatting that doesn't map to a preset.
  </action>
  <verify>
    <automated>grep "PostListSkeleton\|ProposalListSkeleton" apps/mobile/app/\(tabs\)/trades.tsx && grep "EmptyState" apps/mobile/app/\(tabs\)/trades.tsx && grep "useStaggeredList" apps/mobile/app/\(tabs\)/trades.tsx && grep "tintColor.*#f0c040" apps/mobile/app/\(tabs\)/trades.tsx && ! grep "ActivityIndicator" apps/mobile/app/\(tabs\)/trades.tsx</automated>
  </verify>
  <acceptance_criteria>
    - trades.tsx contains `import { PostListSkeleton }` and `import { ProposalListSkeleton }`
    - trades.tsx renders `<PostListSkeleton` in posts loading state
    - trades.tsx renders `<ProposalListSkeleton` in proposals loading state
    - trades.tsx does NOT contain `ActivityIndicator` for list loading
    - trades.tsx contains `<EmptyState` with `icon={Tag}` and `title="No posts yet"` for posts empty
    - trades.tsx contains `<EmptyState` with `icon={ArrowsLeftRight}` and `title="No proposals yet"` for proposals empty
    - trades.tsx contains `useStaggeredList(` and `getItemStyle(`
    - trades.tsx contains `tintColor="#f0c040"` in RefreshControl
    - trades.tsx contains `<RefreshControl` component
    - trades.tsx imports from `@/src/components/ui` (Badge, EmptyState, or Text)
  </acceptance_criteria>
  <done>Trades tab fully migrated: PostListSkeleton/ProposalListSkeleton for loading, contextual EmptyState per segment, staggered list entrance, Badge/Button-styled segment tabs and filters, gold pull-to-refresh, no ActivityIndicator or inline empty state markup remaining</done>
</task>

</tasks>

<verification>
- MyPostCard, ProposalCard, MatchCard all use Card primitive with onPress
- No TouchableOpacity remains in trade card components
- Trades loading shows shimmer skeletons (not spinners)
- Empty posts/proposals/matches show EmptyState with correct copy and CTA
- List items stagger-animate on first load
- Pull-to-refresh is gold-tinted
</verification>

<success_criteria>
- 3 trade card components migrated to Card + Text + Badge primitives
- trades.tsx uses skeleton compositions for loading
- trades.tsx uses EmptyState for empty segments
- trades.tsx uses useStaggeredList for entrance animation
- trades.tsx uses gold RefreshControl
- No regressions in trade functionality (CRUD, navigation, filtering)
</success_criteria>

<output>
After completion, create `.planning/phases/16-screen-migration-tier-1/16-04-SUMMARY.md`
</output>
