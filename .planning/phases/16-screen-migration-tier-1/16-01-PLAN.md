---
phase: 16-screen-migration-tier-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/src/components/ui/Text.tsx
  - apps/mobile/src/components/ui/Badge.tsx
  - apps/mobile/src/components/ui/Divider.tsx
  - apps/mobile/src/components/ui/Input.tsx
  - apps/mobile/src/components/ui/Card.tsx
  - apps/mobile/src/components/ui/Button.tsx
  - apps/mobile/src/components/ui/EmptyState.tsx
  - apps/mobile/src/components/ui/index.ts
autonomous: true
requirements: [CL-01, CL-03, CL-05]

must_haves:
  truths:
    - "Button renders in 4 variants (primary/secondary/ghost/destructive) and 3 sizes (sm/md/lg) with animated press and haptics"
    - "Card renders as a surface container with optional onPress triggering animated press and haptics"
    - "Text renders with 4 typography presets (heading/subheading/body/label) using correct font sizes and weights"
    - "Badge renders in 7 variants (default/success/warning/error/rarity-diamond/rarity-star/rarity-crown)"
    - "Input renders with surface background, focus ring in gold, error state in red, label above"
    - "Divider renders as a hairline rule in border color with configurable vertical spacing"
    - "EmptyState renders centered icon + title + subtitle + optional CTA button"
  artifacts:
    - path: "apps/mobile/src/components/ui/Button.tsx"
      provides: "Button primitive with 4 variants, 3 sizes, useAnimatedPress"
      exports: ["Button", "ButtonProps"]
    - path: "apps/mobile/src/components/ui/Card.tsx"
      provides: "Card surface container with optional onPress animation"
      exports: ["Card", "CardProps"]
    - path: "apps/mobile/src/components/ui/Text.tsx"
      provides: "Typography preset wrapper"
      exports: ["Text", "TextProps"]
    - path: "apps/mobile/src/components/ui/Badge.tsx"
      provides: "Status pill with variant colors"
      exports: ["Badge", "BadgeProps"]
    - path: "apps/mobile/src/components/ui/Input.tsx"
      provides: "Styled TextInput with focus ring"
      exports: ["Input", "InputProps"]
    - path: "apps/mobile/src/components/ui/Divider.tsx"
      provides: "Horizontal rule"
      exports: ["Divider"]
    - path: "apps/mobile/src/components/ui/EmptyState.tsx"
      provides: "Empty state with icon, title, subtitle, CTA"
      exports: ["EmptyState", "EmptyStateProps"]
    - path: "apps/mobile/src/components/ui/index.ts"
      provides: "Barrel export for all primitives"
  key_links:
    - from: "apps/mobile/src/components/ui/Button.tsx"
      to: "apps/mobile/src/hooks/useAnimatedPress.ts"
      via: "import useAnimatedPress"
      pattern: "useAnimatedPress.*haptic.*true"
    - from: "apps/mobile/src/components/ui/Card.tsx"
      to: "apps/mobile/src/hooks/useAnimatedPress.ts"
      via: "import useAnimatedPress when onPress provided"
      pattern: "useAnimatedPress.*haptic"
    - from: "apps/mobile/src/components/ui/EmptyState.tsx"
      to: "apps/mobile/src/components/ui/Button.tsx"
      via: "CTA uses Button primitive"
      pattern: "import.*Button.*from.*ui"
---

<objective>
Build the shared UI primitive component library (Button, Card, Text, Badge, Input, Divider) and EmptyState component in `apps/mobile/src/components/ui/`.

Purpose: These primitives are the foundation for all three screen migrations. Every subsequent plan depends on these components existing with correct token usage, animated press integration, and TypeScript interfaces.

Output: 8 files in `apps/mobile/src/components/ui/` — 6 primitives + EmptyState + barrel index.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-screen-migration-tier-1/16-CONTEXT.md
@.planning/phases/16-screen-migration-tier-1/16-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From apps/mobile/src/hooks/useAnimatedPress.ts:
```typescript
export function useAnimatedPress(options?: { haptic?: boolean }): {
  animatedStyle: { transform: { scale: number }[] };
  pressHandlers: { onPressIn: () => void; onPressOut: () => void };
};
```

From apps/mobile/src/constants/theme.ts:
```typescript
export const colors = {
  background, surface, surfaceLight, primary, primaryDark,
  text, textSecondary, textMuted, error, success, border,
  tabBar, inputBackground,
};
export const typography = { heading, subheading, body, caption, label };
export const spacing = { xs: 4, sm: 8, md: 16, lg: 24, xl: 32, xxl: 48 };
export const borderRadius = { sm, md, lg, xl, full };
export const fontFamily = { regular, medium, semiBold, bold };
export const fontWeight = { regular: '400', medium: '500', semiBold: '600', bold: '700' };
```

From packages/shared/src/tokens/elevation.ts:
```typescript
export const elevation = {
  low: { shadowColor: '#000000', shadowOffset: { width: 0, height: 1 }, shadowOpacity: 0.2, shadowRadius: 2, elevation: 2 },
  medium: { shadowColor: '#000000', shadowOffset: { width: 0, height: 2 }, shadowOpacity: 0.25, shadowRadius: 4, elevation: 4 },
};
```

From packages/shared/src/tokens/colors.ts:
```typescript
export const colors = {
  background: palette.gray[950],  // #0c0c18
  surface: palette.gray[800],     // #1a1a2e
  surfaceLight: palette.gray[700], // #252540
  onSurface: palette.white,       // #ffffff
  onSurfaceSecondary: palette.gray[300], // #a0a0b8
  onSurfaceMuted: palette.gray[400],     // #6c6c80
  accent: palette.gold[500],      // #f0c040
  error: palette.red[500],        // #e74c3c
  success: palette.green[500],    // #2ecc71
  warning: palette.orange[500],   // #e67e22
  border: palette.gray[600],      // #2a2a45
  rarityDiamond: palette.diamond[500], // #7ec8e3
  rarityStar: palette.gold[500],  // #f0c040
  rarityCrown: palette.crown[500], // #e8b4f8
};
```

From apps/mobile/src/constants/springs.ts:
```typescript
export const SPRING_PRESS = { damping: 15, stiffness: 300, mass: 0.8 };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Text, Badge, Divider, and Input primitives</name>
  <files>
    apps/mobile/src/components/ui/Text.tsx
    apps/mobile/src/components/ui/Badge.tsx
    apps/mobile/src/components/ui/Divider.tsx
    apps/mobile/src/components/ui/Input.tsx
  </files>
  <read_first>
    apps/mobile/src/constants/theme.ts
    packages/shared/src/tokens/colors.ts
    packages/shared/src/tokens/elevation.ts
    .planning/phases/16-screen-migration-tier-1/16-UI-SPEC.md (lines 155-200 for component contracts)
  </read_first>
  <action>
Create `apps/mobile/src/components/ui/` directory and build 4 primitives:

**Text.tsx** (per D-04, UI-SPEC Typography section):
- Interface: `TextProps = { preset?: 'heading' | 'subheading' | 'body' | 'label'; color?: string; style?: StyleProp<TextStyle>; children: React.ReactNode; numberOfLines?: number }`
- Wraps RN `Text` component (import as `RNText` from 'react-native')
- Preset styles map:
  - `heading`: fontSize 28, fontWeight '700', lineHeight 34, color `colors.text` (#ffffff), fontFamily `fontFamily.bold`
  - `subheading`: fontSize 20, fontWeight '700', lineHeight 26, color `colors.text`, fontFamily `fontFamily.bold`
  - `body`: fontSize 16, fontWeight '400', lineHeight 22, color `colors.text`, fontFamily `fontFamily.regular`
  - `label`: fontSize 13, fontWeight '400', lineHeight 18, color `colors.textSecondary` (#a0a0b8), fontFamily `fontFamily.regular`
- Default preset: `body`
- `color` prop overrides preset default color
- Export `Text` and `TextProps`

**Badge.tsx** (per D-05, UI-SPEC Badge section):
- Interface: `BadgeProps = { variant?: 'default' | 'success' | 'warning' | 'error' | 'rarity-diamond' | 'rarity-star' | 'rarity-crown'; label: string; style?: StyleProp<ViewStyle> }`
- Pill shape: `borderRadius: borderRadius.full` (9999)
- Padding: `paddingVertical: spacing.xs` (4), `paddingHorizontal: spacing.sm` (8)
- Font: Label preset (fontSize 13, fontWeight '400', fontFamily `fontFamily.regular`)
- Variant color map (background + text):
  - `default`: bg `colors.surfaceLight` (#252540), text `colors.text` (#ffffff)
  - `success`: bg `rgba(46, 204, 113, 0.2)`, text `#2ecc71`
  - `warning`: bg `rgba(230, 126, 34, 0.2)`, text `#e67e22`
  - `error`: bg `rgba(231, 76, 60, 0.2)`, text `#e74c3c`
  - `rarity-diamond`: bg `rgba(126, 200, 227, 0.15)`, text `#7ec8e3`
  - `rarity-star`: bg `rgba(240, 192, 64, 0.15)`, text `#f0c040`
  - `rarity-crown`: bg `rgba(232, 180, 248, 0.15)`, text `#e8b4f8`
- Export `Badge` and `BadgeProps`

**Divider.tsx** (per D-07, UI-SPEC Divider section):
- Interface: `DividerProps = { spacing?: number; style?: StyleProp<ViewStyle> }`
- Renders a `View` with: `height: StyleSheet.hairlineWidth`, `backgroundColor: colors.border` (#2a2a45), `marginVertical: spacing ?? 16` (default `spacing.md`)
- Export `Divider`

**Input.tsx** (per D-06, UI-SPEC Input section):
- Interface: `InputProps = { label?: string; error?: string; value: string; onChangeText: (text: string) => void; placeholder?: string; secureTextEntry?: boolean; style?: StyleProp<ViewStyle> } & Omit<TextInputProps, 'style'>`
- Uses `useState` for `isFocused` to toggle border color
- Container View wraps optional label `Text` (label preset, color `colors.textSecondary`) + `TextInput`
- TextInput styles: backgroundColor `colors.surface` (#1a1a2e), borderWidth 1, borderColor switches between:
  - Error state: `colors.error` (#e74c3c) width 1
  - Focused: `colors.primary` (#f0c040) width 2
  - Default: `colors.border` (#2a2a45) width 1
- borderRadius: `borderRadius.md` (12), paddingVertical: `spacing.sm` (8), paddingHorizontal: `spacing.md` (16)
- Font: body preset (fontSize 16, fontWeight '400', color `colors.text`, fontFamily `fontFamily.regular`)
- Placeholder color: `colors.textMuted` (#6c6c80)
- Error text below input: label preset in `colors.error`
- Export `Input` and `InputProps`
  </action>
  <verify>
    <automated>ls apps/mobile/src/components/ui/Text.tsx apps/mobile/src/components/ui/Badge.tsx apps/mobile/src/components/ui/Divider.tsx apps/mobile/src/components/ui/Input.tsx && grep -l "export function Text" apps/mobile/src/components/ui/Text.tsx && grep -l "export function Badge" apps/mobile/src/components/ui/Badge.tsx && grep -l "export function Divider" apps/mobile/src/components/ui/Divider.tsx && grep -l "export function Input" apps/mobile/src/components/ui/Input.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Text.tsx contains `export function Text(` and `preset?: 'heading' | 'subheading' | 'body' | 'label'`
    - Text.tsx contains `fontSize: 28` (heading), `fontSize: 20` (subheading), `fontSize: 16` (body), `fontSize: 13` (label)
    - Text.tsx imports from `@/src/constants/theme`
    - Badge.tsx contains `export function Badge(` and all 7 variant strings: `default`, `success`, `warning`, `error`, `rarity-diamond`, `rarity-star`, `rarity-crown`
    - Badge.tsx contains `borderRadius` with full/9999 value
    - Divider.tsx contains `StyleSheet.hairlineWidth` and `colors.border` or `#2a2a45`
    - Input.tsx contains `onFocus` and `onBlur` handlers for focus ring toggle
    - Input.tsx contains `colors.primary` or `#f0c040` for focus border color
    - Input.tsx contains `borderWidth: 2` for focused state
  </acceptance_criteria>
  <done>Text, Badge, Divider, and Input primitives exist with correct token values, TypeScript interfaces, and all variant/preset combinations per UI-SPEC</done>
</task>

<task type="auto">
  <name>Task 2: Create Button, Card, EmptyState primitives and barrel export</name>
  <files>
    apps/mobile/src/components/ui/Button.tsx
    apps/mobile/src/components/ui/Card.tsx
    apps/mobile/src/components/ui/EmptyState.tsx
    apps/mobile/src/components/ui/index.ts
  </files>
  <read_first>
    apps/mobile/src/hooks/useAnimatedPress.ts
    apps/mobile/src/constants/theme.ts
    apps/mobile/src/constants/springs.ts
    packages/shared/src/tokens/elevation.ts
    .planning/phases/16-screen-migration-tier-1/16-UI-SPEC.md (lines 129-155 for Button/Card, lines 235-257 for EmptyState)
    apps/mobile/src/components/ui/Text.tsx (created in Task 1 — verify preset API)
  </read_first>
  <action>
Build the 3 remaining components that depend on useAnimatedPress + other primitives:

**Button.tsx** (per D-02, UI-SPEC Button section):
- Interface: `ButtonProps = { onPress: () => void; label: string; variant?: 'primary' | 'secondary' | 'ghost' | 'destructive'; size?: 'sm' | 'md' | 'lg'; disabled?: boolean; loading?: boolean; Icon?: React.ComponentType<{ size: number; color: string; weight: string }> }`
- Uses `useAnimatedPress({ haptic: true })` from `@/src/hooks/useAnimatedPress`
- Renders `Animated.View` (from react-native-reanimated) with `animatedStyle` + opacity 0.5 when disabled
- Inside: `Pressable` with `pressHandlers` spread (disabled when `disabled || loading`)
- Inside Pressable: `ActivityIndicator` when loading, else optional `Icon` + `Text` (RN Text, not custom Text primitive)
- Size map (height): sm=32, md=44, lg=52
- Border radius: sm gets `borderRadius.sm` (6), md/lg get `borderRadius.md` (12)
- Variant styles:
  - `primary`: backgroundColor `#f0c040`, text color `#0c0c18` (dark on gold)
  - `secondary`: backgroundColor transparent, borderWidth 1, borderColor `#f0c040`, text color `#f0c040`
  - `ghost`: backgroundColor transparent, text color `colors.text` (#ffffff)
  - `destructive`: backgroundColor `#e74c3c`, text color `#ffffff`
- Text style: body preset (fontSize 16, fontWeight '400', fontFamily `fontFamily.regular`), centered
- Layout: flexDirection 'row', alignItems 'center', justifyContent 'center', gap `spacing.xs` (4) between icon and label
- Export `Button` and `ButtonProps`

**Card.tsx** (per D-03, UI-SPEC Card section):
- Interface: `CardProps = { children: React.ReactNode; onPress?: () => void; style?: StyleProp<ViewStyle>; padding?: number }`
- When `onPress` is provided: uses `useAnimatedPress({ haptic: true })`, renders `Animated.View` with animatedStyle wrapping `Pressable` with pressHandlers
- When no `onPress`: renders plain `View`
- Base styles: backgroundColor `colors.surface` (#1a1a2e), borderRadius `borderRadius.lg` (16), `...elevation.low` (from `@pocket-trade-hub/shared`), padding `spacing.md` (16) by default (overridable via `padding` prop), overflow 'hidden'
- Export `Card` and `CardProps`

**EmptyState.tsx** (per D-11 through D-14, UI-SPEC Empty State Contract):
- Interface: `EmptyStateProps = { icon: React.ComponentType<{ size: number; color: string; weight: string }>; title: string; subtitle?: string; ctaLabel?: string; onCta?: () => void }`
- Layout: centered flex container (flex 1, justifyContent 'center', alignItems 'center', padding `spacing.lg` (24))
- Icon: rendered at size 64, color `colors.textMuted` (#6c6c80), weight "light"
- Title: uses custom `Text` primitive from this package with preset "subheading" — marginTop `spacing.md` (16)
- Subtitle: uses custom `Text` primitive with preset "body", color `colors.textSecondary` (#a0a0b8) — marginTop `spacing.md` (16), textAlign 'center'
- CTA: when `ctaLabel` and `onCta` provided, renders `Button` primitive with variant "primary", size "md" — marginTop `spacing.md` (16)
- Import `Text` from `./Text` and `Button` from `./Button`
- Export `EmptyState` and `EmptyStateProps`

**index.ts** — Barrel export:
```typescript
export { Button } from './Button';
export type { ButtonProps } from './Button';
export { Card } from './Card';
export type { CardProps } from './Card';
export { Text } from './Text';
export type { TextProps } from './Text';
export { Badge } from './Badge';
export type { BadgeProps } from './Badge';
export { Input } from './Input';
export type { InputProps } from './Input';
export { Divider } from './Divider';
export { EmptyState } from './EmptyState';
export type { EmptyStateProps } from './EmptyState';
```
  </action>
  <verify>
    <automated>ls apps/mobile/src/components/ui/Button.tsx apps/mobile/src/components/ui/Card.tsx apps/mobile/src/components/ui/EmptyState.tsx apps/mobile/src/components/ui/index.ts && grep -l "useAnimatedPress" apps/mobile/src/components/ui/Button.tsx && grep -l "useAnimatedPress" apps/mobile/src/components/ui/Card.tsx && grep -l "export.*Button" apps/mobile/src/components/ui/index.ts && grep -c "export" apps/mobile/src/components/ui/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - Button.tsx contains `export function Button(` and `useAnimatedPress({ haptic: true })`
    - Button.tsx contains all 4 variant strings: `primary`, `secondary`, `ghost`, `destructive`
    - Button.tsx contains size heights: `32` (sm), `44` (md), `52` (lg)
    - Button.tsx contains `#0c0c18` (primary text on gold) and `#f0c040` (accent)
    - Button.tsx imports `Animated` from `react-native-reanimated` and uses `Animated.View`
    - Card.tsx contains `export function Card(` and conditionally calls `useAnimatedPress`
    - Card.tsx contains `elevation.low` or the equivalent shadow spread from elevation token
    - Card.tsx contains `borderRadius` value of 16 (borderRadius.lg)
    - EmptyState.tsx contains `export function EmptyState(` and imports `Button` from `./Button` and `Text` from `./Text`
    - EmptyState.tsx contains `size={64}` for icon and `weight="light"`
    - index.ts contains exports for all 7 components: Button, Card, Text, Badge, Input, Divider, EmptyState
  </acceptance_criteria>
  <done>Button with animated press and 4 variants, Card with conditional animated press, EmptyState with Phosphor icon + CTA button, and barrel export file all exist and compile</done>
</task>

</tasks>

<verification>
- All 8 files exist in `apps/mobile/src/components/ui/`
- `index.ts` barrel exports all 7 components
- Button uses `useAnimatedPress` with haptic enabled
- Card conditionally uses `useAnimatedPress` only when `onPress` is provided
- All color values come from theme.ts tokens (not hardcoded except `#0c0c18` for primary button text and `#3498db` for info)
- EmptyState composes Button and Text primitives internally
- No new npm packages introduced
</verification>

<success_criteria>
- 7 primitive components + 1 barrel export created in `apps/mobile/src/components/ui/`
- Button: 4 variants, 3 sizes, animated press with haptics
- Card: surface container, optional animated press on tap
- Text: 4 typography presets mapping to correct font sizes/weights
- Badge: 7 color variants with correct opacity backgrounds
- Input: focus ring in gold, error state in red
- Divider: hairline rule in border color
- EmptyState: icon + title + subtitle + optional CTA using Button/Text primitives
</success_criteria>

<output>
After completion, create `.planning/phases/16-screen-migration-tier-1/16-01-SUMMARY.md`
</output>
