---
phase: 06-premium-tier
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified:
  - apps/mobile/src/services/purchases.ts
  - apps/mobile/src/stores/premium.ts
  - apps/mobile/src/hooks/usePremium.ts
  - apps/mobile/src/components/premium/PremiumBadge.tsx
  - apps/mobile/src/components/premium/PaywallCard.tsx
  - apps/mobile/src/components/premium/LockedFeatureCard.tsx
  - apps/mobile/src/components/premium/AnalyticsDashboard.tsx
  - apps/mobile/app/(tabs)/profile.tsx
  - apps/mobile/app/(tabs)/index.tsx
  - apps/mobile/app/analytics.tsx
  - apps/mobile/src/components/trades/MatchCard.tsx
autonomous: false
requirements:
  - PREM-01
  - PREM-02
  - PREM-03
  - PREM-04
user_setup:
  - service: revenuecat
    why: "In-app purchase billing and subscription management"
    env_vars:
      - name: EXPO_PUBLIC_RC_IOS_KEY
        source: "RevenueCat Dashboard -> Project -> iOS App -> Public API Key"
      - name: EXPO_PUBLIC_RC_ANDROID_KEY
        source: "RevenueCat Dashboard -> Project -> Android App -> Public API Key"
      - name: REVENUECAT_WEBHOOK_SECRET
        source: "RevenueCat Dashboard -> Project -> Integrations -> Webhooks -> Authorization header value"
    dashboard_config:
      - task: "Create RevenueCat project and apps (iOS + Android)"
        location: "RevenueCat Dashboard -> Projects -> New Project"
      - task: "Create 'premium' entitlement"
        location: "RevenueCat Dashboard -> Project -> Entitlements -> New"
      - task: "Create product ($4.99/month subscription)"
        location: "RevenueCat Dashboard -> Project -> Products -> New"
      - task: "Configure webhook URL pointing to API /webhooks/revenuecat"
        location: "RevenueCat Dashboard -> Project -> Integrations -> Webhooks"

must_haves:
  truths:
    - "User can see premium section in Profile tab with Go Premium card or subscription status"
    - "Tapping Go Premium shows paywall with feature list and price"
    - "Premium user can access full-screen analytics dashboard from Home tab or Profile"
    - "Free user sees locked analytics card on Home tab with soft upsell"
    - "Gold crown badge appears next to premium users' names on match cards"
    - "Premium user sees subscription status and manage link in Profile"
  artifacts:
    - path: "apps/mobile/src/services/purchases.ts"
      provides: "RevenueCat init, purchase flow, premium status check"
      exports: ["initPurchases", "purchasePremium", "checkPremiumStatus", "restorePurchases"]
    - path: "apps/mobile/src/stores/premium.ts"
      provides: "Zustand store for premium state"
      exports: ["usePremiumStore"]
    - path: "apps/mobile/src/hooks/usePremium.ts"
      provides: "Hook for premium status and purchase actions"
      exports: ["usePremium"]
    - path: "apps/mobile/src/components/premium/PremiumBadge.tsx"
      provides: "Gold crown icon component"
      exports: ["PremiumBadge"]
    - path: "apps/mobile/src/components/premium/PaywallCard.tsx"
      provides: "Go Premium card with feature list and subscribe button"
      exports: ["PaywallCard"]
    - path: "apps/mobile/src/components/premium/AnalyticsDashboard.tsx"
      provides: "Full-screen analytics with 4 sections"
      exports: ["default"]
    - path: "apps/mobile/app/analytics.tsx"
      provides: "Analytics route screen"
      exports: ["default"]
  key_links:
    - from: "apps/mobile/src/stores/premium.ts"
      to: "/premium/status"
      via: "fetchStatus calls GET /premium/status"
      pattern: "apiFetch.*premium/status"
    - from: "apps/mobile/src/components/premium/AnalyticsDashboard.tsx"
      to: "/premium/analytics"
      via: "useEffect fetches GET /premium/analytics"
      pattern: "apiFetch.*premium/analytics"
    - from: "apps/mobile/src/services/purchases.ts"
      to: "react-native-purchases"
      via: "RevenueCat SDK for IAP"
      pattern: "Purchases.configure"
    - from: "apps/mobile/app/(tabs)/profile.tsx"
      to: "apps/mobile/src/components/premium/PaywallCard.tsx"
      via: "Profile renders PaywallCard or subscription status"
      pattern: "PaywallCard"
---

<objective>
Mobile premium experience: RevenueCat SDK integration, premium store, paywall, PremiumBadge, analytics dashboard, soft upsells on Home/Profile tabs, and gold crown on match cards.

Purpose: Users discover premium features organically via the Profile tab and Home tab. Premium users access analytics dashboard and see their gold crown badge. The experience should feel like Discord Nitro -- clean, not salesy.

Output: Complete mobile premium UI with IAP subscription flow, analytics viewing, and premium badges.
</objective>

<execution_context>
@C:/Users/romec/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/romec/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-premium-tier/06-CONTEXT.md
@.planning/phases/06-premium-tier/06-RESEARCH.md
@.planning/phases/06-premium-tier/06-01-SUMMARY.md
@.planning/phases/06-premium-tier/06-02-SUMMARY.md

<interfaces>
<!-- Backend APIs from Plan 01 -->

API Endpoints:
```
GET  /premium/status        -> { isPremium: boolean, premiumExpiresAt: string | null }
GET  /premium/analytics     -> { mostWanted: AnalyticsCard[], leastAvailable: AnalyticsCard[], trending: AnalyticsCard[], tradePower: AnalyticsCard[] }
POST /premium/sync          -> { isPremium: boolean, premiumExpiresAt: string | null }
```

AnalyticsCard shape:
```typescript
{ cardId: string, cardName: string, cardImageUrl: string, rarity: string | null, value: number, rank: number }
```

Match response (Plan 01 addition):
```typescript
// Added to match objects in getMatchesForUser response:
{ ...existingFields, partnerIsPremium: boolean }
```

<!-- Existing mobile patterns -->

From apps/mobile/src/hooks/useApi.ts:
```typescript
export async function apiFetch<T>(path: string, options?): Promise<T>;
```

From apps/mobile/src/stores/trades.ts:
```typescript
// Zustand store pattern with create(), reset() on logout
```

From apps/mobile/src/constants/theme.ts:
```typescript
export const colors = { primary: '#f0c040', background: '#1a1a2e', surface: '#16213e', ... };
```

From apps/mobile/app/(tabs)/profile.tsx:
```typescript
// ScrollView with avatar, reputation stars, friend code, info card, edit/logout buttons
```

From apps/mobile/app/(tabs)/index.tsx:
```typescript
// ScrollView with welcome, SetupChecklist, CollectionSummary, Coming Soon previews
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RevenueCat service, premium store, hook, and reusable components</name>
  <files>
    apps/mobile/src/services/purchases.ts
    apps/mobile/src/stores/premium.ts
    apps/mobile/src/hooks/usePremium.ts
    apps/mobile/src/components/premium/PremiumBadge.tsx
    apps/mobile/src/components/premium/PaywallCard.tsx
    apps/mobile/src/components/premium/LockedFeatureCard.tsx
  </files>
  <action>
    1. Install RevenueCat: Run `cd apps/mobile && npx expo install react-native-purchases`

    2. Create `apps/mobile/src/services/purchases.ts`:
       - `initPurchases(appUserId?: string)`: Configure RevenueCat with platform-specific API keys (EXPO_PUBLIC_RC_IOS_KEY, EXPO_PUBLIC_RC_ANDROID_KEY). Set LOG_LEVEL.VERBOSE in __DEV__. Guard: if no apiKey (web platform), return early.
       - `checkPremiumStatus()`: Get CustomerInfo, return `entitlements.active['premium'] !== undefined`. Catch errors, return false.
       - `purchasePremium()`: Get offerings, purchase the 'premium' package from current offering. Return { success: boolean, customerInfo }. On error, check if user cancelled (PurchasesErrorCode.PURCHASE_CANCELLED_ERROR) vs real error.
       - `restorePurchases()`: Call Purchases.restorePurchases(), return premium status.
       - All functions wrapped in try/catch with graceful fallbacks.

    3. Create `apps/mobile/src/stores/premium.ts` (Zustand):
       - State: `isPremium: boolean`, `premiumExpiresAt: string | null`, `analyticsData: AnalyticsResponse | null`, `loading: boolean`
       - Actions: `fetchStatus()` (calls GET /premium/status via apiFetch, updates state), `fetchAnalytics()` (calls GET /premium/analytics), `setIsPremium(val)`, `reset()` (for logout)
       - Follow existing Zustand pattern (create, reset on logout)

    4. Create `apps/mobile/src/hooks/usePremium.ts`:
       - Returns `{ isPremium, loading, purchase, restore, fetchStatus }` from store + purchase service
       - `purchase()`: Calls purchasePremium(), on success calls POST /premium/sync then fetchStatus()
       - `restore()`: Calls restorePurchases(), syncs with backend

    5. Create `apps/mobile/src/components/premium/PremiumBadge.tsx`:
       - Small gold crown icon (Ionicons "trophy" or custom crown character) in #f0c040
       - Props: `size?: number` (default 16)
       - Renders inline next to display names
       - Simple: `<Ionicons name="diamond" size={size} color="#f0c040" />` -- or use a crown emoji/icon that fits the Pokemon card theme

    6. Create `apps/mobile/src/components/premium/PaywallCard.tsx`:
       - "Go Premium" card for Profile tab (pre-subscription state)
       - Gold-accented surface card with crown icon header
       - Feature list with checkmarks: "Card demand analytics", "Priority match placement", "Advanced card alerts", "Premium badge"
       - Price: "$4.99/month"
       - "Subscribe" button (calls usePremium().purchase)
       - "Restore Purchases" link below button
       - Post-subscription state: Shows "Premium Active" with expiry date, "Manage Subscription" link (opens device settings), quick links to Analytics and Alert settings
       - Style: Clean, Discord Nitro inspired. Gold (#f0c040) accents on dark surface. Not salesy.

    7. Create `apps/mobile/src/components/premium/LockedFeatureCard.tsx`:
       - Props: `title: string`, `description: string`, `icon: string`, `onPress: () => void`
       - Surface card with lock icon overlay, muted content, "Premium" label in gold
       - Tapping fires onPress (opens paywall or navigates to premium section)
       - Used on Home tab for locked analytics card
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - RevenueCat service handles init, purchase, restore, and status check
    - Premium Zustand store manages subscription state and analytics data
    - usePremium hook provides clean interface for components
    - PremiumBadge renders gold crown icon at configurable size
    - PaywallCard shows feature list + subscribe button (pre) or status (post)
    - LockedFeatureCard shows soft upsell with lock icon
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Analytics dashboard, Home/Profile integration, and match card badge</name>
  <files>
    apps/mobile/src/components/premium/AnalyticsDashboard.tsx
    apps/mobile/app/analytics.tsx
    apps/mobile/app/(tabs)/profile.tsx
    apps/mobile/app/(tabs)/index.tsx
    apps/mobile/app/(tabs)/_layout.tsx
    apps/mobile/src/components/trades/MatchCard.tsx
  </files>
  <action>
    1. Create `apps/mobile/src/components/premium/AnalyticsDashboard.tsx`:
       - Full-screen ScrollView with 4 collapsible sections:
         a. **Most Wanted Cards** -- Top 10 cards most users have on wanted lists. Shows CardThumbnail, card name, rarity symbol, demand count ("247 traders want this")
         b. **Least Available Cards** -- Top 10 cards fewest users have. Shows CardThumbnail, name, rarity, scarcity count ("Only 12 trainers have this")
         c. **Trending Cards** -- Top 10 cards with biggest wanted-list increase (7 days). Shows CardThumbnail, name, rarity, trend count ("+43 this week")
         d. **Your Trade Power** -- Cards in user's collection that others want most. Shows CardThumbnail, name, rarity, demand ("89 traders want your card")
       - Each section header with Ionicons icon and gold accent
       - Card list items: horizontal row with 48x48 CardThumbnail image, card name, rarity symbol (unicode diamond/star/crown per existing pattern), stat number
       - Loading state with ActivityIndicator
       - Empty state per section if no data
       - Fetches data via usePremiumStore().fetchAnalytics() on mount
       - Style: Dark theme, scannable card lists (per user decision -- no charts)

    2. Create `apps/mobile/app/analytics.tsx`:
       - Route screen that renders AnalyticsDashboard
       - Stack screen with title "Card Analytics"
       - Premium gate: if not premium, redirect to profile tab (shouldn't happen normally, but safety check)

    3. Modify `apps/mobile/app/(tabs)/profile.tsx`:
       - Import PaywallCard, usePremiumStore
       - After the friend code section and before info card, add premium section:
         - If NOT premium: Render `<PaywallCard />` (Go Premium card)
         - If premium: Render premium status card with:
           - "Premium Active" header with gold crown
           - Expiry date
           - "View Analytics" button (navigates to /analytics)
           - "Manage Subscription" link
       - Fetch premium status on mount via usePremiumStore().fetchStatus()
       - Add PremiumBadge next to displayName if premium

    4. Modify `apps/mobile/app/(tabs)/index.tsx`:
       - Import LockedFeatureCard, usePremiumStore
       - After CollectionSummary section, before Coming Soon:
         - If premium: Render analytics summary card (surface card with "Card Analytics" title, brief stats teaser, "View Dashboard" button navigating to /analytics)
         - If NOT premium: Render `<LockedFeatureCard title="Card Analytics" description="See which cards are most wanted, least available, and trending" icon="analytics" onPress={() => router.push('/profile')} />`
       - Fetch premium status on mount

    5. Modify `apps/mobile/app/(tabs)/_layout.tsx`:
       - Import usePremiumStore
       - In the isLoggedIn useEffect, also call `usePremiumStore.getState().fetchStatus()` to load premium state on app start
       - Add `usePremiumStore.getState().reset()` in the logout cleanup useEffect

    6. Find and modify the MatchCard component (likely `apps/mobile/src/components/trades/MatchCard.tsx` or within trades screen):
       - Import PremiumBadge
       - Check for `partnerIsPremium` field in match data
       - If partnerIsPremium is true, render `<PremiumBadge size={14} />` next to the partner's display name
       - Gold crown serves as passive advertising -- signals trust per user decision
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - Analytics dashboard shows 4 sections with card lists (no charts)
    - /analytics route accessible from Home and Profile
    - Profile tab shows PaywallCard (free) or premium status (subscribed)
    - Home tab shows locked analytics card (free) or summary card (premium)
    - Gold crown badge appears on premium partners' match cards
    - Premium state loaded on app start and cleared on logout
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete premium experience</name>
  <action>
    Human verifies the complete premium tier: subscription paywall, analytics dashboard, premium badge, soft upsells, and match priority boost.

    Steps:
    1. Open app on device/emulator, navigate to Profile tab
    2. Verify "Go Premium" card appears with feature list and $4.99/month price
    3. Navigate to Home tab -- verify locked analytics card appears with lock icon
    4. (If RevenueCat configured) Tap Subscribe, complete sandbox purchase
    5. After subscribing: Profile shows "Premium Active" with manage link
    6. Home tab analytics card is now unlocked -- tap "View Dashboard"
    7. Analytics dashboard shows 4 sections (Most Wanted, Least Available, Trending, Trade Power) with card lists
    8. Navigate to Trades tab -- verify gold crown badge on premium partners' match cards
    9. Verify no aggressive pop-ups or interruptions throughout the flow
  </action>
  <verify>User confirms premium flow works end-to-end</verify>
  <done>User types "approved" or describes issues to fix</done>
</task>

</tasks>

<verification>
- `cd apps/mobile && npx tsc --noEmit` -- no type errors
- Profile tab renders premium section (paywall or status)
- Home tab renders analytics card (locked or unlocked)
- Analytics dashboard loads and displays pre-computed data
- Gold crown badge visible on premium match cards
- Premium state persists across tab switches
</verification>

<success_criteria>
- User can see and interact with premium paywall on Profile tab
- Premium user can access analytics dashboard with 4 data sections
- Free user sees soft upsells (lock icon + description) without interruption
- Gold crown badge renders on premium users' match cards
- RevenueCat SDK initializes correctly (or gracefully no-ops on web/Expo Go)
- Premium state loads on app start and resets on logout
</success_criteria>

<output>
After completion, create `.planning/phases/06-premium-tier/06-03-SUMMARY.md`
</output>
