---
phase: 06-premium-tier
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/jobs/analytics-worker.ts
  - apps/api/src/jobs/card-alert-worker.ts
  - apps/api/src/services/collection.service.ts
  - apps/api/src/routes/collection.ts
  - apps/api/src/server.ts
autonomous: true
requirements:
  - PREM-02
  - PREM-04

must_haves:
  truths:
    - "Analytics are pre-computed daily at 4am via BullMQ cron job"
    - "Card alerts are batched and sent every 2 hours via BullMQ cron job"
    - "Adding a card to collection triggers card alert check for premium users"
    - "Bulk collection updates also trigger card alert checks"
  artifacts:
    - path: "apps/api/src/jobs/analytics-worker.ts"
      provides: "BullMQ repeatable job that runs computeAllAnalytics daily"
      exports: ["initAnalyticsWorker", "closeAnalyticsWorker"]
    - path: "apps/api/src/jobs/card-alert-worker.ts"
      provides: "BullMQ repeatable job that runs processCardAlertBatch every 2 hours"
      exports: ["initCardAlertWorker", "closeCardAlertWorker"]
    - path: "apps/api/src/services/collection.service.ts"
      provides: "Card alert trigger after addToCollection and bulkUpdateCollection"
      contains: "checkCardAlerts"
    - path: "apps/api/src/routes/collection.ts"
      provides: "Card alert trigger wiring in collection routes"
      contains: "checkCardAlerts"
  key_links:
    - from: "apps/api/src/jobs/analytics-worker.ts"
      to: "apps/api/src/services/analytics.service.ts"
      via: "worker calls computeAllAnalytics"
      pattern: "computeAllAnalytics"
    - from: "apps/api/src/jobs/card-alert-worker.ts"
      to: "apps/api/src/services/card-alert.service.ts"
      via: "worker calls processCardAlertBatch"
      pattern: "processCardAlertBatch"
    - from: "apps/api/src/routes/collection.ts"
      to: "apps/api/src/services/card-alert.service.ts"
      via: "POST /collection triggers checkCardAlerts after success"
      pattern: "checkCardAlerts"
---

<objective>
Background workers and collection-trigger wiring for analytics pre-computation and card alert batching.

Purpose: Analytics data must be pre-computed daily for fast reads (not computed on demand). Card alerts must batch into 2-hour digests to prevent notification spam. Collection mutations must trigger alert checks for premium users.

Output: Two BullMQ workers (analytics cron, alert cron) and collection service wiring that creates alert events.
</objective>

<execution_context>
@C:/Users/romec/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/romec/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-premium-tier/06-CONTEXT.md
@.planning/phases/06-premium-tier/06-RESEARCH.md
@.planning/phases/06-premium-tier/06-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs (services created there) -->

From apps/api/src/services/analytics.service.ts (created in Plan 01):
```typescript
export async function computeAllAnalytics(db: any): Promise<void>;
```

From apps/api/src/services/card-alert.service.ts (created in Plan 01):
```typescript
export async function checkCardAlerts(db: any, addedByUserId: string, cardId: string): Promise<void>;
export async function processCardAlertBatch(db: any): Promise<void>;
```

From apps/api/src/jobs/match-worker.ts (existing pattern to follow):
```typescript
function getConnection() {
  return new IORedis(process.env.REDIS_URL || 'redis://localhost:6379', { maxRetriesPerRequest: null });
}
export function initMatchWorker(db: any, io: Server): Worker;
export async function closeMatchWorker(): Promise<void>;
```

From apps/api/src/services/collection.service.ts (existing, to modify):
```typescript
export async function addToCollection(db, userId, cardId, quantity): Promise<any>;
export async function bulkUpdateCollection(db, userId, additions, removals): Promise<{ added, removed }>;
```

From apps/api/src/routes/collection.ts (existing, to modify):
```typescript
// POST /collection { cardId } -> calls addToCollection
// PUT /collection/bulk { additions, removals } -> calls bulkUpdateCollection
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analytics and card alert BullMQ workers</name>
  <files>
    apps/api/src/jobs/analytics-worker.ts
    apps/api/src/jobs/card-alert-worker.ts
  </files>
  <action>
    1. Create `apps/api/src/jobs/analytics-worker.ts` following the match-worker.ts pattern exactly:
       - Import Queue, Worker from 'bullmq', IORedis from 'ioredis'
       - Import computeAllAnalytics from '../services/analytics.service'
       - QUEUE_NAME = 'analytics-compute'
       - `getConnection()` helper (same pattern as match-worker)
       - `initAnalyticsWorker(db)`: Creates Queue + Worker. Uses `queue.upsertJobScheduler('compute-daily-analytics', { pattern: '0 4 * * *' }, { name: 'compute', data: {} })` for daily 4am cron. Worker processor calls `computeAllAnalytics(db)`. Add error handler on worker.
       - `closeAnalyticsWorker()`: Graceful shutdown of queue and worker
       - Export both functions

    2. Create `apps/api/src/jobs/card-alert-worker.ts` following same pattern:
       - QUEUE_NAME = 'card-alert-batch'
       - Import processCardAlertBatch from '../services/card-alert.service'
       - `initCardAlertWorker(db)`: Creates Queue + Worker. Uses `queue.upsertJobScheduler('process-card-alerts', { pattern: '0 */2 * * *' }, { name: 'process-batch', data: {} })` for every 2 hours. Worker processor calls `processCardAlertBatch(db)`.
       - `closeCardAlertWorker()`: Graceful shutdown
       - Export both functions
  </action>
  <verify>
    <automated>cd apps/api && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - analytics-worker.ts creates a daily cron at 4am calling computeAllAnalytics
    - card-alert-worker.ts creates a 2-hour cron calling processCardAlertBatch
    - Both follow established match-worker.ts patterns (getConnection, init/close exports)
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire alert triggers into collection mutations and start workers</name>
  <files>
    apps/api/src/routes/collection.ts
    apps/api/src/server.ts
  </files>
  <action>
    1. Modify `apps/api/src/routes/collection.ts`:
       - Import `checkCardAlerts` from '../services/card-alert.service'
       - In the POST /collection handler (single card add): After successful addToCollection call, fire `checkCardAlerts(db, userId, cardId)` in a fire-and-forget pattern (do NOT await -- wrap in `.catch(() => {})` so it never blocks the response). This matches the project's pattern of non-critical operations being fire-and-forget.
       - In the PUT /collection/bulk handler: After successful bulkUpdateCollection call, fire-and-forget for each card in `additions` array: `for (const cardId of additions) { checkCardAlerts(db, userId, cardId).catch(() => {}); }`
       - Do NOT modify collection.service.ts directly -- keep the trigger at the route level to maintain service purity (services don't have side effects outside their domain).

    2. Update `apps/api/src/server.ts`:
       - Import `initAnalyticsWorker` from './jobs/analytics-worker' and `initCardAlertWorker` from './jobs/card-alert-worker'
       - In the server start block (where initMatchWorker is called), add: `initAnalyticsWorker(app.db)` and `initCardAlertWorker(app.db)` after initMatchWorker
       - Note: These workers don't need Socket.IO (io parameter) since they operate via push notifications and DB
       - Add graceful shutdown calls for both workers alongside closeMatchWorker
  </action>
  <verify>
    <automated>cd apps/api && npx jest --no-coverage</automated>
  </verify>
  <done>
    - Collection route POST triggers checkCardAlerts fire-and-forget after adding card
    - Collection route PUT/bulk triggers checkCardAlerts for each added card
    - Analytics and card alert workers initialized on server start
    - Graceful shutdown closes all workers
    - Full test suite passes
  </done>
</task>

</tasks>

<verification>
- `cd apps/api && npx jest --no-coverage` -- full test suite green
- `cd apps/api && npx tsc --noEmit` -- no type errors
- Workers initialize without errors on server start (check logs)
</verification>

<success_criteria>
- Analytics pre-computation runs daily at 4am via BullMQ cron
- Card alert batching runs every 2 hours via BullMQ cron
- Collection mutations trigger card alert checks for premium users
- Both workers follow established match-worker.ts patterns
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/06-premium-tier/06-02-SUMMARY.md`
</output>
