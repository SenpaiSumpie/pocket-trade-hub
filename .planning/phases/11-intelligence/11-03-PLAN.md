---
phase: 11-intelligence
plan: 03
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - apps/mobile/src/stores/meta.ts
  - apps/mobile/src/stores/suggestions.ts
  - apps/mobile/src/stores/tierlists.ts
  - apps/mobile/app/(tabs)/meta.tsx
  - apps/mobile/app/(tabs)/_layout.tsx
  - apps/mobile/src/components/meta/DeckRankingList.tsx
  - apps/mobile/src/components/meta/DeckDetailModal.tsx
  - apps/mobile/src/components/meta/TierListBrowser.tsx
  - apps/mobile/src/components/meta/TierListCard.tsx
autonomous: true
requirements:
  - INTL-02
  - INTL-03
  - INTL-04

must_haves:
  truths:
    - "User can navigate to the Meta tab and see deck rankings"
    - "User can tap a deck to see detail modal with free/premium data split"
    - "User can switch between Rankings and Tier Lists segments"
    - "User can browse community tier lists sorted by most liked or newest"
    - "User can upvote a tier list"
  artifacts:
    - path: "apps/mobile/app/(tabs)/meta.tsx"
      provides: "Meta tab screen with segmented control"
      min_lines: 30
    - path: "apps/mobile/app/(tabs)/_layout.tsx"
      provides: "6th Meta tab in tab bar"
      contains: "meta"
    - path: "apps/mobile/src/stores/meta.ts"
      provides: "Zustand store for deck meta data"
      exports: ["useMetaStore"]
    - path: "apps/mobile/src/stores/tierlists.ts"
      provides: "Zustand store for tier lists"
      exports: ["useTierListStore"]
    - path: "apps/mobile/src/components/meta/DeckRankingList.tsx"
      provides: "Deck rankings list with sort toggles"
    - path: "apps/mobile/src/components/meta/DeckDetailModal.tsx"
      provides: "Deck detail modal with premium gating"
    - path: "apps/mobile/src/components/meta/TierListBrowser.tsx"
      provides: "Tier list browsing with sort and pagination"
  key_links:
    - from: "apps/mobile/app/(tabs)/meta.tsx"
      to: "apps/mobile/src/components/meta/DeckRankingList.tsx"
      via: "renders when Rankings segment active"
      pattern: "DeckRankingList"
    - from: "apps/mobile/app/(tabs)/meta.tsx"
      to: "apps/mobile/src/components/meta/TierListBrowser.tsx"
      via: "renders when Tier Lists segment active"
      pattern: "TierListBrowser"
    - from: "apps/mobile/src/stores/meta.ts"
      to: "/meta/decks"
      via: "apiFetch in fetchDecks action"
      pattern: "apiFetch.*meta/decks"
---

<objective>
Build the Meta tab with deck rankings and tier list browsing on mobile.

Purpose: Give users access to competitive deck analytics and community tier lists through a new tab with segmented navigation.
Output: Meta tab screen, 3 Zustand stores, deck ranking list, deck detail modal, tier list browser.
</objective>

<execution_context>
@C:/Users/romec/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/romec/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-intelligence/11-CONTEXT.md
@.planning/phases/11-intelligence/11-RESEARCH.md
@.planning/phases/11-intelligence/11-01-SUMMARY.md
@.planning/phases/11-intelligence/11-02-SUMMARY.md
@apps/mobile/app/(tabs)/_layout.tsx
@apps/mobile/src/stores/premium.ts
@apps/mobile/src/components/premium/PaywallCard.tsx
@apps/mobile/src/constants/theme.ts
@apps/mobile/src/hooks/useCards.ts

<interfaces>
<!-- API endpoints from Plan 02 -->
GET /meta/decks -> { decks: DeckMeta[], isPremium: boolean, scrapedAt: string }
GET /meta/decks/:id -> DeckMeta (with premium fields if premium)
GET /tierlists?sort=most_liked|newest&page=1&limit=20 -> { tierLists: TierList[], total: number }
POST /tierlists/:id/vote -> { upvoteCount: number, userVoted: boolean }
POST /tierlists -> 201 with TierList
DELETE /tierlists/:id -> 204

<!-- Shared types from Plan 01 -->
DeckMeta: { id, name, winRate, usageRate, playCount, matchRecord, topCards, cards?, matchups?, winRateTrend?, tournamentResults? }
TierList: { id, userId, title, description, tiers: { S, A, B, C, D }, isOfficial, upvoteCount, userVoted?, createdAt }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Zustand stores and Meta tab with deck rankings</name>
  <files>apps/mobile/src/stores/meta.ts, apps/mobile/src/stores/suggestions.ts, apps/mobile/src/stores/tierlists.ts, apps/mobile/app/(tabs)/meta.tsx, apps/mobile/app/(tabs)/_layout.tsx, apps/mobile/src/components/meta/DeckRankingList.tsx, apps/mobile/src/components/meta/DeckDetailModal.tsx</files>
  <action>
1. Create apps/mobile/src/stores/meta.ts (Zustand store, follow premium.ts pattern):
   - State: decks: DeckMeta[], isPremium: boolean, scrapedAt: string|null, loading: boolean, error: string|null, sortBy: 'winRate'|'usageRate'|'trending'
   - Actions: fetchDecks() -- calls GET /meta/decks via apiFetch, setSortBy(), reset()
   - Sort locally after fetch based on sortBy

2. Create apps/mobile/src/stores/suggestions.ts (Zustand store):
   - State: suggestions: TradeSuggestion[], isPremium: boolean, loading: boolean, error: string|null
   - Actions: fetchSuggestions(refresh?: boolean) -- calls GET /suggestions?refresh=true|false, reset()

3. Create apps/mobile/src/stores/tierlists.ts (Zustand store):
   - State: tierLists: TierList[], total: number, loading: boolean, error: string|null, sortBy: 'most_liked'|'newest', page: number
   - Actions: fetchTierLists(), vote(tierListId) -- optimistic update upvoteCount and userVoted, loadMore(), setSortBy(), reset()

4. Add Meta tab to apps/mobile/app/(tabs)/_layout.tsx:
   - Add 6th Tabs.Screen for "meta" between trades and profile
   - Icon: Ionicons "trophy" (outline variant when not focused)
   - Title: t('tabs.meta') -- use short "Meta" label (per CONTEXT.md and research pitfall about tab overflow)

5. Create apps/mobile/app/(tabs)/meta.tsx:
   - Segmented control at top with "Rankings" | "Tier Lists" toggle (follow existing segment toggle pill pattern from Trades tab)
   - State: activeTab: 'rankings' | 'tierlists'
   - Render DeckRankingList when rankings active, TierListBrowser when tierlists active
   - Use useTranslation for all strings

6. Create apps/mobile/src/components/meta/DeckRankingList.tsx:
   - FlashList of deck cards showing: rank number, deck name, win rate %, usage rate %, top 3 card thumbnails
   - Sort toggle pills at top: Win Rate | Usage | Trending (calls useMetaStore.setSortBy)
   - Pull-to-refresh triggers fetchDecks
   - "Last updated: {scrapedAt}" text at bottom
   - Tap deck -> open DeckDetailModal
   - Loading skeleton state, empty state when no data
   - Free users see all summary data (name, win rate, usage, top 3 cards per CONTEXT.md)

7. Create apps/mobile/src/components/meta/DeckDetailModal.tsx:
   - Full-screen modal showing deck details
   - All users see: deck name, win rate, usage rate, play count, match record, top 3 cards
   - Premium users additionally see: full card list grid, matchup data (good/bad against), tournament results
   - For non-premium users: show blurred/overlay sections for premium content with PaywallCard CTA (reuse existing PaywallCard.tsx component)
   - Close button, swipe-down dismiss
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Meta tab appears as 6th tab with trophy icon. Rankings shows sorted deck list with summary data for all users. Deck detail modal shows premium content for premium users and blurred teaser for free users. All 3 stores fetch from API endpoints.</done>
</task>

<task type="auto">
  <name>Task 2: Tier list browser and tier list card components</name>
  <files>apps/mobile/src/components/meta/TierListBrowser.tsx, apps/mobile/src/components/meta/TierListCard.tsx</files>
  <action>
1. Create apps/mobile/src/components/meta/TierListCard.tsx:
   - Card showing: title, author (if available), isOfficial badge (gold crown if official), tier preview (S/A/B/C tier labels with deck count per tier), upvoteCount with upvote button
   - Upvote button: filled heart if userVoted, outline if not. Tap calls useTierListStore.vote(tierListId)
   - Official tier lists get a gold "Official" badge and are pinned to top
   - Tap card -> navigate to tier list detail (can be inline expand or modal -- use modal for consistency)
   - Tier preview: show colored tier labels (S=red, A=orange, B=yellow, C=green, D=blue) with count of decks in each tier

2. Create apps/mobile/src/components/meta/TierListBrowser.tsx:
   - Sort toggle pills: "Most Liked" | "Newest" (calls useTierListStore.setSortBy, refetches)
   - FlashList of TierListCard components
   - Pull-to-refresh triggers fetchTierLists
   - Infinite scroll via loadMore on end reached
   - "Create Tier List" FAB button at bottom-right (navigates to tier list creator -- built in Plan 04)
   - Empty state when no tier lists exist
   - Loading skeleton state
   - Official tier list always appears first regardless of sort

3. In TierListCard, when tapped, show a modal/expanded view displaying the full tier breakdown:
   - S/A/B/C/D tier rows, each showing deck names in that tier
   - Creator name and date
   - Full upvote count with vote button
   - Delete button (trash icon) visible only if current user is the owner (compare userId from useAuthStore)
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Tier list browser shows paginated community tier lists with sort options. Tier list cards display tier preview, upvote count with toggle, and official badge. Tapping a card shows full tier breakdown. Owner can delete their own lists.</done>
</task>

</tasks>

<verification>
- `cd apps/mobile && npx tsc --noEmit` passes
- Meta tab visible in tab layout as 6th tab
- DeckRankingList renders with sort toggles
- TierListBrowser renders with sort, pagination, and vote functionality
- DeckDetailModal shows premium-gated content
</verification>

<success_criteria>
- New Meta tab accessible from tab bar with trophy icon
- Rankings segment shows deck list with win rate, usage, top cards
- Tier Lists segment shows community tier lists with upvoting
- Premium content properly gated with blurred teasers for free users
- All components compile and follow existing theme/pattern conventions
</success_criteria>

<output>
After completion, create `.planning/phases/11-intelligence/11-03-SUMMARY.md`
</output>
