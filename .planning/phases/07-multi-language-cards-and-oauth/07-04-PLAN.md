---
phase: 07-multi-language-cards-and-oauth
plan: 04
type: execute
wave: 3
depends_on: [07-02]
files_modified:
  - apps/mobile/src/stores/cards.ts
  - apps/mobile/src/stores/collection.ts
  - apps/mobile/src/stores/auth.ts
  - apps/mobile/src/components/cards/CardDetailModal.tsx
  - apps/mobile/src/components/cards/FilterChips.tsx
  - apps/mobile/src/components/cards/CardGrid.tsx
  - apps/mobile/src/components/collection/AddToCollectionModal.tsx
  - apps/mobile/app/(tabs)/cards.tsx
autonomous: false
requirements: [CARD-02, CARD-03, CARD-04]
user_setup: []

must_haves:
  truths:
    - "User can browse cards filtered by language via a language filter chip"
    - "User can see card detail with available translations across languages"
    - "User can select a specific language when adding a card to their collection"
    - "Collection entries display the language of each card"
    - "User's preferred language pre-selects when adding cards but can be overridden"
  artifacts:
    - path: "apps/mobile/src/stores/cards.ts"
      provides: "Language filter state, fetchCards with language param"
      contains: "language"
    - path: "apps/mobile/src/components/cards/CardDetailModal.tsx"
      provides: "Translation display showing available languages for a card"
      contains: "translations"
    - path: "apps/mobile/src/components/cards/FilterChips.tsx"
      provides: "Language filter chip for card browsing"
      contains: "language"
    - path: "apps/mobile/src/stores/collection.ts"
      provides: "addToCollection with language, collection entries keyed by cardId:language"
      contains: "language"
  key_links:
    - from: "apps/mobile/src/stores/cards.ts"
      to: "/api/cards"
      via: "fetch with language query param"
      pattern: "language"
    - from: "apps/mobile/src/components/cards/CardDetailModal.tsx"
      to: "/api/cards/:id/translations"
      via: "fetch translations on card detail open"
      pattern: "translations"
    - from: "apps/mobile/src/stores/collection.ts"
      to: "/api/collection"
      via: "POST with language in body"
      pattern: "language"
---

<objective>
Add language-aware card browsing, card detail translations, and language selection when adding to collection in the mobile app.

Purpose: Mobile UI for CARD-02 (select language when adding to collection), CARD-03 (filter/search by language), and CARD-04 (card detail shows translations). Consumes the API endpoints created in Plan 02.

Output: Language filter chip in card browsing, translations section in card detail modal, language picker in add-to-collection flow, and language badges on collection items.
</objective>

<execution_context>
@C:/Users/romec/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/romec/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-multi-language-cards-and-oauth/07-CONTEXT.md
@.planning/phases/07-multi-language-cards-and-oauth/07-02-SUMMARY.md

<interfaces>
<!-- API endpoints from Plan 02 that the mobile app consumes -->

Card API:
- GET /cards?language=de&q=Pikachu&set=A1 -> { cards: [...], total: N }
  Cards have translated name/imageUrl when language specified
- GET /cards/:id/translations -> { translations: [{ language, name, imageUrl, attacks }] }
- GET /cards/:id?language=fr -> card with translated name/imageUrl

Collection API (updated in Plan 02):
- POST /collection { cardId, language, quantity } -> creates language-specific entry
- GET /collection -> returns items with language field
- DELETE /collection/:cardId?language=en -> removes specific language entry
- POST /wanted { cardId, language, priority } -> creates language-specific wanted entry

User profile:
- users.preferredCardLanguage field (from Plan 01 schema)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add language filter to card browsing and card detail translations</name>
  <files>apps/mobile/src/stores/cards.ts, apps/mobile/src/components/cards/FilterChips.tsx, apps/mobile/src/components/cards/CardDetailModal.tsx, apps/mobile/src/components/cards/CardGrid.tsx, apps/mobile/app/(tabs)/cards.tsx</files>
  <action>
1. **apps/mobile/src/stores/cards.ts** -- Add language state:
   - Add `selectedLanguage: string | undefined` to store state (undefined = all languages / English default)
   - Add `setSelectedLanguage(lang: string | undefined)` action
   - Update `fetchCards` to include `language` query param when selectedLanguage is set
   - Add `fetchTranslations(cardId: string)` action that calls GET /cards/:id/translations and stores result

2. **apps/mobile/src/components/cards/FilterChips.tsx** -- Add language filter:
   - Add a language filter chip/dropdown alongside existing filter chips (set, rarity, type)
   - Display language options using 2-letter codes with full names: EN (English), DE (German), ES (Spanish), FR (French), IT (Italian), JA (Japanese), KO (Korean), PT (Portuguese), ZH (Chinese)
   - Use text codes (not flags) per Claude's discretion -- text codes are culturally neutral and unambiguous
   - "All Languages" option clears the filter
   - When a language is selected, update store's selectedLanguage and trigger fetchCards

3. **apps/mobile/src/components/cards/CardDetailModal.tsx** -- Show translations:
   - When card detail modal opens, fetch translations via store's fetchTranslations(cardId)
   - Display a "Available in" section showing language badges (e.g., "EN", "DE", "FR")
   - Tapping a language badge switches the displayed card name and image to that language's translation
   - If a language has no translation, show it as grayed out / unavailable
   - Show the currently displayed language prominently

4. **apps/mobile/src/components/cards/CardGrid.tsx** -- Optional: If cards in the grid show names, ensure they display the translated name when a language filter is active. The image should also use the language-specific imageUrl from the API response.

5. **apps/mobile/app/(tabs)/cards.tsx** -- Ensure the language filter chip is rendered in the filter bar area. Wire up the store's language state.

Claude's Discretion choices being made:
- Language representation: text codes (EN, DE, etc.) -- not flags (flags are politically complex and don't map 1:1 to languages)
- Card detail language display: tappable badges that switch the displayed translation (not tabs, since there can be up to 9 languages)
- Collection view language distinction: small badge on collection items showing the language code
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Card browsing has language filter chip. Card detail shows available translations with tappable language badges. Card grid shows translated names/images when language filter active. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Add language selection to collection and wanted list flows</name>
  <files>apps/mobile/src/stores/collection.ts, apps/mobile/src/stores/auth.ts, apps/mobile/src/components/collection/AddToCollectionModal.tsx</files>
  <action>
1. **apps/mobile/src/stores/auth.ts** -- Add preferred language:
   - Add `preferredCardLanguage: string` to user state (default 'en')
   - When user profile is fetched/loaded, set preferredCardLanguage from the API response
   - Add `setPreferredCardLanguage(lang: string)` action (calls API to update user profile)

2. **apps/mobile/src/stores/collection.ts** -- Language-aware collection:
   - Update `addToCollection(cardId, language, quantity)` to include `language` in the POST body
   - Update `removeFromCollection(cardId, language)` to include `language` query param on DELETE
   - Update `addToWanted(cardId, language, priority)` to include `language` in the POST body
   - Update `removeFromWanted(cardId, language)` to include `language` query param on DELETE
   - Update collection state key from `cardId` to `cardId:language` composite key
   - Update `getUserCollection` response parsing to include language field
   - Ensure collection progress counts distinct cardIds (language-agnostic)

3. **apps/mobile/src/components/collection/AddToCollectionModal.tsx** (create if it doesn't exist, or modify the existing add-to-collection flow):
   - When adding a card to collection, show a language picker/selector
   - Pre-select the user's `preferredCardLanguage` from auth store
   - Show available languages for this card (from translations data if loaded, otherwise show all 9)
   - User can override language selection per-card
   - On confirm, call `addToCollection(cardId, selectedLanguage, quantity)`
   - Same pattern for adding to wanted list

4. **Update existing collection display components** (find the component that renders collection items):
   - Show a small language badge (e.g., "EN", "FR") on each collection item
   - Same card in multiple languages should appear as separate entries with their respective language badges

Note: If an AddToCollectionModal does not exist (collection might be added inline), find the existing add-to-collection UI pattern and augment it with a language dropdown/picker. Check `apps/mobile/src/components/collection/` and the collection tab screen.
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Adding a card to collection shows language picker pre-set to user's preferred language. Collection entries include language badge. Same card in different languages appears as separate entries. Wanted list also language-aware. TypeScript compiles.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify language-aware card browsing and collection UI</name>
  <action>Human verification of the language-aware card browsing, card detail translations, and collection language selection features.</action>
  <what-built>Language-aware card browsing, card detail translations, and language selection when adding to collection</what-built>
  <how-to-verify>
    1. Open the app and navigate to the Cards tab
    2. Verify a language filter chip appears alongside existing filters (set, rarity, type)
    3. Select "DE" (German) -- card names should display in German where translations exist
    4. Tap a card to open the detail modal
    5. Verify "Available in" section shows language badges (EN, DE, FR, etc.)
    6. Tap a different language badge -- card name and image should switch to that language
    7. Add a card to your collection -- verify a language picker appears, pre-set to your preferred language
    8. Change language to "FR" and add the card
    9. Go to your collection -- verify the card appears with an "FR" badge
    10. Add the same card again but in "EN" -- verify it appears as a separate entry with "EN" badge
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `cd apps/mobile && npx tsc --noEmit` -- no type errors
- Language filter chip visible on cards tab
- Card detail modal shows translations
- Collection add flow includes language picker
- Collection items show language badges
- Same card in different languages = separate collection entries
</verification>

<success_criteria>
1. Language filter chip filters card browsing by language (CARD-03 mobile)
2. Card detail shows available translations with switchable language badges (CARD-04 mobile)
3. Add-to-collection flow has language picker pre-set to preferred language (CARD-02)
4. Collection items display language badges
5. Same card in different languages creates separate collection entries (per user decision)
6. Human-verified that the visual presentation is correct
</success_criteria>

<output>
After completion, create `.planning/phases/07-multi-language-cards-and-oauth/07-04-SUMMARY.md`
</output>
