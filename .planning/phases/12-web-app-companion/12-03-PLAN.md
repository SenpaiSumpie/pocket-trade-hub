---
phase: 12-web-app-companion
plan: 03
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - apps/web/src/app/(app)/cards/page.tsx
  - apps/web/src/app/(app)/collection/page.tsx
  - apps/web/src/stores/cards.ts
  - apps/web/src/stores/collection.ts
  - apps/web/src/components/cards/CardGrid.tsx
  - apps/web/src/components/cards/CardDetailModal.tsx
  - apps/web/src/components/cards/CardFilters.tsx
  - apps/web/src/components/cards/CardThumbnail.tsx
  - apps/web/src/components/collection/CollectionGrid.tsx
  - apps/web/src/components/collection/CollectionFilters.tsx
  - apps/web/src/components/ui/Modal.tsx
  - apps/web/src/components/ui/Input.tsx
  - apps/web/src/components/ui/Button.tsx
autonomous: true
requirements:
  - PLAT-01
  - PLAT-02

must_haves:
  truths:
    - "User can browse cards in a grid layout with 4-6 cards per row on desktop"
    - "User can search cards by name with live search"
    - "User can filter cards by set, rarity, and language"
    - "User can click a card to see full detail in an overlay modal"
    - "User can view their collection with owned quantities"
    - "User can add/remove cards from their collection via the web app"
  artifacts:
    - path: "apps/web/src/app/(app)/cards/page.tsx"
      provides: "Card browsing page"
      contains: "CardGrid"
    - path: "apps/web/src/stores/cards.ts"
      provides: "Card state management"
      contains: "useCardStore"
    - path: "apps/web/src/components/cards/CardDetailModal.tsx"
      provides: "Card detail overlay"
      contains: "Modal"
    - path: "apps/web/src/app/(app)/collection/page.tsx"
      provides: "Collection management page"
      contains: "CollectionGrid"
  key_links:
    - from: "apps/web/src/stores/cards.ts"
      to: "/cards/search API"
      via: "apiFetch GET"
      pattern: "apiFetch.*cards"
    - from: "apps/web/src/stores/collection.ts"
      to: "/collection API"
      via: "apiFetch GET/POST/DELETE"
      pattern: "apiFetch.*collection"
    - from: "apps/web/src/components/cards/CardGrid.tsx"
      to: "CardDetailModal"
      via: "onClick handler"
      pattern: "setSelectedCard"
---

<objective>
Build card browsing and collection management pages with grid layouts, search, filters, card detail modal, and collection CRUD.

Purpose: Cards and collection are the core data views -- users need to browse their card database and manage their collection from the web.
Output: Working Cards page with search/filter/grid and Collection page with add/remove functionality.
</objective>

<execution_context>
@C:/Users/romec/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/romec/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-web-app-companion/12-CONTEXT.md
@.planning/phases/12-web-app-companion/12-RESEARCH.md
@.planning/phases/12-web-app-companion/12-01-SUMMARY.md

@apps/web/src/lib/api.ts
@apps/web/src/lib/constants.ts
@apps/web/src/app/(app)/layout.tsx
@packages/shared/src/schemas/card.ts
@packages/shared/src/schemas/collection.ts

<interfaces>
From packages/shared/src/schemas/card.ts (expected):
```typescript
export const cardSearchSchema = z.object({
  query: z.string().optional(),
  setId: z.string().optional(),
  rarity: z.enum([...]).optional(),
  language: z.string().optional(),
  page: z.number().optional(),
  limit: z.number().optional(),
});
```

API endpoints (from existing routes):
- GET /cards/search?query=&setId=&rarity=&language=&page=&limit= -> { cards: Card[], total: number }
- GET /sets -> Set[]
- GET /cards/:id -> Card
- GET /collection -> CollectionItem[]
- POST /collection -> add card { cardId, language?, quantity? }
- DELETE /collection/:cardId -> remove card
- GET /collection/progress -> set progress stats

From apps/web/src/lib/api.ts:
```typescript
export async function apiFetch<T>(path: string, options?: RequestInit): Promise<T>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Card browsing page with grid, search, filters, and detail modal</name>
  <files>
    apps/web/src/app/(app)/cards/page.tsx,
    apps/web/src/stores/cards.ts,
    apps/web/src/components/cards/CardGrid.tsx,
    apps/web/src/components/cards/CardDetailModal.tsx,
    apps/web/src/components/cards/CardFilters.tsx,
    apps/web/src/components/cards/CardThumbnail.tsx,
    apps/web/src/components/ui/Modal.tsx,
    apps/web/src/components/ui/Input.tsx,
    apps/web/src/components/ui/Button.tsx
  </files>
  <action>
    1. Create shared UI components first:
       - Modal.tsx: "use client" overlay modal using React createPortal. Fixed inset-0 bg-black/60 z-50 flex items-center justify-center. Inner container: bg-surface rounded-xl max-w-2xl max-h-[90vh] overflow-auto. Close button (X icon from lucide-react). Click outside or Escape to close.
       - Input.tsx: Styled text input with bg-bg border-border text-text rounded-lg px-3 py-2. Supports label, error message, type, placeholder. Focus ring with ring-gold.
       - Button.tsx: Variants: primary (bg-gold text-bg), secondary (bg-surface border-border text-text), ghost (transparent). Sizes: sm, md, lg. Disabled state. Loading state with spinner.

    2. cards store (stores/cards.ts): "use client" Zustand store.
       - State: cards[], sets[], total, page, limit (24), query, setId, rarity, language, selectedCard, loading
       - Actions: fetchCards() -- calls GET /cards/search with current filters, fetchSets() -- calls GET /sets, setFilter(key, value) -- updates filter and refetches, setPage(n), selectCard(card), clearSelection()
       - Search is debounced: setQuery updates state, useEffect in component triggers fetchCards after 300ms

    3. CardFilters.tsx: "use client" component.
       - Search input (Input component) for card name query
       - Set dropdown (select element styled with Tailwind) populated from sets
       - Rarity filter chips (diamond1-4, star1-3, crown) as toggleable buttons
       - Language dropdown with the 9 supported languages
       - All filters update the cards store via setFilter()
       - Layout: horizontal row wrapping on smaller screens

    4. CardThumbnail.tsx: Card image with hover effect.
       - Shows card image (from card.image URL), card name below
       - Hover: scale-105 transition, shadow-lg, ring-1 ring-gold/30
       - Click: calls selectCard(card) on the store
       - Rarity badge in corner (diamond/star/crown symbols)
       - Aspect ratio maintained, object-cover

    5. CardGrid.tsx: "use client" component.
       - CSS grid: grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5 xl:grid-cols-6 gap-4
       - Maps over cards from store, renders CardThumbnail for each
       - Shows loading skeletons when store.loading is true (grid of Skeleton components)
       - Pagination at bottom: Previous/Next buttons with page indicator
       - Empty state: "No cards found" message

    6. CardDetailModal.tsx: "use client" component.
       - Uses Modal component, opens when selectedCard is not null
       - Shows large card image, name, set name, rarity, card number
       - Shows available languages as badges
       - "Add to Collection" button (calls collection store -- will be wired in Task 2)
       - Close clears selectedCard

    7. cards/page.tsx: "use client" page.
       - On mount, fetch sets and initial cards via useEffect
       - Renders CardFilters at top, CardGrid below, CardDetailModal overlay
       - Page title "Card Database" in text-2xl font-bold text-text
  </action>
  <verify>
    <automated>cd apps/web && pnpm build 2>&1 | tail -10</automated>
  </verify>
  <done>Cards page renders with responsive grid (4-6 per row on desktop). Search filters cards by name with debounce. Set/rarity/language filters work. Clicking a card opens detail modal overlay. Pagination navigates through results.</done>
</task>

<task type="auto">
  <name>Task 2: Collection management page with add/remove</name>
  <files>
    apps/web/src/app/(app)/collection/page.tsx,
    apps/web/src/stores/collection.ts,
    apps/web/src/components/collection/CollectionGrid.tsx,
    apps/web/src/components/collection/CollectionFilters.tsx,
    apps/web/src/components/cards/CardDetailModal.tsx
  </files>
  <action>
    1. collection store (stores/collection.ts): "use client" Zustand store.
       - State: items[] (CollectionItem with card data), sets[] with progress, loading, filter (setId, query)
       - Actions:
         - fetchCollection() -- GET /collection, populates items
         - fetchProgress() -- GET /collection/progress, populates set progress
         - addToCollection(cardId, language?, quantity?) -- POST /collection with optimistic update
         - removeFromCollection(cardId) -- DELETE /collection/:cardId with optimistic update
         - updateQuantity(cardId, quantity) -- PUT /collection/:cardId (if exists) or POST
         - setFilter(key, value) -- client-side filtering of loaded items

    2. CollectionFilters.tsx: "use client" component.
       - Search input for filtering collection by card name (client-side filter on loaded items)
       - Set filter dropdown
       - Show total owned count and set completion percentage
       - Layout similar to CardFilters

    3. CollectionGrid.tsx: "use client" component.
       - Same responsive grid as CardGrid but shows collection-specific info
       - Each card shows: image, name, quantity badge (bottom-right corner), language badge
       - Hover shows quick actions: +/- quantity buttons, remove button
       - Click opens CardDetailModal in "collection mode" showing quantity controls
       - Empty state: "No cards in your collection yet. Browse cards to start adding."
       - Loading skeletons while fetching

    4. collection/page.tsx: "use client" page.
       - On mount, fetch collection and progress
       - Renders CollectionFilters at top, CollectionGrid below
       - Page title "My Collection" with total count badge
       - Set progress section: horizontal scrollable row of set cards showing owned/total with progress bar

    5. Update CardDetailModal.tsx:
       - Accept optional mode prop: 'browse' | 'collection'
       - In browse mode: "Add to Collection" button that calls collection store addToCollection
       - In collection mode: Show quantity controls (+/-), language, remove button
       - Wire the "Add to Collection" button on the cards page detail modal to actually call the collection store
  </action>
  <verify>
    <automated>cd apps/web && pnpm build 2>&1 | tail -10</automated>
  </verify>
  <done>Collection page shows user's owned cards with quantities. User can add cards from the card browser. User can remove cards or adjust quantities from the collection view. Set progress displays completion stats.</done>
</task>

</tasks>

<verification>
- `cd apps/web && pnpm build` succeeds
- Cards page shows responsive card grid
- Search filters cards in real-time
- Card detail modal opens on click
- Collection page shows owned cards with quantities
- Add/remove from collection works via API calls
</verification>

<success_criteria>
- Card grid renders 4-6 cards per row on desktop, responsive down to 2 columns
- Card search returns results within 300ms debounce
- Set, rarity, and language filters narrow results correctly
- Card detail modal shows full card info in overlay
- Collection shows all owned cards with quantities
- Add to collection from card browser works
- Remove from collection works with optimistic update
- Set progress shows completion percentage
</success_criteria>

<output>
After completion, create `.planning/phases/12-web-app-companion/12-03-SUMMARY.md`
</output>
