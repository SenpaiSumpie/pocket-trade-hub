---
phase: 12-web-app-companion
plan: 03
type: execute
wave: 2
depends_on: ["12-02"]
files_modified:
  - apps/web/src/app/(auth)/layout.tsx
  - apps/web/src/app/(auth)/login/page.tsx
  - apps/web/src/app/(auth)/signup/page.tsx
  - apps/web/src/app/(app)/layout.tsx
  - apps/web/src/app/(app)/cards/page.tsx
  - apps/web/src/components/layout/Sidebar.tsx
  - apps/web/src/components/layout/MobileGate.tsx
  - apps/web/src/components/ui/Skeleton.tsx
  - apps/web/src/middleware.ts
autonomous: true
requirements:
  - PLAT-01

must_haves:
  truths:
    - "Unauthenticated user is redirected to /login by middleware"
    - "Sidebar navigation is visible on authenticated pages"
    - "Mobile browser users (<768px) see download prompt instead of app"
    - "Auth and app route groups have separate layouts"
  artifacts:
    - path: "apps/web/src/middleware.ts"
      provides: "Auth route protection"
      contains: "middleware"
    - path: "apps/web/src/components/layout/Sidebar.tsx"
      provides: "Persistent sidebar navigation"
      contains: "Sidebar"
    - path: "apps/web/src/app/(app)/layout.tsx"
      provides: "Sidebar layout wrapper"
      contains: "Sidebar"
    - path: "apps/web/src/components/layout/MobileGate.tsx"
      provides: "Mobile download prompt"
      contains: "download"
  key_links:
    - from: "apps/web/src/middleware.ts"
      to: "/login redirect"
      via: "cookie check"
      pattern: "accessToken"
    - from: "apps/web/src/app/(app)/layout.tsx"
      to: "Sidebar"
      via: "layout composition"
      pattern: "Sidebar"
---

<objective>
Build layouts, sidebar navigation, auth middleware, and route group structure. Creates the auth page placeholders, authenticated app shell with sidebar, and mobile gate.

Purpose: Route protection and layout shell are required before any feature pages. This establishes the navigation structure all feature plans depend on.
Output: Middleware protects routes, sidebar shows navigation, mobile users see download prompt, auth/app layouts are separate.
</objective>

<execution_context>
@C:/Users/romec/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/romec/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-web-app-companion/12-CONTEXT.md
@.planning/phases/12-web-app-companion/12-RESEARCH.md
@.planning/phases/12-web-app-companion/12-02-SUMMARY.md

@apps/web/src/stores/auth.ts
@apps/web/src/lib/api.ts
@apps/web/src/app/layout.tsx

<interfaces>
From apps/web/src/stores/auth.ts (from Plan 02):
```typescript
export const useAuthStore = create<AuthState>((set) => ({
  user, isLoggedIn, isHydrated,
  login: async (email, password) => { ... },
  signup: async (email, password, displayName) => { ... },
  logout: async () => { ... },
  hydrate: async () => { ... },
}));
```

From apps/web/__tests__/middleware.test.ts (stubs from Plan 01):
```typescript
// TODO stubs for middleware -- implement actual tests after creating middleware.ts
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auth middleware and route group layouts</name>
  <files>
    apps/web/src/middleware.ts,
    apps/web/src/app/(auth)/layout.tsx,
    apps/web/src/app/(auth)/login/page.tsx,
    apps/web/src/app/(auth)/signup/page.tsx,
    apps/web/src/app/(app)/layout.tsx,
    apps/web/src/app/(app)/cards/page.tsx
  </files>
  <action>
    1. middleware.ts: Check for accessToken cookie. If missing and route is not /login or /signup, redirect to /login. If present and route is /login or /signup, redirect to /cards. Matcher excludes _next, static, favicon, api paths.

    2. (auth)/layout.tsx: Centered layout without sidebar, dark bg, for login/signup pages. Max-w-md, flex items-center justify-center min-h-screen. Show "Pocket Trade Hub" title above content.

    3. (auth)/login/page.tsx: Placeholder "Login" page with heading (fully built in Plan 04).

    4. (auth)/signup/page.tsx: Placeholder "Sign Up" page with heading (fully built in Plan 04).

    5. (app)/layout.tsx: "use client" layout with Sidebar + MobileGate + main content area. Uses auth store hydration -- shows skeleton until isHydrated is true.

    6. (app)/cards/page.tsx: Placeholder "Cards" page with heading (fully built in Plan 05).

    7. Implement the todo middleware test stubs from Plan 01 in __tests__/middleware.test.ts -- test redirect logic by importing the middleware function and mocking NextRequest/NextResponse.
  </action>
  <verify>
    <automated>cd apps/web && pnpm build 2>&1 | tail -10 && pnpm test 2>&1 | tail -10</automated>
  </verify>
  <done>Middleware redirects unauthenticated users to /login. Auth layout centered without sidebar. App layout includes sidebar shell. Middleware tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Sidebar navigation and mobile gate</name>
  <files>
    apps/web/src/components/layout/Sidebar.tsx,
    apps/web/src/components/layout/MobileGate.tsx,
    apps/web/src/components/ui/Skeleton.tsx
  </files>
  <action>
    1. Sidebar.tsx: "use client" component with persistent left sidebar. Width 240px, collapsible to 64px icon-only on smaller screens (< 1024px). Navigation items: Cards (/cards), Collection (/collection), Market (/market), Proposals (/proposals), Meta (/meta), Tier Lists (/tierlists). Use lucide-react icons (LayoutGrid, Library, Store, ArrowLeftRight, BarChart3, List). Dark surface bg, gold accent on active item (use usePathname from next/navigation). Logout button at bottom calling auth store logout.

    2. MobileGate.tsx: CSS media query approach per research -- hidden md:contents for main content, md:hidden for download prompt. Shows "Pocket Trade Hub" title and "download the app" message for viewports < 768px.

    3. Skeleton.tsx: Simple reusable skeleton component with animate-pulse, configurable width/height/rounded via className.
  </action>
  <verify>
    <automated>cd apps/web && pnpm build 2>&1 | tail -10 && pnpm test 2>&1 | tail -10</automated>
  </verify>
  <done>Sidebar renders with correct icons and links, gold accent on active item. MobileGate blocks small viewports with download prompt. Skeleton component available for loading states.</done>
</task>

</tasks>

<verification>
- `cd apps/web && pnpm build` -- build succeeds
- `cd apps/web && pnpm test` -- middleware tests pass
- Visit http://localhost:3001 -- redirects to /login (no cookie)
- Sidebar navigation visible on authenticated pages
</verification>

<success_criteria>
- Middleware redirects unauthenticated users to /login
- Sidebar navigation renders with 6 nav items and correct icons
- Active sidebar item highlighted with gold accent
- MobileGate shows download prompt below 768px
- Skeleton component available for loading states
- Middleware tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/12-web-app-companion/12-03-SUMMARY.md`
</output>
