---
phase: 12-web-app-companion
plan: 05
type: execute
wave: 3
depends_on: ["12-01"]
files_modified:
  - apps/web/src/app/(app)/meta/page.tsx
  - apps/web/src/app/(app)/tierlists/page.tsx
  - apps/web/src/stores/meta.ts
  - apps/web/src/stores/tierlists.ts
  - apps/web/src/components/meta/DeckRankings.tsx
  - apps/web/src/components/meta/DeckDetailModal.tsx
  - apps/web/src/components/meta/MetaFilters.tsx
  - apps/web/src/components/meta/TierListBrowser.tsx
  - apps/web/src/components/meta/TierListDetailModal.tsx
  - apps/web/src/components/meta/TierListCreator.tsx
  - apps/web/src/hooks/useSocket.ts
autonomous: true
requirements:
  - PLAT-02

must_haves:
  truths:
    - "User can browse competitive deck meta with rankings, win rates, and usage data"
    - "User can view deck detail with card list and tournament results"
    - "User can browse curated and user-created tier lists"
    - "User can create and share their own tier list"
    - "User receives real-time notifications for new matches and proposals via Socket.IO"
  artifacts:
    - path: "apps/web/src/app/(app)/meta/page.tsx"
      provides: "Deck meta page"
      contains: "DeckRankings"
    - path: "apps/web/src/app/(app)/tierlists/page.tsx"
      provides: "Tier lists page"
      contains: "TierListBrowser"
    - path: "apps/web/src/stores/meta.ts"
      provides: "Meta state management"
      contains: "useMetaStore"
    - path: "apps/web/src/hooks/useSocket.ts"
      provides: "Socket.IO real-time hook"
      contains: "useSocket"
  key_links:
    - from: "apps/web/src/stores/meta.ts"
      to: "/meta API"
      via: "apiFetch GET"
      pattern: "apiFetch.*meta"
    - from: "apps/web/src/stores/tierlists.ts"
      to: "/tierlists API"
      via: "apiFetch CRUD"
      pattern: "apiFetch.*tierlists"
    - from: "apps/web/src/hooks/useSocket.ts"
      to: "apps/web/src/lib/socket.ts"
      via: "socket connection on auth"
      pattern: "socket.connect"
---

<objective>
Build deck meta analytics, tier list pages, and Socket.IO real-time notification wiring.

Purpose: Meta and tier lists are premium differentiators. Socket.IO provides live trade notifications completing the real-time experience.
Output: Working Meta page with deck rankings, Tier Lists page with browse/create, and real-time notification support.
</objective>

<execution_context>
@C:/Users/romec/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/romec/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-web-app-companion/12-CONTEXT.md
@.planning/phases/12-web-app-companion/12-RESEARCH.md
@.planning/phases/12-web-app-companion/12-01-SUMMARY.md

@apps/web/src/lib/api.ts
@apps/web/src/lib/socket.ts
@apps/web/src/stores/auth.ts
@apps/web/src/components/ui/Modal.tsx
@packages/shared/src/schemas/meta.ts
@packages/shared/src/schemas/tierlist.ts

<interfaces>
API endpoints (from existing routes):
- GET /meta/decks?format=&sort=&page=&limit= -> { decks: Deck[], total }
- GET /meta/decks/:id -> Deck with cards and tournament results
- GET /meta/snapshot -> MetaSnapshot (latest meta summary)
- GET /tierlists?sort=&page=&limit= -> { tierLists: TierList[], total }
- GET /tierlists/:id -> TierList with tiers and cards
- POST /tierlists -> create tier list { name, description, tiers: [{ name, rank, deckIds }] }
- PUT /tierlists/:id -> update
- DELETE /tierlists/:id -> delete own

Socket.IO events (from existing server):
- 'notification' -> { type, message, data } for new matches, proposals, etc.
- Client emits 'authenticate' with token after connection

From apps/web/src/lib/socket.ts (from Plan 01):
```typescript
export const socket: Socket = io(API_URL, { autoConnect: false, withCredentials: true });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deck meta and tier list pages</name>
  <files>
    apps/web/src/app/(app)/meta/page.tsx,
    apps/web/src/app/(app)/tierlists/page.tsx,
    apps/web/src/stores/meta.ts,
    apps/web/src/stores/tierlists.ts,
    apps/web/src/components/meta/DeckRankings.tsx,
    apps/web/src/components/meta/DeckDetailModal.tsx,
    apps/web/src/components/meta/MetaFilters.tsx,
    apps/web/src/components/meta/TierListBrowser.tsx,
    apps/web/src/components/meta/TierListDetailModal.tsx,
    apps/web/src/components/meta/TierListCreator.tsx
  </files>
  <action>
    1. meta store (stores/meta.ts): "use client" Zustand store.
       - State: decks[], snapshot (MetaSnapshot | null), total, page, filters (format, sort), selectedDeck, loading
       - Actions: fetchDecks(), fetchSnapshot(), selectDeck(deck), setFilter(key, value), setPage(n)

    2. tierlists store (stores/tierlists.ts): "use client" Zustand store.
       - State: tierLists[], total, page, selectedTierList, showCreator, loading
       - Actions: fetchTierLists(), selectTierList(tl), createTierList(data), updateTierList(id, data), deleteTierList(id), toggleCreator(), setPage(n)

    3. DeckRankings.tsx: "use client" component.
       - Table/list layout showing deck rankings
       - Columns: Rank (#), Deck Name, Win Rate (percentage with color: green > 50%, red < 50%), Usage Rate (percentage), Trend indicator (up/down/stable arrow)
       - Each row clickable, opens DeckDetailModal
       - Rows use alternating bg-surface/bg-bg for readability
       - Responsive: table on desktop, cards on tablet

    4. MetaFilters.tsx: Sort options (by win rate, usage, name). Format filter if applicable.

    5. DeckDetailModal.tsx: Overlay modal for deck detail.
       - Deck name, archetype description
       - Win rate and usage rate with visual bars
       - Card list: grid of card thumbnails that make up the deck
       - Tournament results section: list of recent tournament placements (date, placement, player)
       - "View Tier Lists with this Deck" link

    6. TierListBrowser.tsx: "use client" component.
       - Grid of tier list cards: grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4
       - Each card shows: tier list name, creator name, created date, preview of tier distribution (S/A/B/C labels with deck counts)
       - Click opens TierListDetailModal
       - "Create Tier List" button at top (gold primary)
       - Sort: newest, most popular

    7. TierListDetailModal.tsx: Full tier list in overlay.
       - Tier rows: S tier (gold bg), A tier (purple), B tier (blue), C tier (green), D tier (gray)
       - Each tier row shows deck names/thumbnails horizontally
       - Creator info and date
       - If own tier list: edit/delete buttons

    8. TierListCreator.tsx: Modal/page for creating a tier list.
       - Name input, description textarea
       - Tier rows: S/A/B/C/D with drag-to-reorder (or click-to-assign approach for simplicity)
       - Deck selector: search available decks from meta store, click to add to a tier
       - Click a deck in a tier to move it between tiers or remove
       - Submit creates via tierlists store
       - Simple approach without drag-and-drop library: each tier has an "Add Deck" button that opens a dropdown/search, and decks show "Move to" and "Remove" buttons on hover

    9. meta/page.tsx: "use client" page.
       - On mount, fetch decks and snapshot
       - Shows MetaSnapshot summary at top (total decks tracked, last updated date)
       - MetaFilters, DeckRankings below
       - DeckDetailModal overlay
       - Page title "Deck Meta"

    10. tierlists/page.tsx: "use client" page.
        - On mount, fetch tier lists
        - TierListBrowser with pagination
        - TierListDetailModal and TierListCreator overlays
        - Page title "Tier Lists"
  </action>
  <verify>
    <automated>cd apps/web && pnpm build 2>&1 | tail -10</automated>
  </verify>
  <done>Meta page shows deck rankings with win/usage rates. Deck detail modal shows cards and tournament results. Tier lists page shows browsable tier lists. Users can create tier lists with deck assignment. Tier list detail shows S/A/B/C/D tiers with decks.</done>
</task>

<task type="auto">
  <name>Task 2: Socket.IO real-time notifications and final wiring</name>
  <files>
    apps/web/src/hooks/useSocket.ts,
    apps/web/src/app/(app)/layout.tsx,
    apps/web/src/components/layout/Sidebar.tsx
  </files>
  <action>
    1. useSocket.ts: "use client" custom hook.
       - Imports socket from lib/socket.ts
       - On mount (if authenticated via auth store), connect the socket
       - Listen for 'notification' events
       - On notification: show a toast/banner notification at top-right of screen (simple div with position fixed, auto-dismiss after 5s, bg-surface border-gold)
       - Track notification count for badge display
       - On unmount or logout, disconnect socket
       - Handle reconnection gracefully

    2. Create a simple toast notification system:
       - NotificationToast component: fixed top-right, stacked toasts
       - Auto-dismiss after 5 seconds with fade-out
       - Shows notification message and type icon
       - Click to dismiss
       - Store toast state in a simple Zustand slice or use React state in the layout

    3. Update (app)/layout.tsx:
       - Add useSocket() hook call to establish connection when authenticated
       - Render NotificationToast container

    4. Update Sidebar.tsx:
       - Add notification badge (red dot or count) on the Proposals nav item when there are unread received proposals
       - Badge uses the notification count from useSocket or proposals store

    5. Final verification:
       - Ensure all sidebar navigation links point to correct routes: /cards, /collection, /market, /proposals, /meta, /tierlists
       - Ensure all pages are properly linked from the sidebar
       - Test that the active sidebar item highlights correctly based on pathname (use usePathname from next/navigation)
  </action>
  <verify>
    <automated>cd apps/web && pnpm build 2>&1 | tail -10</automated>
  </verify>
  <done>Socket.IO connects on auth and receives real-time notifications. Toast notifications appear for new matches/proposals. Sidebar shows notification badge. All navigation links work correctly.</done>
</task>

</tasks>

<verification>
- `cd apps/web && pnpm build` succeeds
- Meta page shows deck rankings table
- Deck detail modal opens with cards and stats
- Tier lists page shows browsable list
- Tier list creator allows adding decks to tiers
- Socket.IO connects after authentication
- Toast notifications appear for events
- All sidebar nav items link to correct pages
</verification>

<success_criteria>
- Deck rankings display with win rates, usage rates, and trends
- Deck detail modal shows full deck composition and tournament results
- Tier list browser shows user and curated tier lists
- Tier list creator allows building S/A/B/C/D tier lists with deck selection
- Socket.IO establishes connection on auth and receives notifications
- Toast notifications display and auto-dismiss
- Sidebar notification badge reflects unread proposals
- All 6 main pages accessible and functional from sidebar
</success_criteria>

<output>
After completion, create `.planning/phases/12-web-app-companion/12-05-SUMMARY.md`
</output>
