---
phase: 12-web-app-companion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/plugins/auth.ts
  - apps/api/src/routes/auth.ts
  - apps/api/src/server.ts
  - apps/api/package.json
  - apps/web/package.json
  - apps/web/tsconfig.json
  - apps/web/next.config.ts
  - apps/web/postcss.config.mjs
  - apps/web/src/app/globals.css
  - apps/web/src/app/layout.tsx
  - apps/web/src/app/page.tsx
  - apps/web/src/app/not-found.tsx
  - apps/web/src/app/error.tsx
  - apps/web/src/app/(auth)/login/page.tsx
  - apps/web/src/app/(auth)/signup/page.tsx
  - apps/web/src/app/(auth)/layout.tsx
  - apps/web/src/app/(app)/layout.tsx
  - apps/web/src/app/(app)/cards/page.tsx
  - apps/web/src/components/layout/Sidebar.tsx
  - apps/web/src/components/layout/MobileGate.tsx
  - apps/web/src/components/ui/Skeleton.tsx
  - apps/web/src/lib/api.ts
  - apps/web/src/lib/constants.ts
  - apps/web/src/lib/socket.ts
  - apps/web/src/i18n/index.ts
  - apps/web/src/middleware.ts
  - apps/web/src/stores/auth.ts
  - turbo.json
autonomous: true
requirements:
  - PLAT-01

must_haves:
  truths:
    - "Web app starts and renders in a browser at localhost:3001"
    - "Unauthenticated user is redirected to /login by middleware"
    - "API accepts JWT from cookies alongside Bearer header for mobile backward compat"
    - "CORS allows web origin with credentials for cookie-based auth"
    - "Dark theme with gold accent matches mobile app branding"
    - "Sidebar navigation is visible on authenticated pages"
    - "Mobile browser users (<768px) see download prompt instead of app"
  artifacts:
    - path: "apps/web/package.json"
      provides: "Next.js project configuration"
      contains: "next"
    - path: "apps/web/src/middleware.ts"
      provides: "Auth route protection"
      contains: "middleware"
    - path: "apps/web/src/lib/api.ts"
      provides: "Cookie-credentialed API fetch wrapper"
      contains: "credentials"
    - path: "apps/api/src/plugins/auth.ts"
      provides: "Dual auth (cookie + Bearer)"
      contains: "cookie"
    - path: "apps/web/src/app/(app)/layout.tsx"
      provides: "Sidebar layout wrapper"
      contains: "Sidebar"
  key_links:
    - from: "apps/web/src/lib/api.ts"
      to: "apps/api"
      via: "fetch with credentials: include"
      pattern: "credentials.*include"
    - from: "apps/api/src/plugins/auth.ts"
      to: "request.cookies"
      via: "@fastify/cookie plugin"
      pattern: "cookies.*accessToken"
    - from: "apps/web/src/middleware.ts"
      to: "/login redirect"
      via: "cookie check"
      pattern: "accessToken"
---

<objective>
Set up the web app foundation: API cookie auth support, Next.js project scaffold with Tailwind dark theme, sidebar layout, auth middleware, apiFetch wrapper, i18n, and Socket.IO client.

Purpose: Everything else in Phase 12 depends on this foundation -- cookie auth, the project structure, theming, and the authenticated layout shell.
Output: Working Next.js app that boots, redirects unauthenticated users to login, and shows a sidebar layout shell for authenticated routes.
</objective>

<execution_context>
@C:/Users/romec/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/romec/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-web-app-companion/12-CONTEXT.md
@.planning/phases/12-web-app-companion/12-RESEARCH.md

@apps/api/src/plugins/auth.ts
@apps/api/src/routes/auth.ts
@apps/api/src/server.ts
@apps/api/package.json
@turbo.json
@pnpm-workspace.yaml

<interfaces>
<!-- Key types and contracts the executor needs -->

From apps/api/src/plugins/auth.ts:
```typescript
// Current: only reads Authorization header via request.jwtVerify()
// Must update to also read from request.cookies.accessToken
fastify.decorate('authenticate', async (request, reply) => {
  try { await request.jwtVerify(); } catch { reply.code(401).send({ error: 'Unauthorized' }); }
});
```

From apps/api/src/routes/auth.ts:
```typescript
// Current: returns tokens in JSON body
// Must also set HTTP-only cookies for web clients
// Detection: check Origin or X-Client-Type header, or always set cookies (harmless for mobile)
reply.code(200).send({ ...tokens, user: { ... } });
```

From apps/api/src/server.ts:
```typescript
// Current CORS: { origin: true } -- must change to specific origins + credentials: true
await app.register(cors, { origin: true });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: API cookie auth and CORS updates</name>
  <files>
    apps/api/src/plugins/auth.ts,
    apps/api/src/routes/auth.ts,
    apps/api/src/server.ts,
    apps/api/package.json
  </files>
  <action>
    1. Install @fastify/cookie in apps/api: `cd apps/api && pnpm add @fastify/cookie`

    2. Update apps/api/src/server.ts:
       - Import and register @fastify/cookie BEFORE authPlugin
       - Update CORS config: `origin: [process.env.WEB_ORIGIN || 'http://localhost:3001', true]` with `credentials: true`
       - The `true` in the origin array preserves mobile compatibility (any origin)
       - Actually, use a function: `origin: (origin, cb) => cb(null, true)` with `credentials: true` to allow all origins with credentials in dev. In production, restrict to specific origins via env var.
       - Simpler approach per research: `origin: process.env.WEB_ORIGIN || 'http://localhost:3001'` with `credentials: true`. But this breaks mobile. So use array approach: `origin: [process.env.WEB_ORIGIN || 'http://localhost:3001']` with `credentials: true`. Actually @fastify/cors supports a function or array. Use function approach to support both mobile (any origin) and web (specific origin with credentials):
       ```typescript
       await app.register(cors, {
         origin: (origin, cb) => { cb(null, true); },
         credentials: true,
       });
       ```

    3. Update apps/api/src/plugins/auth.ts:
       - Import FastifyCookieOptions type is not needed, just use request.cookies
       - Update authenticate decorator: try request.jwtVerify() first (Bearer header for mobile). On catch, check request.cookies?.accessToken. If found, verify it with fastify.jwt.verify(token) and set request.user = decoded. If neither works, return 401.
       - Add type import for the cookie: `import type { FastifyRequest, FastifyReply } from 'fastify';` (already imported)

    4. Update apps/api/src/routes/auth.ts:
       - After issuing tokens on /auth/signup and /auth/login, ALSO set HTTP-only cookies:
         ```typescript
         const cookieOpts = {
           httpOnly: true,
           secure: process.env.NODE_ENV === 'production',
           sameSite: process.env.NODE_ENV === 'production' ? 'none' as const : 'lax' as const,
           path: '/',
         };
         reply
           .setCookie('accessToken', tokens.accessToken, { ...cookieOpts, maxAge: 15 * 60 })
           .setCookie('refreshToken', tokens.refreshToken, { ...cookieOpts, maxAge: 7 * 24 * 60 * 60 });
         ```
       - Update /auth/refresh: read refreshToken from body (mobile) OR request.cookies?.refreshToken (web). After issuing new tokens, set cookies again.
       - Update /auth/logout: clear cookies with `reply.clearCookie('accessToken', { path: '/' }).clearCookie('refreshToken', { path: '/' })`
       - These cookie operations are harmless for mobile clients (mobile ignores Set-Cookie headers).

    5. Verify existing API tests still pass -- cookie additions should not break Bearer auth flow.
  </action>
  <verify>
    <automated>cd apps/api && pnpm test 2>&1 | tail -20</automated>
  </verify>
  <done>API auth plugin reads JWT from cookies or Bearer header. Auth routes set HTTP-only cookies on login/signup/refresh. CORS allows credentials. Existing mobile auth flow unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Next.js project scaffold with theme, layout, and core lib</name>
  <files>
    apps/web/package.json,
    apps/web/tsconfig.json,
    apps/web/next.config.ts,
    apps/web/postcss.config.mjs,
    apps/web/src/app/globals.css,
    apps/web/src/app/layout.tsx,
    apps/web/src/app/page.tsx,
    apps/web/src/app/not-found.tsx,
    apps/web/src/app/error.tsx,
    apps/web/src/app/(auth)/layout.tsx,
    apps/web/src/app/(auth)/login/page.tsx,
    apps/web/src/app/(auth)/signup/page.tsx,
    apps/web/src/app/(app)/layout.tsx,
    apps/web/src/app/(app)/cards/page.tsx,
    apps/web/src/components/layout/Sidebar.tsx,
    apps/web/src/components/layout/MobileGate.tsx,
    apps/web/src/components/ui/Skeleton.tsx,
    apps/web/src/lib/api.ts,
    apps/web/src/lib/constants.ts,
    apps/web/src/lib/socket.ts,
    apps/web/src/i18n/index.ts,
    apps/web/src/middleware.ts,
    apps/web/src/stores/auth.ts,
    turbo.json
  </files>
  <action>
    1. Create apps/web directory and initialize:
       - package.json with name "web", scripts: dev (next dev -p 3001), build (next build), start (next start), test placeholder
       - Dependencies per RESEARCH.md: next@^15.2, react@^19.1, react-dom@^19.1, zustand@^5.0, socket.io-client@^4.8, i18next@^25.8, react-i18next@^16.5, zod@^3.24, @pocket-trade-hub/shared (workspace:*), lucide-react
       - DevDeps: typescript@^5.8, @types/react@^19.1, tailwindcss@^4.0, @tailwindcss/postcss, postcss, autoprefixer
       - Run `cd apps/web && pnpm install`

    2. tsconfig.json: Extend from base if exists, or create with target ES2022, module ESNext, moduleResolution bundler, jsx preserve, paths for @/* -> src/*

    3. next.config.ts: Enable transpilePackages: ['@pocket-trade-hub/shared']. Set output standalone if needed.

    4. postcss.config.mjs: `export default { plugins: { '@tailwindcss/postcss': {} } }`

    5. globals.css with Tailwind v4 CSS-first config:
       ```css
       @import "tailwindcss";
       @theme {
         --color-bg: #0a0a0a;
         --color-surface: #18181b;
         --color-surface-hover: #27272a;
         --color-border: #3f3f46;
         --color-text: #fafafa;
         --color-text-muted: #a1a1aa;
         --color-gold: #f0c040;
         --color-gold-hover: #d4a830;
       }
       ```

    6. Root layout.tsx: Import globals.css, set <html lang="en" className="dark">, <body className="bg-bg text-text min-h-screen">

    7. Root page.tsx: Redirect to /cards (or /login if not authenticated -- middleware handles this)

    8. not-found.tsx: Simple 404 page with dark theme, "Page not found" message, link back to /cards

    9. error.tsx: "use client" error boundary with dark theme, error message, retry button

    10. (auth)/layout.tsx: Centered layout without sidebar, dark bg, for login/signup pages

    11. (auth)/login/page.tsx: Placeholder "Login" page (will be fully built in Plan 02). Just show the heading and a note.

    12. (auth)/signup/page.tsx: Placeholder "Sign Up" page (will be fully built in Plan 02). Just show the heading.

    13. (app)/layout.tsx: "use client" layout with Sidebar + MobileGate + main content area. Uses auth store hydration -- shows skeleton until isHydrated is true.

    14. (app)/cards/page.tsx: Placeholder "Cards" page with heading (fully built in Plan 03).

    15. Sidebar.tsx: "use client" component with persistent left sidebar. Width 240px, collapsible to 64px icon-only on smaller screens (< 1024px). Navigation items: Cards (/cards), Collection (/collection), Market (/market), Proposals (/proposals), Meta (/meta), Tier Lists (/tierlists). Use lucide-react icons (LayoutGrid, Library, Store, ArrowLeftRight, BarChart3, List). Dark surface bg, gold accent on active item. Logout button at bottom calling auth store logout.

    16. MobileGate.tsx: CSS media query approach per research -- hidden md:contents for main content, md:hidden for download prompt. Shows "Pocket Trade Hub" title and "download the app" message for viewports < 768px.

    17. Skeleton.tsx: Simple reusable skeleton component with animate-pulse, configurable width/height/rounded via className.

    18. lib/api.ts: apiFetch<T> wrapper per research Pattern 1. Uses NEXT_PUBLIC_API_URL env var. credentials: 'include'. On 401, tries /auth/refresh (POST with credentials: include), on success retries original request, on failure redirects to /login. Content-Type: application/json.

    19. lib/constants.ts: API_URL from env, theme color tokens (matching globals.css values for JS usage if needed).

    20. lib/socket.ts: "use client" module. Creates socket.io-client instance with autoConnect: false, withCredentials: true. Export socket instance and a connectSocket/disconnectSocket pair.

    21. i18n/index.ts: Initialize i18next with react-i18next. Import en.json from mobile's locale path (../../mobile/src/i18n/locales/en.json -- or copy the locale files into apps/web/locales/). Use getBrowserLanguage() to detect initial language. Fallback to en.
       - Decision: Copy locale files into apps/web/src/i18n/locales/ (simpler than cross-app imports which may not resolve in Next.js). Copy all 10 locale files from apps/mobile/src/i18n/locales/.

    22. middleware.ts: Check for accessToken cookie. If missing and route is not /login or /signup, redirect to /login. If present and route is /login or /signup, redirect to /cards. Matcher excludes _next, static, favicon, api paths.

    23. stores/auth.ts: "use client" Zustand store with: user (null | User type), isLoggedIn, isHydrated. Actions: login(email, password) via apiFetch POST /auth/login, signup(email, password, displayName) via apiFetch POST /auth/signup, logout() via apiFetch POST /auth/logout then clear state and redirect, hydrate() via apiFetch GET /users/me to populate user on app load.

    24. Update turbo.json: Add web-specific task config if needed (build outputs .next/**).

    25. Run `pnpm install` at root to link workspaces, then `cd apps/web && pnpm dev` to verify the app starts.
  </action>
  <verify>
    <automated>cd apps/web && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <done>Next.js app builds and starts at localhost:3001. Dark theme with gold accent renders. Unauthenticated users redirected to /login. Authenticated layout shows sidebar. MobileGate blocks small viewports. apiFetch sends credentials. i18n initialized with 10 locales.</done>
</task>

</tasks>

<verification>
- `cd apps/api && pnpm test` -- existing API tests pass (cookie auth is additive)
- `cd apps/web && pnpm build` -- Next.js build succeeds
- `cd apps/web && pnpm dev` starts without errors on port 3001
- Visit http://localhost:3001 -- redirects to /login (no cookie)
- API CORS headers include Access-Control-Allow-Credentials: true
</verification>

<success_criteria>
- API auth plugin accepts JWT from both cookies and Bearer headers
- Auth routes set HTTP-only cookies on login/signup/refresh
- Next.js project scaffold builds and runs
- Dark theme with gold accent matches mobile branding
- Sidebar navigation renders with correct icons and links
- Middleware redirects unauthenticated users to /login
- apiFetch wrapper sends credentials: include
- i18n initialized with 10 locale files
- MobileGate shows download prompt below 768px
</success_criteria>

<output>
After completion, create `.planning/phases/12-web-app-companion/12-01-SUMMARY.md`
</output>
