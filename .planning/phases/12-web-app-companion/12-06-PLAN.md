---
phase: 12-web-app-companion
plan: 06
type: execute
wave: 4
depends_on: ["12-03", "12-05"]
files_modified:
  - apps/web/src/app/(app)/market/page.tsx
  - apps/web/src/app/(app)/proposals/page.tsx
  - apps/web/src/stores/posts.ts
  - apps/web/src/stores/proposals.ts
  - apps/web/src/components/trading/PostList.tsx
  - apps/web/src/components/trading/PostCard.tsx
  - apps/web/src/components/trading/PostFilters.tsx
  - apps/web/src/components/trading/CreatePostModal.tsx
  - apps/web/src/components/trading/PostDetailModal.tsx
  - apps/web/src/components/trading/ProposalList.tsx
  - apps/web/src/components/trading/ProposalCard.tsx
  - apps/web/src/components/trading/ProposalDetailModal.tsx
  - apps/web/src/components/trading/CreateProposalModal.tsx
autonomous: true
requirements:
  - PLAT-02

must_haves:
  truths:
    - "User can browse Offering and Seeking trade posts with filters"
    - "User can create a new trade post (Offering or Seeking) from the web"
    - "User can view post details and send a proposal from a post"
    - "User can view their sent and received proposals"
    - "User can accept, reject, or counter a proposal"
  artifacts:
    - path: "apps/web/src/app/(app)/market/page.tsx"
      provides: "Marketplace page"
      contains: "PostList"
    - path: "apps/web/src/app/(app)/proposals/page.tsx"
      provides: "Proposals page"
      contains: "ProposalList"
    - path: "apps/web/src/stores/posts.ts"
      provides: "Post state management"
      contains: "usePostStore"
    - path: "apps/web/src/stores/proposals.ts"
      provides: "Proposal state management"
      contains: "useProposalStore"
  key_links:
    - from: "apps/web/src/stores/posts.ts"
      to: "/posts API"
      via: "apiFetch CRUD"
      pattern: "apiFetch.*posts"
    - from: "apps/web/src/stores/proposals.ts"
      to: "/proposals API"
      via: "apiFetch CRUD"
      pattern: "apiFetch.*proposals"
    - from: "apps/web/src/components/trading/PostDetailModal.tsx"
      to: "CreateProposalModal"
      via: "Send Proposal button"
      pattern: "CreateProposalModal"
---

<objective>
Build the marketplace (trade posts) and proposals pages with browsing, creation, and full proposal workflow (accept/reject/counter).

Purpose: Trading is the core value proposition. Users need to browse posts, create new posts, and manage proposals from the web.
Output: Working Market page with post listing/creation and Proposals page with full accept/reject/counter flow.
</objective>

<execution_context>
@C:/Users/romec/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/romec/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-web-app-companion/12-CONTEXT.md
@.planning/phases/12-web-app-companion/12-RESEARCH.md
@.planning/phases/12-web-app-companion/12-02-SUMMARY.md
@.planning/phases/12-web-app-companion/12-03-SUMMARY.md
@.planning/phases/12-web-app-companion/12-05-SUMMARY.md

@apps/web/src/lib/api.ts
@apps/web/src/components/ui/Modal.tsx
@apps/web/src/components/ui/Button.tsx
@apps/web/src/components/ui/Input.tsx
@packages/shared/src/schemas/post.ts
@packages/shared/src/schemas/proposal.ts

<interfaces>
API endpoints (from existing routes):
- GET /posts?type=offering|seeking&query=&setId=&rarity=&language=&page=&limit= -> { posts: MarketPost[], total }
- POST /posts -> create post { type, cards: [{ cardId, language }] }
- GET /posts/:id -> MarketPost with cards and poster info
- DELETE /posts/:id -> delete own post
- GET /proposals -> { sent: Proposal[], received: Proposal[] }
- POST /proposals -> create proposal { postId, offeredCards, requestedCards, message? }
- PUT /proposals/:id/accept -> accept
- PUT /proposals/:id/reject -> reject
- POST /proposals/:id/counter -> counter-offer { offeredCards, requestedCards, message? }

From packages/shared/src/schemas/post.ts (expected):
```typescript
export const createPostSchema = z.object({
  type: z.enum(['offering', 'seeking']),
  cards: z.array(z.object({ cardId: z.string(), language: z.string().optional() })),
});
```

From packages/shared/src/schemas/proposal.ts (expected):
```typescript
export const createProposalSchema = z.object({
  postId: z.string().optional(),
  matchId: z.string().optional(),
  offeredCards: z.array(...),
  requestedCards: z.array(...),
  message: z.string().optional(),
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Market page with post browsing, filtering, and creation</name>
  <files>
    apps/web/src/app/(app)/market/page.tsx,
    apps/web/src/stores/posts.ts,
    apps/web/src/components/trading/PostList.tsx,
    apps/web/src/components/trading/PostCard.tsx,
    apps/web/src/components/trading/PostFilters.tsx,
    apps/web/src/components/trading/CreatePostModal.tsx,
    apps/web/src/components/trading/PostDetailModal.tsx
  </files>
  <action>
    1. posts store (stores/posts.ts): "use client" Zustand store.
       - State: posts[], total, page, limit (20), filters (type, query, setId, rarity, language), selectedPost, loading, showCreateModal
       - Actions: fetchPosts(), setFilter(key, value), setPage(n), selectPost(post), createPost(type, cards[]), deletePost(id), toggleCreateModal()

    2. PostFilters.tsx: Type toggle (Offering | Seeking | All), search input, set/rarity/language filters. "Create Post" gold button.

    3. PostCard.tsx: Post type badge (Offering=green, Seeking=blue), card thumbnails row, poster name/reputation, time ago. Hover ring.

    4. PostList.tsx: grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-4. Loading skeletons, empty state, pagination.

    5. PostDetailModal.tsx: Full post detail in overlay. All cards with images, poster info, "Send Proposal" button (opens CreateProposalModal in Task 2). If own post: "Delete Post" button.

    6. CreatePostModal.tsx: Step 1 select type, Step 2 search/select cards, Step 3 review and submit. Uses card search via apiFetch.

    7. market/page.tsx: On mount fetch posts. PostFilters, PostList, modals. Title "Marketplace".
  </action>
  <verify>
    <automated>cd apps/web && pnpm build 2>&1 | tail -10 && pnpm test 2>&1 | tail -10</automated>
  </verify>
  <done>Market page shows trade posts in a grid. Filters narrow by type/search/set/rarity/language. Users can create posts via modal. Post detail opens in overlay.</done>
</task>

<task type="auto">
  <name>Task 2: Proposals page with full accept/reject/counter workflow</name>
  <files>
    apps/web/src/app/(app)/proposals/page.tsx,
    apps/web/src/stores/proposals.ts,
    apps/web/src/components/trading/ProposalList.tsx,
    apps/web/src/components/trading/ProposalCard.tsx,
    apps/web/src/components/trading/ProposalDetailModal.tsx,
    apps/web/src/components/trading/CreateProposalModal.tsx
  </files>
  <action>
    1. proposals store (stores/proposals.ts): "use client" Zustand store.
       - State: sent[], received[], selectedProposal, loading, activeTab ('received' | 'sent')
       - Actions: fetchProposals(), acceptProposal(id), rejectProposal(id), counterProposal(id, offeredCards, requestedCards, message?), createProposal(postId, offeredCards, requestedCards, message?), selectProposal(proposal), setActiveTab(tab)

    2. ProposalCard.tsx: Status badge (pending=yellow, accepted=green, rejected=red, countered=blue). Offered/requested cards rows. Partner info, fairness indicator, time ago.

    3. ProposalList.tsx: Tab toggle Received | Sent. Vertical list of ProposalCards. Loading skeletons, empty states per tab.

    4. ProposalDetailModal.tsx: Large card images in two columns (offered/requested). Status, fairness, partner info, message. Received: Accept/Reject/Counter buttons with confirmation. Counter form: card search and select, optional message. Sent: status display only.

    5. CreateProposalModal.tsx: Shows target post's cards. User selects cards to offer from collection. Optional message. Submit via proposals store.

    6. proposals/page.tsx: On mount fetch proposals. ProposalList with tabs, modals. Title "Trade Proposals" with received count badge.

    7. Wire PostDetailModal "Send Proposal" button to open CreateProposalModal.
  </action>
  <verify>
    <automated>cd apps/web && pnpm build 2>&1 | tail -10 && pnpm test 2>&1 | tail -10</automated>
  </verify>
  <done>Proposals page shows received/sent with tab toggle. Accept/reject/counter workflow works. Users can send proposals from post detail. Counter-offer flow works.</done>
</task>

</tasks>

<verification>
- `cd apps/web && pnpm build` succeeds
- `cd apps/web && pnpm test` passes
- Market page shows trade posts with filters
- Post detail shows Send Proposal button
- Proposals page shows received/sent tabs with actions
</verification>

<success_criteria>
- Trade posts display in responsive grid with type/search/filter controls
- Users can create Offering or Seeking posts with card selection
- Proposal workflow: create from post, accept, reject, counter all functional
- Proposal status displays correctly
- Fairness indicator shows on proposals
</success_criteria>

<output>
After completion, create `.planning/phases/12-web-app-companion/12-06-SUMMARY.md`
</output>
