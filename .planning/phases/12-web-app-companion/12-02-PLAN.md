---
phase: 12-web-app-companion
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - apps/web/src/app/(auth)/login/page.tsx
  - apps/web/src/app/(auth)/signup/page.tsx
  - apps/web/src/app/(auth)/layout.tsx
  - apps/web/src/components/auth/LoginForm.tsx
  - apps/web/src/components/auth/SignupForm.tsx
  - apps/web/src/components/auth/GoogleSignIn.tsx
  - apps/web/src/stores/auth.ts
autonomous: true
requirements:
  - PLAT-01

must_haves:
  truths:
    - "User can log in with email and password from the web app"
    - "User can sign up with email, password, and display name from the web app"
    - "User can sign in with Google OAuth from the web app"
    - "After login/signup, user is redirected to /cards with sidebar visible"
    - "Invalid credentials show error message"
  artifacts:
    - path: "apps/web/src/app/(auth)/login/page.tsx"
      provides: "Login page"
      contains: "LoginForm"
    - path: "apps/web/src/components/auth/LoginForm.tsx"
      provides: "Email/password login form"
      contains: "react-hook-form"
    - path: "apps/web/src/components/auth/GoogleSignIn.tsx"
      provides: "Google OAuth button"
      contains: "google"
  key_links:
    - from: "apps/web/src/components/auth/LoginForm.tsx"
      to: "/auth/login API"
      via: "apiFetch POST"
      pattern: "apiFetch.*auth/login"
    - from: "apps/web/src/stores/auth.ts"
      to: "/users/me API"
      via: "hydrate on app mount"
      pattern: "apiFetch.*users/me"
---

<objective>
Build the complete auth UI: login page with email/password form and Google OAuth, signup page with registration form, and wire to the auth Zustand store.

Purpose: Users must be able to authenticate before accessing any web app features. This is the entry point.
Output: Working login and signup pages that authenticate against the Fastify API via cookie auth.
</objective>

<execution_context>
@C:/Users/romec/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/romec/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-web-app-companion/12-CONTEXT.md
@.planning/phases/12-web-app-companion/12-RESEARCH.md
@.planning/phases/12-web-app-companion/12-01-SUMMARY.md

@apps/web/src/stores/auth.ts
@apps/web/src/lib/api.ts
@apps/web/src/app/(auth)/layout.tsx
@packages/shared/src/schemas/auth.ts

<interfaces>
From packages/shared/src/schemas/auth.ts (expected exports):
```typescript
export const signupSchema = z.object({
  email: z.string().email(),
  password: z.string().min(8),
  displayName: z.string().optional(),
});
export const loginSchema = z.object({
  email: z.string().email(),
  password: z.string(),
});
```

From apps/web/src/lib/api.ts (from Plan 01):
```typescript
export async function apiFetch<T>(path: string, options?: RequestInit): Promise<T>;
```

From apps/web/src/stores/auth.ts (from Plan 01):
```typescript
export const useAuthStore = create<AuthState>((set) => ({
  user, isLoggedIn, isHydrated,
  login: async (email, password) => { ... },
  signup: async (email, password, displayName) => { ... },
  logout: async () => { ... },
  hydrate: async () => { ... },
}));
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Login and signup forms with validation</name>
  <files>
    apps/web/src/app/(auth)/login/page.tsx,
    apps/web/src/app/(auth)/signup/page.tsx,
    apps/web/src/app/(auth)/layout.tsx,
    apps/web/src/components/auth/LoginForm.tsx,
    apps/web/src/components/auth/SignupForm.tsx
  </files>
  <action>
    1. Install react-hook-form and resolver if not already: `cd apps/web && pnpm add react-hook-form @hookform/resolvers`

    2. LoginForm.tsx: "use client" component.
       - Use react-hook-form with zodResolver and loginSchema from @pocket-trade-hub/shared
       - Fields: email (text input), password (password input)
       - Submit calls useAuthStore.login(email, password)
       - On success, redirect to /cards via router.push('/cards')
       - On error, show error message below form (red text on dark bg)
       - Styling: Tailwind dark theme. bg-surface rounded-lg p-6. Inputs with bg-bg border-border text-text. Gold submit button (bg-gold text-bg hover:bg-gold-hover).
       - "Don't have an account? Sign up" link to /signup
       - Loading state on submit button (disabled + spinner)

    3. SignupForm.tsx: "use client" component.
       - Use react-hook-form with zodResolver and signupSchema from @pocket-trade-hub/shared
       - Fields: display name (text), email (text), password (password), confirm password (password)
       - Confirm password validated client-side (match check via refine)
       - Submit calls useAuthStore.signup(email, password, displayName)
       - On success, redirect to /cards
       - Error display same pattern as login
       - "Already have an account? Log in" link to /login

    4. Update (auth)/login/page.tsx: Replace placeholder with LoginForm component. Page title "Log In".

    5. Update (auth)/signup/page.tsx: Replace placeholder with SignupForm component. Page title "Sign Up".

    6. Update (auth)/layout.tsx if needed: Centered layout with max-w-md, flex items-center justify-center min-h-screen. Show "Pocket Trade Hub" logo/title above form.

    7. Update auth store if needed: Ensure login/signup actions properly handle API response shape ({ user, accessToken, refreshToken }) -- tokens are in cookies now but user data comes in JSON body.
  </action>
  <verify>
    <automated>cd apps/web && pnpm build 2>&1 | tail -10</automated>
  </verify>
  <done>Login page renders with email/password form and validation. Signup page renders with registration form. Form submission calls API and redirects on success. Errors display inline.</done>
</task>

<task type="auto">
  <name>Task 2: Google OAuth sign-in for web</name>
  <files>
    apps/web/src/components/auth/GoogleSignIn.tsx,
    apps/web/src/app/(auth)/login/page.tsx,
    apps/web/src/app/(auth)/signup/page.tsx,
    apps/web/package.json
  </files>
  <action>
    1. Install @react-oauth/google: `cd apps/web && pnpm add @react-oauth/google`

    2. GoogleSignIn.tsx: "use client" component.
       - Wrap with GoogleOAuthProvider using NEXT_PUBLIC_GOOGLE_CLIENT_ID env var
       - Use GoogleLogin component from @react-oauth/google which renders the "Sign in with Google" button
       - On credential response, extract the credential (ID token) and POST to API /auth/oauth/google with { idToken: credential, provider: 'google' }
       - Use apiFetch for the POST call with credentials: include
       - On success (user returned), update auth store with user data and redirect to /cards
       - On error, show error message
       - Style the container to center the Google button, add a divider ("or") between email form and Google button

    3. Update login/page.tsx: Add GoogleSignIn component below LoginForm with an "or" divider (horizontal rule with text)

    4. Update signup/page.tsx: Add GoogleSignIn component below SignupForm with an "or" divider

    5. Note: The existing API /auth/oauth/google endpoint accepts { idToken, provider } and returns user + tokens. The web version gets the idToken from Google Identity Services instead of the native SDK, but the API endpoint is the same. The API will now also set cookies thanks to Plan 01 changes. If the oauth route does not set cookies, update it to match the login/signup cookie-setting pattern.

    6. Check apps/api/src/routes/oauth.ts -- if it does not set cookies like the auth routes were updated in Plan 01, add the same cookie-setting logic there.
  </action>
  <verify>
    <automated>cd apps/web && pnpm build 2>&1 | tail -10</automated>
  </verify>
  <done>Google Sign-In button renders on login and signup pages. Clicking it initiates Google OAuth flow. On success, user is authenticated and redirected to /cards with cookies set.</done>
</task>

</tasks>

<verification>
- `cd apps/web && pnpm build` succeeds
- Login page shows email/password form + Google Sign-In button
- Signup page shows registration form + Google Sign-In button
- Login with valid credentials redirects to /cards
- Invalid credentials show error message
- Sidebar visible after login
</verification>

<success_criteria>
- Email/password login works end-to-end (form -> API -> cookie set -> redirect to /cards)
- Email/password signup works end-to-end
- Google OAuth button renders and initiates flow
- Form validation shows errors for invalid input
- Auth store hydrates on app mount and persists auth state via cookies
</success_criteria>

<output>
After completion, create `.planning/phases/12-web-app-companion/12-02-SUMMARY.md`
</output>
