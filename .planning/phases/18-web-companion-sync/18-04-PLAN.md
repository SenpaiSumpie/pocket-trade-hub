---
phase: 18-web-companion-sync
plan: 04
type: execute
wave: 2
depends_on: [18-01, 18-02]
files_modified:
  - apps/web/src/app/(app)/meta/page.tsx
  - apps/web/src/app/(app)/tierlists/page.tsx
  - apps/web/src/app/(auth)/login/page.tsx
  - apps/web/src/app/(auth)/signup/page.tsx
  - apps/web/src/components/meta/DeckRankings.tsx
  - apps/web/src/components/meta/TierListBrowser.tsx
  - apps/web/src/components/meta/TierListCreator.tsx
  - apps/web/src/components/meta/TierListDetailModal.tsx
  - apps/web/src/components/meta/DeckDetailModal.tsx
  - apps/web/src/components/layout/Sidebar.tsx
  - apps/web/src/components/auth/LoginForm.tsx
  - apps/web/src/components/auth/SignupForm.tsx
  - apps/web/src/components/auth/GoogleSignIn.tsx
autonomous: true
requirements: [WEB-03]

must_haves:
  truths:
    - "Meta page shows skeleton while loading and empty state when no decks"
    - "Tier Lists page shows skeleton while loading and empty state when no tier lists"
    - "Auth pages use token-based colors with no hardcoded red/gray references"
    - "Sidebar uses token CSS vars for all colors including notification badge"
    - "All tier colors in meta components use token CSS vars"
  artifacts:
    - path: "apps/web/src/app/(app)/meta/page.tsx"
      provides: "Refreshed Meta page with skeleton + empty state"
      contains: "EmptyState"
    - path: "apps/web/src/app/(app)/tierlists/page.tsx"
      provides: "Refreshed Tier Lists page with skeleton + empty state"
      contains: "EmptyState"
    - path: "apps/web/src/components/layout/Sidebar.tsx"
      provides: "Sidebar with token-based colors"
      contains: "var(--color-error)"
  key_links:
    - from: "apps/web/src/app/(app)/meta/page.tsx"
      to: "apps/web/src/stores/meta.ts"
      via: "loading flag drives skeleton"
      pattern: "loading.*Skeleton"
    - from: "apps/web/src/components/layout/Sidebar.tsx"
      to: "apps/web/src/app/tokens.css"
      via: "token CSS vars for notification badge"
      pattern: "var\\(--color"
---

<objective>
Refresh Meta, Tier Lists, and Auth pages with token-based styling, skeleton loading, and empty states. Refresh Sidebar with token CSS vars. Complete the hardcoded color audit across all remaining web components.

Purpose: Completes WEB-03 by refreshing the remaining 4 pages + sidebar + auth pages, ensuring every web surface uses the shared token system.
Output: 13 files updated; all 8 web pages + sidebar fully refreshed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-web-companion-sync/18-CONTEXT.md
@.planning/phases/18-web-companion-sync/18-UI-SPEC.md
@.planning/phases/18-web-companion-sync/18-01-SUMMARY.md
@.planning/phases/18-web-companion-sync/18-02-SUMMARY.md

<interfaces>
<!-- Primitives available: -->
import { Skeleton } from '@/components/ui/Skeleton';    // variant: default|circle|text|card
import { EmptyState } from '@/components/ui/EmptyState'; // icon, title, subtitle, ctaLabel, onCta
import { Card } from '@/components/ui/Card';            // elevation, padding, onClick
import { Badge } from '@/components/ui/Badge';          // 8 variants including rarity-*

<!-- Stores: -->
useMetaStore: { decks, loading, fetchDecks, fetchSnapshot, snapshot, ... }
useTierListStore: { tierLists, loading, fetchTierLists, ... }

<!-- Tier color tokens (from tokens.css): -->
--color-tier-s: #e74c3c
--color-tier-a: #e67e22
--color-tier-b: #f0c040
--color-tier-c: #2ecc71
--color-tier-d: #3498db

<!-- UI-SPEC Sidebar refresh (before -> after): -->
Notification badge: bg-red-500 -> bg-[var(--color-error)]
Transition: transition-colors -> transition-colors duration-[var(--motion-duration-fast)]

<!-- Copywriting: -->
Meta empty: title="No meta data available" subtitle="Check back later as the community builds the meta."
TierLists empty: title="No tier lists yet" subtitle="Create the first tier list for this format." ctaLabel="Create Tier List"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh Meta and Tier Lists pages with skeleton, empty state, tier color token replacement</name>
  <files>apps/web/src/app/(app)/meta/page.tsx, apps/web/src/app/(app)/tierlists/page.tsx, apps/web/src/components/meta/DeckRankings.tsx, apps/web/src/components/meta/TierListBrowser.tsx, apps/web/src/components/meta/TierListCreator.tsx, apps/web/src/components/meta/TierListDetailModal.tsx, apps/web/src/components/meta/DeckDetailModal.tsx</files>
  <read_first>
    - apps/web/src/app/(app)/meta/page.tsx (current -- snapshot summary with hardcoded text-xs)
    - apps/web/src/app/(app)/tierlists/page.tsx (current -- no skeleton/empty state)
    - apps/web/src/components/meta/DeckRankings.tsx (has text-green-400, text-red-400 for trend/value)
    - apps/web/src/components/meta/TierListBrowser.tsx (has bg-blue-500/20, bg-green-500/20, bg-gray-500/20 tier colors)
    - apps/web/src/components/meta/TierListCreator.tsx (has border-blue-500, border-green-500, border-gray-500 tier colors, text-red-400)
    - apps/web/src/components/meta/TierListDetailModal.tsx (has tier color map with blue/green/gray, text-red-400 on delete)
    - apps/web/src/components/meta/DeckDetailModal.tsx (check for hardcoded colors)
    - apps/web/src/stores/meta.ts (loading flag)
    - apps/web/src/stores/tierlists.ts (loading flag)
  </read_first>
  <action>
**meta/page.tsx (D-21, D-22):**
- Import `Skeleton`, `EmptyState`, `Card` from `@/components/ui/*`, `BarChart3` from `lucide-react`
- Destructure `loading`, `decks` from `useMetaStore`
- Update heading: `text-[var(--font-size-heading)] font-bold text-[var(--color-on-surface)]`
- Wrap snapshot summary in `<Card>` component instead of raw `rounded-lg border border-border bg-surface`
- Update snapshot text to use token typography:
  - `text-xs font-semibold uppercase text-text-muted` -> `text-[var(--font-size-label)] font-semibold uppercase text-[var(--color-on-surface-muted)]`
  - `text-lg font-bold text-text` -> `text-[var(--font-size-subheading)] font-bold text-[var(--color-on-surface)]`
  - `text-sm text-gold` -> `text-[var(--font-size-label)] text-[var(--color-accent)]`
- Skeleton state: when `loading`, render 4 card skeletons
- Empty state: when `!loading && decks.length === 0`:
  `<EmptyState icon={BarChart3} title="No meta data available" subtitle="Check back later as the community builds the meta." />`

**tierlists/page.tsx (D-21, D-22):**
- Import `Skeleton`, `EmptyState` from `@/components/ui/*`, `List` from `lucide-react`
- Destructure `loading`, `tierLists` from `useTierListStore`
- Update heading: `text-[var(--font-size-heading)] font-bold text-[var(--color-on-surface)]`
- Skeleton state: when `loading`, render 3 card skeletons
- Empty state: when `!loading && tierLists.length === 0`:
  `<EmptyState icon={List} title="No tier lists yet" subtitle="Create the first tier list for this format." ctaLabel="Create Tier List" onCta={...trigger TierListCreator} />`

**DeckRankings.tsx tier/value color replacement (D-20):**
- Replace `text-green-400` -> `text-[var(--color-success)]` for positive trends/values
- Replace `text-red-400` -> `text-[var(--color-error)]` for negative trends/values

**TierListBrowser.tsx tier color replacement (D-20):**
- Replace tier color map with token vars. The map currently has S/A tiers using gold/red, and:
  - `B`: `'bg-blue-500/20 text-blue-400'` -> `'bg-[rgba(52,152,219,0.2)] text-[var(--color-tier-d)]'` (B tier uses --color-tier-b which is #f0c040 but the original uses blue, so map to --color-tier-b for correctness: `'bg-[color-mix(in_srgb,var(--color-tier-b)_20%,transparent)] text-[var(--color-tier-b)]'`)
  - Actually, check the tier tokens: `--color-tier-s: #e74c3c` (red), `--color-tier-a: #e67e22` (orange), `--color-tier-b: #f0c040` (gold), `--color-tier-c: #2ecc71` (green), `--color-tier-d: #3498db` (blue)
  - Replace B tier: `'bg-[color-mix(in_srgb,var(--color-tier-b)_20%,transparent)] text-[var(--color-tier-b)]'`
  - Replace C tier: `'bg-[color-mix(in_srgb,var(--color-tier-c)_20%,transparent)] text-[var(--color-tier-c)]'`
  - Replace D tier: `'bg-[color-mix(in_srgb,var(--color-tier-d)_20%,transparent)] text-[var(--color-tier-d)]'`
  - S tier: `'bg-[color-mix(in_srgb,var(--color-tier-s)_20%,transparent)] text-[var(--color-tier-s)]'`
  - A tier: `'bg-[color-mix(in_srgb,var(--color-tier-a)_20%,transparent)] text-[var(--color-tier-a)]'`

**TierListDetailModal.tsx tier color replacement (D-20):**
- Same tier color token replacement as TierListBrowser
- Replace `border-blue-500 bg-blue-500/10` -> `border-[var(--color-tier-b)] bg-[color-mix(in_srgb,var(--color-tier-b)_10%,transparent)]`
- Replace `border-green-500 bg-green-500/10` -> `border-[var(--color-tier-c)] bg-[color-mix(in_srgb,var(--color-tier-c)_10%,transparent)]`
- Replace `border-gray-500 bg-gray-500/10` -> `border-[var(--color-tier-d)] bg-[color-mix(in_srgb,var(--color-tier-d)_10%,transparent)]`
- Replace `text-red-400` on delete -> `text-[var(--color-error)]`
- Replace `border-red-500/50 ... hover:bg-red-500/10` -> `border-[var(--color-error)]/50 ... hover:bg-[color-mix(in_srgb,var(--color-error)_10%,transparent)]`

**TierListCreator.tsx tier color replacement (D-20):**
- Same tier color token map as TierListBrowser/DetailModal
- Replace `text-red-400` -> `text-[var(--color-error)]`

**DeckDetailModal.tsx (D-20):** Read file, replace any hardcoded color references with token vars.
  </action>
  <verify>
    <automated>grep -c "EmptyState" apps/web/src/app/\(app\)/meta/page.tsx && grep -c "EmptyState" apps/web/src/app/\(app\)/tierlists/page.tsx && grep -c "color-tier" apps/web/src/components/meta/TierListBrowser.tsx && ! grep -q "bg-gray-500" apps/web/src/components/meta/TierListBrowser.tsx && echo "no-gray-pass" && ! grep -q "text-red-400" apps/web/src/components/meta/DeckRankings.tsx && echo "no-red-pass"</automated>
  </verify>
  <acceptance_criteria>
    - meta/page.tsx imports and uses `Skeleton` and `EmptyState`
    - meta/page.tsx EmptyState has title "No meta data available"
    - meta/page.tsx snapshot summary uses `Card` component wrapper
    - tierlists/page.tsx imports and uses `Skeleton` and `EmptyState`
    - tierlists/page.tsx EmptyState has title "No tier lists yet" and ctaLabel "Create Tier List"
    - DeckRankings.tsx contains `var(--color-success)` instead of `text-green-400`
    - DeckRankings.tsx contains `var(--color-error)` instead of `text-red-400`
    - TierListBrowser.tsx contains `var(--color-tier-b)` and NO `bg-blue-500` or `bg-gray-500`
    - TierListDetailModal.tsx contains `var(--color-tier-c)` and NO `border-green-500`
    - TierListDetailModal.tsx contains `var(--color-error)` instead of `text-red-400`
    - TierListCreator.tsx contains `var(--color-tier-d)` and NO `border-gray-500`
  </acceptance_criteria>
  <done>Meta and Tier Lists pages fully refreshed with skeleton, empty states, and all tier/status colors replaced with token CSS vars</done>
</task>

<task type="auto">
  <name>Task 2: Refresh Sidebar, Auth pages, and final hardcoded color audit</name>
  <files>apps/web/src/components/layout/Sidebar.tsx, apps/web/src/app/(auth)/login/page.tsx, apps/web/src/app/(auth)/signup/page.tsx, apps/web/src/components/auth/LoginForm.tsx, apps/web/src/components/auth/SignupForm.tsx, apps/web/src/components/auth/GoogleSignIn.tsx</files>
  <read_first>
    - apps/web/src/components/layout/Sidebar.tsx (has bg-red-500 notification badge)
    - apps/web/src/app/(auth)/login/page.tsx (uses bg-surface, text-text -- already token-backed, but text-xl should use token var)
    - apps/web/src/app/(auth)/signup/page.tsx (same as login)
    - apps/web/src/components/auth/LoginForm.tsx (has text-red-400, bg-red-900/30 for errors)
    - apps/web/src/components/auth/SignupForm.tsx (has multiple text-red-400, bg-red-900/30 references)
    - apps/web/src/components/auth/GoogleSignIn.tsx (has text-red-400, bg-red-900/30)
    - .planning/phases/18-web-companion-sync/18-UI-SPEC.md (Sidebar visual refresh table)
  </read_first>
  <action>
**Sidebar.tsx refresh (D-23):**
- Replace `bg-red-500` notification badge -> `bg-[var(--color-error)]`
- Add motion transition: `transition-colors` -> `transition-colors duration-[var(--motion-duration-fast)]` on nav links
- Keep existing Lucide icons (per D-08), keep existing nav structure
- Verify `text-gold` / `bg-gold/10` references -- these are already token-backed via @theme aliases, so leave them. The UI-SPEC says `text-accent` but `text-gold` already maps to `--color-accent` via @theme. Leave as-is per Pitfall 1 in RESEARCH.md.

**login/page.tsx (D-21, D-22):**
- Update heading: `text-xl font-semibold text-text` -> `text-[var(--font-size-subheading)] font-bold text-[var(--color-on-surface)]`
- Replace `bg-surface` with `bg-[var(--color-surface)]` (or keep bg-surface as it is token-backed via @theme -- prefer keeping the Tailwind alias)
- Wrap in `Card` component if desired, but the existing `rounded-xl bg-surface p-6` is functionally equivalent. Keep existing structure for simplicity.

**signup/page.tsx (D-21, D-22):**
- Same updates as login: heading to `text-[var(--font-size-subheading)] font-bold text-[var(--color-on-surface)]`

**LoginForm.tsx hardcoded color replacement (D-20):**
- Replace `text-red-400` (error messages) -> `text-[var(--color-error)]`
- Replace `bg-red-900/30` (error banner) -> `bg-[color-mix(in_srgb,var(--color-error)_15%,transparent)]`

**SignupForm.tsx hardcoded color replacement (D-20):**
- Replace all `text-red-400` (5 instances) -> `text-[var(--color-error)]`
- Replace `bg-red-900/30` -> `bg-[color-mix(in_srgb,var(--color-error)_15%,transparent)]`

**GoogleSignIn.tsx hardcoded color replacement (D-20):**
- Replace `text-red-400` -> `text-[var(--color-error)]`
- Replace `bg-red-900/30` -> `bg-[color-mix(in_srgb,var(--color-error)_15%,transparent)]`

**Final audit sweep:** After all changes, run grep across `apps/web/src/` for remaining hardcoded Tailwind color classes: `text-gray-`, `text-zinc-`, `bg-gray-`, `bg-zinc-`, `border-gray-`, `border-zinc-`, `bg-red-`, `text-red-`, `bg-green-`, `text-green-`, `bg-blue-`, `text-blue-`. Log any remaining hits that were NOT in files modified by Plans 03 or 04. If any hits exist in unmodified component files, replace them as well.
  </action>
  <verify>
    <automated>grep -c "var(--color-error)" apps/web/src/components/layout/Sidebar.tsx && ! grep -q "bg-red-500" apps/web/src/components/layout/Sidebar.tsx && echo "sidebar-pass" && ! grep -q "text-red-400" apps/web/src/components/auth/LoginForm.tsx && echo "login-pass" && ! grep -q "text-red-400" apps/web/src/components/auth/SignupForm.tsx && echo "signup-pass" && ! grep -q "text-red-400" apps/web/src/components/auth/GoogleSignIn.tsx && echo "google-pass"</automated>
  </verify>
  <acceptance_criteria>
    - Sidebar.tsx notification badge uses `var(--color-error)` instead of `bg-red-500`
    - Sidebar.tsx nav links have `duration-[var(--motion-duration-fast)]` transition
    - login/page.tsx heading uses `var(--font-size-subheading)` token
    - signup/page.tsx heading uses `var(--font-size-subheading)` token
    - LoginForm.tsx contains NO `text-red-400` -- uses `var(--color-error)`
    - LoginForm.tsx contains NO `bg-red-900` -- uses `color-mix` with error var
    - SignupForm.tsx contains NO `text-red-400` (was 5 instances) -- all use `var(--color-error)`
    - SignupForm.tsx contains NO `bg-red-900` -- uses `color-mix` with error var
    - GoogleSignIn.tsx contains NO `text-red-400` or `bg-red-900` -- uses token vars
    - Final grep sweep: zero hits for `text-red-400`, `bg-red-500`, `bg-red-900`, `bg-gray-500`, `bg-zinc-500`, `text-gray-400`, `text-zinc-400` across apps/web/src/ (excluding node_modules)
  </acceptance_criteria>
  <done>Sidebar refreshed with token colors, auth pages refreshed, all hardcoded color audit complete with zero remaining non-token color references</done>
</task>

</tasks>

<verification>
- All 8 pages have token-based heading typography
- Meta and Tier Lists pages show skeleton and empty states
- Auth pages use token CSS vars for all error colors
- Sidebar notification badge uses `--color-error` token
- Final audit: `grep -rn "text-red-\|bg-red-\|text-green-\|bg-green-\|text-blue-\|bg-blue-\|text-gray-\|bg-gray-\|text-zinc-\|bg-zinc-\|border-gray-\|border-zinc-" apps/web/src/ --include="*.tsx"` returns zero results
</verification>

<success_criteria>
- Meta page: skeleton (4 items), EmptyState "No meta data available", snapshot in Card component
- Tier Lists page: skeleton (3 items), EmptyState "No tier lists yet" + "Create Tier List" CTA
- Login and Signup pages: token-based typography headings
- Auth forms: all error text uses `--color-error` token, no hardcoded red classes
- Sidebar: notification badge uses error token, transitions use motion token
- All tier colors in meta components use `--color-tier-*` CSS vars
- Hardcoded color audit complete: zero non-token Tailwind color classes remain in apps/web/src/
</success_criteria>

<output>
After completion, create `.planning/phases/18-web-companion-sync/18-04-SUMMARY.md`
</output>
