---
phase: 18-web-companion-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/layout.tsx
  - apps/web/src/app/globals.css
  - apps/web/src/components/ui/Button.tsx
  - apps/web/src/components/ui/Input.tsx
  - apps/web/src/components/ui/Skeleton.tsx
  - apps/web/src/components/ui/Modal.tsx
autonomous: true
requirements: [WEB-01, WEB-02]

must_haves:
  truths:
    - "Inter font loads on web via next/font with font-display swap"
    - "Token CSS pipeline runs before web build in Turborepo"
    - "Skeleton components show shimmer gradient sweep instead of pulse"
    - "Button supports icon prop and outline variant"
    - "Input supports textarea variant for multi-line"
    - "Modal animates in/out with scale+opacity CSS transition"
  artifacts:
    - path: "apps/web/src/app/layout.tsx"
      provides: "Inter font via next/font/google"
      contains: "next/font/google"
    - path: "apps/web/src/app/globals.css"
      provides: "Shimmer keyframe animation"
      contains: "@keyframes shimmer"
    - path: "apps/web/src/components/ui/Button.tsx"
      provides: "Upgraded button with icon + outline"
      exports: ["Button"]
    - path: "apps/web/src/components/ui/Input.tsx"
      provides: "Upgraded input with textarea variant"
      exports: ["Input"]
    - path: "apps/web/src/components/ui/Skeleton.tsx"
      provides: "Shimmer skeleton with shaped variants"
      exports: ["Skeleton"]
    - path: "apps/web/src/components/ui/Modal.tsx"
      provides: "Modal with CSS entrance/exit transitions"
      exports: ["Modal"]
  key_links:
    - from: "apps/web/src/app/layout.tsx"
      to: "next/font/google"
      via: "Inter font import and className on html element"
      pattern: "inter\\.className"
    - from: "apps/web/src/components/ui/Skeleton.tsx"
      to: "apps/web/src/app/globals.css"
      via: "shimmer CSS class"
      pattern: "shimmer"
---

<objective>
Validate the token pipeline, load Inter font via next/font, add shimmer animation to globals.css, and upgrade the 4 existing web UI primitives (Button, Input, Skeleton, Modal) to consume token CSS vars with new features.

Purpose: Establishes the visual foundation -- font, shimmer animation, and upgraded primitives -- that all page refresh work depends on.
Output: 6 files updated; web primitives ready for page-level consumption.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-web-companion-sync/18-CONTEXT.md
@.planning/phases/18-web-companion-sync/18-RESEARCH.md
@.planning/phases/18-web-companion-sync/18-UI-SPEC.md

<interfaces>
<!-- Token CSS vars available in apps/web/src/app/tokens.css (68 vars in @theme block) -->
<!-- Key vars used by primitives: -->
--color-background: #0c0c18
--color-surface: #1a1a2e
--color-surface-light: #252540
--color-surface-hover: #2a2a45
--color-on-surface: #ffffff
--color-on-surface-secondary: #a0a0b8
--color-on-surface-muted: #6c6c80
--color-accent: #f0c040
--color-accent-dark: #c49a20
--color-error: #e74c3c
--color-border: #2a2a45
--border-radius-sm: 0.375rem
--border-radius-md: 0.75rem
--border-radius-lg: 1rem
--elevation-none: none
--elevation-low: 0px 1px 2px rgba(0, 0, 0, 0.2)
--elevation-medium: 0px 2px 4px rgba(0, 0, 0, 0.25)
--elevation-high: 0px 4px 8px rgba(0, 0, 0, 0.3)
--motion-duration-fast: 200ms
--motion-duration-normal: 300ms
--motion-easing-standard: cubic-bezier(0.2, 0, 0, 1)
--font-size-label: 0.875rem
--font-size-body: 1rem
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inter font via next/font + shimmer keyframes + token pipeline validation</name>
  <files>apps/web/src/app/layout.tsx, apps/web/src/app/globals.css, turbo.json</files>
  <read_first>
    - apps/web/src/app/layout.tsx (current root layout -- no next/font import)
    - apps/web/src/app/globals.css (currently only 2 lines: @import tailwindcss + tokens)
    - turbo.json (validate generate-tokens task wiring)
    - apps/web/src/app/tokens.css (confirm 68 vars present)
  </read_first>
  <action>
1. **Validate token pipeline (D-19):** Read `turbo.json` and confirm `generate-tokens` task exists with `outputs: ["apps/web/src/app/tokens.css"]` and `build` task has `dependsOn` including `generate-tokens`. Read `tokens.css` and confirm it has 68 CSS custom properties in a `@theme` block. No changes needed to turbo.json -- it is already correctly wired.

2. **Add Inter font via next/font (D-07):** Update `apps/web/src/app/layout.tsx`:
   - Add import: `import { Inter } from 'next/font/google';`
   - Initialize: `const inter = Inter({ subsets: ['latin'], display: 'swap' });`
   - Apply to html element: `<html lang="en" className={`dark ${inter.className}`}>`
   - Keep existing body className `bg-bg text-text min-h-screen`
   - Keep existing metadata export unchanged

3. **Add shimmer keyframes to globals.css (D-12):** Append after existing `@import` lines in `apps/web/src/app/globals.css`:

```css
@keyframes shimmer {
  0%   { background-position: -200% center; }
  100% { background-position: 200% center; }
}

.shimmer {
  background: linear-gradient(
    90deg,
    var(--color-surface-hover) 25%,
    var(--color-surface-light) 50%,
    var(--color-surface-hover) 75%
  );
  background-size: 200% 100%;
  animation: shimmer 1.5s ease-in-out infinite;
}
```
  </action>
  <verify>
    <automated>grep -c "next/font/google" apps/web/src/app/layout.tsx && grep -c "@keyframes shimmer" apps/web/src/app/globals.css && grep -c "generate-tokens" turbo.json</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/app/layout.tsx contains `import { Inter } from 'next/font/google'`
    - apps/web/src/app/layout.tsx contains `inter.className`
    - apps/web/src/app/layout.tsx contains `display: 'swap'`
    - apps/web/src/app/globals.css contains `@keyframes shimmer`
    - apps/web/src/app/globals.css contains `.shimmer`
    - apps/web/src/app/globals.css contains `background-size: 200% 100%`
    - apps/web/src/app/globals.css contains `animation: shimmer 1.5s ease-in-out infinite`
    - turbo.json contains `generate-tokens` in tasks
    - apps/web/src/app/tokens.css contains `--color-accent: #f0c040`
  </acceptance_criteria>
  <done>Inter font loads via next/font with swap display, shimmer keyframe defined globally, token pipeline validated as wired</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade Button, Input, Skeleton, Modal to consume token CSS vars</name>
  <files>apps/web/src/components/ui/Button.tsx, apps/web/src/components/ui/Input.tsx, apps/web/src/components/ui/Skeleton.tsx, apps/web/src/components/ui/Modal.tsx</files>
  <read_first>
    - apps/web/src/components/ui/Button.tsx (current: 3 variants primary/secondary/ghost, no icon prop, no outline)
    - apps/web/src/components/ui/Input.tsx (current: input only, no textarea, uses border-red-500 hardcoded)
    - apps/web/src/components/ui/Skeleton.tsx (current: animate-pulse, no variants)
    - apps/web/src/components/ui/Modal.tsx (current: immediate show/hide, no transition)
    - apps/mobile/src/components/ui/Badge.tsx (reference for mobile API patterns)
    - .planning/phases/18-web-companion-sync/18-UI-SPEC.md (upgrade contracts)
  </read_first>
  <action>
**Button.tsx upgrade (D-10):**
- Add `icon` prop: `icon?: LucideIcon` (import `type LucideIcon` from `lucide-react`)
- Add `iconPosition` prop: `'left' | 'right'`, default `'left'`
- Add `outline` variant to `Variant` type: `'primary' | 'secondary' | 'ghost' | 'outline'`
- Replace variant classes with token CSS var references:
  - `primary`: `'bg-[var(--color-accent)] text-[var(--color-background)] hover:bg-[var(--color-accent-dark)]'`
  - `secondary`: `'bg-[var(--color-surface)] border border-[var(--color-border)] text-[var(--color-on-surface)] hover:bg-[var(--color-surface-hover)]'`
  - `ghost`: `'bg-transparent text-[var(--color-on-surface)] hover:bg-[var(--color-surface-hover)]'`
  - `outline`: `'bg-transparent border border-[var(--color-accent)] text-[var(--color-accent)] hover:bg-[color-mix(in_srgb,var(--color-accent)_10%,transparent)]'`
- Keep `forwardRef`, `loading` with Loader2 spinner, `disabled` styling
- Render icon: `{Icon && iconPosition === 'left' && <Icon size={16} />}` before children, and right-side after children
- Add `transition-all duration-[var(--motion-duration-fast)]` to base classes
- Focus ring: `focus:ring-2 focus:ring-[var(--color-accent)]/50`

**Input.tsx upgrade (D-11):**
- Add `textarea` prop: `textarea?: boolean` to InputProps
- When `textarea` is true, render `<textarea>` instead of `<input>` with same styling + `min-h-[100px] resize-y`
- Use union type for ref: keep `forwardRef<HTMLInputElement, InputProps>` for input, add separate export or conditional rendering
- Replace hardcoded colors:
  - `border-red-500` -> `border-[var(--color-error)]`
  - `text-red-400` -> `text-[var(--color-error)]`
  - `bg-bg` -> `bg-[var(--color-background)]`
  - `text-text-muted` (on label) -> keep as-is (already token-backed via @theme)
  - `text-text` -> keep as-is (already token-backed)
  - `border-border` -> keep as-is (already token-backed)
- Focus ring: `focus:ring-2 focus:ring-[var(--color-accent)]/50`
- Add `transition-colors duration-[var(--motion-duration-fast)]` to input/textarea

**Skeleton.tsx upgrade (D-12):**
- Replace `animate-pulse bg-surface-hover` with `shimmer` class (defined in globals.css)
- Add `variant` prop: `'default' | 'circle' | 'text' | 'card'`
- Shape classes:
  - `default`: no additional class
  - `circle`: `rounded-full`
  - `text`: `h-[14px] rounded-[4px]`
  - `card`: `rounded-[var(--border-radius-md)]`
- Keep `aria-hidden="true"`
- Export: `export function Skeleton({ className = '', variant = 'default' }: { className?: string; variant?: 'default' | 'circle' | 'text' | 'card' })`

**Modal.tsx upgrade (D-13):**
- Replace immediate mount/unmount with `data-open` attribute CSS transition pattern
- Keep portal mounted when `open` was recently true (use internal `mounted` state + timeout)
- Add `useState` for `mounted`: set to true when `open` becomes true, set to false after 300ms delay when `open` becomes false
- Render portal when `mounted || open` is true
- Add data-open attribute: `data-open={open}` on both backdrop and content divs
- Backdrop transitions: `transition-opacity duration-[var(--motion-duration-normal)] data-[open=false]:opacity-0 data-[open=true]:opacity-100`
- Content div transitions: `transition-all duration-[var(--motion-duration-normal)] data-[open=false]:opacity-0 data-[open=false]:scale-95 data-[open=true]:opacity-100 data-[open=true]:scale-100`
- Replace hardcoded `bg-surface` with `bg-[var(--color-surface)]` on content div
- Keep existing: `createPortal`, Escape key handler, backdrop click, X button, body overflow lock
  </action>
  <verify>
    <automated>grep -c "outline" apps/web/src/components/ui/Button.tsx && grep -c "LucideIcon\|LucideProps" apps/web/src/components/ui/Button.tsx && grep -c "textarea" apps/web/src/components/ui/Input.tsx && grep -c "shimmer" apps/web/src/components/ui/Skeleton.tsx && grep -c "data-open" apps/web/src/components/ui/Modal.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Button.tsx contains `icon?: LucideIcon` or equivalent Lucide type import
    - Button.tsx contains variant `outline` in variantClasses record
    - Button.tsx contains `var(--color-accent)` in primary variant class
    - Button.tsx contains `var(--color-accent-dark)` in primary hover
    - Input.tsx contains `textarea` prop in interface
    - Input.tsx renders `<textarea` conditionally
    - Input.tsx contains `var(--color-error)` replacing hardcoded red classes
    - Skeleton.tsx contains `shimmer` className (not `animate-pulse`)
    - Skeleton.tsx contains `variant` prop with `circle`, `text`, `card` options
    - Modal.tsx contains `data-open` attribute
    - Modal.tsx contains `scale-95` in transition classes
    - Modal.tsx contains `var(--motion-duration-normal)` or `--motion-duration-normal`
  </acceptance_criteria>
  <done>All 4 existing primitives upgraded: Button has icon+outline, Input has textarea, Skeleton has shimmer+shapes, Modal has CSS transitions</done>
</task>

</tasks>

<verification>
- `grep -r "next/font/google" apps/web/src/app/layout.tsx` returns match
- `grep -r "@keyframes shimmer" apps/web/src/app/globals.css` returns match
- `grep -r "LucideIcon" apps/web/src/components/ui/Button.tsx` returns match
- `grep -r "shimmer" apps/web/src/components/ui/Skeleton.tsx` returns match
- `grep -r "data-open" apps/web/src/components/ui/Modal.tsx` returns match
- No `animate-pulse` remains in Skeleton.tsx
- No `border-red-500` or `text-red-400` remains in Input.tsx
</verification>

<success_criteria>
- Inter font loads on web via next/font with display swap
- Token pipeline is validated (turbo.json wiring confirmed, tokens.css has 68 vars)
- Shimmer keyframe animation defined in globals.css
- Button supports 4 variants (primary, secondary, ghost, outline) + icon prop + token CSS vars
- Input supports textarea variant + token CSS vars for error states
- Skeleton uses shimmer class with 4 shape variants (default, circle, text, card)
- Modal has CSS entrance/exit transitions via data-open attribute pattern
</success_criteria>

<output>
After completion, create `.planning/phases/18-web-companion-sync/18-01-SUMMARY.md`
</output>
