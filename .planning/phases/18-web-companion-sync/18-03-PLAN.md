---
phase: 18-web-companion-sync
plan: 03
type: execute
wave: 2
depends_on: [18-01, 18-02]
files_modified:
  - apps/web/src/app/(app)/cards/page.tsx
  - apps/web/src/app/(app)/collection/page.tsx
  - apps/web/src/app/(app)/market/page.tsx
  - apps/web/src/app/(app)/proposals/page.tsx
  - apps/web/src/components/cards/CardGrid.tsx
  - apps/web/src/components/cards/CardThumbnail.tsx
  - apps/web/src/components/cards/CardDetailModal.tsx
  - apps/web/src/components/collection/CollectionGrid.tsx
  - apps/web/src/components/trading/PostCard.tsx
  - apps/web/src/components/trading/PostList.tsx
  - apps/web/src/components/trading/PostDetailModal.tsx
  - apps/web/src/components/trading/CreatePostModal.tsx
  - apps/web/src/components/trading/ProposalCard.tsx
  - apps/web/src/components/trading/ProposalList.tsx
  - apps/web/src/components/trading/ProposalDetailModal.tsx
  - apps/web/src/components/trading/CreateProposalModal.tsx
autonomous: true
requirements: [WEB-03]

must_haves:
  truths:
    - "Cards page shows skeleton shimmer while loading and empty state when no cards"
    - "Collection page shows skeleton shimmer while loading and empty state when empty"
    - "Market page shows skeleton shimmer while loading, empty state when no posts, and gradient accent on premium posts"
    - "Proposals page shows skeleton shimmer while loading and empty state when no proposals"
    - "All hardcoded color references in Cards, Collection, Market, and Proposals components are replaced with token CSS vars"
  artifacts:
    - path: "apps/web/src/app/(app)/cards/page.tsx"
      provides: "Refreshed Cards page with skeleton + empty state"
      contains: "Skeleton"
    - path: "apps/web/src/app/(app)/collection/page.tsx"
      provides: "Refreshed Collection page with skeleton + empty state"
      contains: "EmptyState"
    - path: "apps/web/src/app/(app)/market/page.tsx"
      provides: "Refreshed Market page with skeleton + empty state"
      contains: "EmptyState"
    - path: "apps/web/src/app/(app)/proposals/page.tsx"
      provides: "Refreshed Proposals page with skeleton + empty state + Badge"
      contains: "Badge"
  key_links:
    - from: "apps/web/src/app/(app)/cards/page.tsx"
      to: "apps/web/src/stores/cards.ts"
      via: "loading flag drives skeleton rendering"
      pattern: "loading.*Skeleton"
    - from: "apps/web/src/app/(app)/market/page.tsx"
      to: "apps/web/src/components/ui/EmptyState.tsx"
      via: "EmptyState component for empty list"
      pattern: "EmptyState"
---

<objective>
Refresh the Cards, Collection, Market, and Proposals pages with token-based styling, skeleton loading states, empty states, and hardcoded color replacement across all their sub-components.

Purpose: Delivers WEB-03 for the 4 highest-traffic pages, making them visually consistent with the mobile refresh.
Output: 4 page files + 12 sub-component files updated with token CSS vars, skeleton states, and empty states.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-web-companion-sync/18-CONTEXT.md
@.planning/phases/18-web-companion-sync/18-UI-SPEC.md
@.planning/phases/18-web-companion-sync/18-01-SUMMARY.md
@.planning/phases/18-web-companion-sync/18-02-SUMMARY.md

<interfaces>
<!-- New primitives available from Plans 01+02: -->
import { Button } from '@/components/ui/Button';       // icon, outline variant
import { Card } from '@/components/ui/Card';            // elevation, padding, onClick
import { Badge } from '@/components/ui/Badge';          // 8 variants
import { Skeleton } from '@/components/ui/Skeleton';    // shimmer, variant: default|circle|text|card
import { EmptyState } from '@/components/ui/EmptyState'; // icon, title, subtitle, ctaLabel, onCta
import { Modal } from '@/components/ui/Modal';          // CSS transitions

<!-- Stores all expose loading: boolean -->
useCardStore: { cards, loading, fetchCards, fetchSets, ... }
useCollectionStore: { items, loading, fetchCollection, fetchProgress, progress, ... }
usePostStore: { posts, loading, fetchPosts, selectedPost, ... }
useProposalStore: { sent, received, loading, fetchProposals, ... }

<!-- Token CSS vars available (via Tailwind @theme or direct var()): -->
--color-error: #e74c3c;  --color-success: #2ecc71;  --color-warning: #e67e22
--color-accent: #f0c040; --color-rarity-diamond: #7ec8e3; --color-rarity-star: #f0c040; --color-rarity-crown: #e8b4f8
--color-surface: #1a1a2e; --color-surface-light: #252540; --color-border: #2a2a45
--color-on-surface: #ffffff; --color-on-surface-secondary: #a0a0b8; --color-on-surface-muted: #6c6c80
--font-size-heading: 1.75rem; --font-size-subheading: 1.25rem
--color-tier-s: #e74c3c; --color-tier-a: #e67e22; --color-tier-b: #f0c040; --color-tier-c: #2ecc71; --color-tier-d: #3498db

<!-- Copywriting from UI-SPEC: -->
Empty state cards: title="No cards found" subtitle="Try adjusting your filters or search terms."
Empty state collection: title="Your collection is empty" subtitle="Browse the card database to start building your collection." ctaLabel="Browse Cards"
Empty state market: title="No posts yet" subtitle="Be the first to post a trade offer." ctaLabel="Create Post"
Empty state proposals: title="No active proposals" subtitle="Browse the marketplace to find cards you want." ctaLabel="Go to Market"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh Cards and Collection pages with skeleton, empty state, token colors</name>
  <files>apps/web/src/app/(app)/cards/page.tsx, apps/web/src/app/(app)/collection/page.tsx, apps/web/src/components/cards/CardGrid.tsx, apps/web/src/components/cards/CardThumbnail.tsx, apps/web/src/components/cards/CardDetailModal.tsx, apps/web/src/components/collection/CollectionGrid.tsx</files>
  <read_first>
    - apps/web/src/app/(app)/cards/page.tsx (current page -- no skeleton/empty state)
    - apps/web/src/app/(app)/collection/page.tsx (current page -- no skeleton/empty state)
    - apps/web/src/components/cards/CardGrid.tsx (grid component -- check for hardcoded colors)
    - apps/web/src/components/cards/CardThumbnail.tsx (has hardcoded bg-zinc-400, bg-zinc-300, bg-blue-400, bg-zinc-500 rarity colors)
    - apps/web/src/components/cards/CardDetailModal.tsx (has text-red-400 on delete)
    - apps/web/src/components/collection/CollectionGrid.tsx (has text-red-400 on remove)
    - apps/web/src/stores/cards.ts (loading flag location)
    - apps/web/src/stores/collection.ts (loading flag location)
  </read_first>
  <action>
**cards/page.tsx (D-21, D-22):**
- Import `Skeleton`, `EmptyState` from `@/components/ui/*` and `LayoutGrid` from `lucide-react`
- Destructure `loading` from `useCardStore`
- Add heading with token typography: `<h1 className="mb-6 text-[var(--font-size-heading)] font-bold text-[var(--color-on-surface)]">Cards</h1>`
- Add skeleton state: when `loading` is true, render a grid of 12 card skeletons:
  ```
  <div className="grid grid-cols-2 gap-4 sm:grid-cols-3 lg:grid-cols-4 xl:grid-cols-5">
    {Array.from({ length: 12 }).map((_, i) => (
      <Skeleton key={i} variant="card" className="h-64" />
    ))}
  </div>
  ```
- Add empty state: when `!loading && cards.length === 0`, render:
  `<EmptyState icon={LayoutGrid} title="No cards found" subtitle="Try adjusting your filters or search terms." />`
- Keep existing CardFilters, CardGrid, CardDetailModal rendering when cards exist

**collection/page.tsx (D-21, D-22):**
- Import `Skeleton`, `EmptyState` from `@/components/ui/*`, `Card` from `@/components/ui/Card`, `Library` from `lucide-react`
- Destructure `loading`, `items` from `useCollectionStore`
- Update heading: `text-[var(--font-size-heading)] font-bold text-[var(--color-on-surface)]`
- Add skeleton state: when `loading`, render 8 card skeletons in grid
- Add empty state: when `!loading && items.length === 0`:
  `<EmptyState icon={Library} title="Your collection is empty" subtitle="Browse the card database to start building your collection." ctaLabel="Browse Cards" onCta={() => router.push('/cards')} />`
  (Import `useRouter` from `next/navigation`)
- Wrap set progress items in `<Card padding="sm">` component instead of raw div with `border border-border bg-surface`
- Replace `text-2xl` heading with `text-[var(--font-size-heading)]`
- Replace `text-sm font-semibold text-text-muted` subheading with `text-[var(--font-size-label)] font-semibold text-[var(--color-on-surface-muted)]`

**CardThumbnail.tsx hardcoded color replacement (D-20):**
- Replace rarity color map with token vars:
  - `diamond1`: `'bg-[var(--color-rarity-diamond)]'`
  - `diamond2`: `'bg-[var(--color-rarity-diamond)] opacity-70'`
  - `diamond3`: `'bg-[var(--color-rarity-diamond)] opacity-50'`
  - `star1/star2/star3`: use `--color-rarity-star` with varying opacity
  - `crown`: use `--color-rarity-crown`
  - Fallback: `'bg-[var(--color-surface-light)]'` instead of `bg-zinc-500`

**CardDetailModal.tsx hardcoded color replacement (D-20):**
- Replace `text-red-400 hover:text-red-300` with `text-[var(--color-error)] hover:opacity-80`

**CollectionGrid.tsx hardcoded color replacement (D-20):**
- Replace `text-red-400 hover:text-red-300` with `text-[var(--color-error)] hover:opacity-80`
  </action>
  <verify>
    <automated>grep -c "EmptyState" apps/web/src/app/\(app\)/cards/page.tsx && grep -c "Skeleton" apps/web/src/app/\(app\)/cards/page.tsx && grep -c "EmptyState" apps/web/src/app/\(app\)/collection/page.tsx && grep -c "color-rarity-diamond" apps/web/src/components/cards/CardThumbnail.tsx && ! grep -q "bg-zinc" apps/web/src/components/cards/CardThumbnail.tsx && echo "no-zinc-pass"</automated>
  </verify>
  <acceptance_criteria>
    - cards/page.tsx imports and uses `Skeleton` component
    - cards/page.tsx imports and uses `EmptyState` with title "No cards found"
    - cards/page.tsx destructures `loading` from useCardStore
    - collection/page.tsx imports and uses `Skeleton` and `EmptyState`
    - collection/page.tsx EmptyState has ctaLabel "Browse Cards"
    - collection/page.tsx uses `Card` component for set progress items
    - CardThumbnail.tsx contains `color-rarity-diamond` instead of `bg-zinc-400`
    - CardThumbnail.tsx contains NO `bg-zinc` references
    - CardDetailModal.tsx contains `var(--color-error)` instead of `text-red-400`
    - CollectionGrid.tsx contains `var(--color-error)` instead of `text-red-400`
    - All page headings use `var(--font-size-heading)` or `var(--color-on-surface)`
  </acceptance_criteria>
  <done>Cards and Collection pages fully refreshed with skeleton loading, empty states, and all hardcoded colors replaced with token CSS vars</done>
</task>

<task type="auto">
  <name>Task 2: Refresh Market and Proposals pages with skeleton, empty state, token colors, gradient accents</name>
  <files>apps/web/src/app/(app)/market/page.tsx, apps/web/src/app/(app)/proposals/page.tsx, apps/web/src/components/trading/PostCard.tsx, apps/web/src/components/trading/PostList.tsx, apps/web/src/components/trading/PostDetailModal.tsx, apps/web/src/components/trading/CreatePostModal.tsx, apps/web/src/components/trading/ProposalCard.tsx, apps/web/src/components/trading/ProposalList.tsx, apps/web/src/components/trading/ProposalDetailModal.tsx, apps/web/src/components/trading/CreateProposalModal.tsx</files>
  <read_first>
    - apps/web/src/app/(app)/market/page.tsx (current page)
    - apps/web/src/app/(app)/proposals/page.tsx (current page -- has hardcoded bg-gold/20 badge)
    - apps/web/src/components/trading/PostCard.tsx (has bg-green-500/20, bg-blue-500/20)
    - apps/web/src/components/trading/PostDetailModal.tsx (has bg-green-500/20, text-red-400)
    - apps/web/src/components/trading/CreatePostModal.tsx (has border-green-500, bg-green-500/10, border-blue-500, bg-blue-500/10)
    - apps/web/src/components/trading/ProposalCard.tsx (has bg-green-500/20, bg-red-500/20, bg-blue-500/20, bg-zinc-500/20)
    - apps/web/src/components/trading/ProposalDetailModal.tsx (has extensive hardcoded colors)
    - apps/web/src/components/trading/CreateProposalModal.tsx (check for hardcoded colors)
    - apps/web/src/stores/posts.ts (loading flag)
    - apps/web/src/stores/proposals.ts (loading flag)
  </read_first>
  <action>
**market/page.tsx (D-21, D-22, D-05):**
- Import `Skeleton`, `EmptyState` from `@/components/ui/*`, `Store` from `lucide-react`
- Destructure `loading`, `posts` from `usePostStore`
- Update heading: `text-[var(--font-size-heading)] font-bold text-[var(--color-on-surface)]`
- Skeleton state: when `loading`, render 6 card skeletons in a vertical list: `<div className="flex flex-col gap-4">{Array.from({length:6}).map((_,i) => <Skeleton key={i} variant="card" className="h-32" />)}</div>`
- Empty state: when `!loading && posts.length === 0`:
  `<EmptyState icon={Store} title="No posts yet" subtitle="Be the first to post a trade offer." ctaLabel="Create Post" onCta={...open create post modal} />`
- Keep existing PostFilters, PostList, PostDetailModal, CreatePostModal, CreateProposalModal

**proposals/page.tsx (D-21, D-22):**
- Import `Skeleton`, `EmptyState`, `Badge` from `@/components/ui/*`, `ArrowLeftRight` from `lucide-react`
- Destructure `loading`, `sent`, `received` from `useProposalStore`
- Update heading: `text-[var(--font-size-heading)] font-bold text-[var(--color-on-surface)]`
- Replace hardcoded pending count badge `<span className="rounded-full bg-gold/20 ...">` with `<Badge variant="premium">{pendingCount}</Badge>`
- Skeleton state: when `loading`, render 4 card skeletons
- Empty state: when `!loading && sent.length === 0 && received.length === 0`:
  `<EmptyState icon={ArrowLeftRight} title="No active proposals" subtitle="Browse the marketplace to find cards you want." ctaLabel="Go to Market" onCta={() => router.push('/market')} />`
  (Import `useRouter` from `next/navigation`)

**PostCard.tsx hardcoded color replacement (D-20):**
- Replace offering/seeking color map:
  - `offering`: `'bg-[rgba(46,204,113,0.2)] text-[var(--color-success)]'` (was `bg-green-500/20 text-green-400`)
  - `seeking`: `'bg-[rgba(52,152,219,0.2)] text-[#3498db]'` (was `bg-blue-500/20 text-blue-400`)
- For premium posts (D-05): add a left border accent `border-l-2 border-l-[var(--color-accent)]` to the card wrapper when post is premium

**PostDetailModal.tsx hardcoded colors (D-20):**
- Replace `bg-green-500/20 text-green-400` -> `bg-[rgba(46,204,113,0.2)] text-[var(--color-success)]`
- Replace `bg-blue-500/20 text-blue-400` -> `bg-[rgba(52,152,219,0.2)] text-[#3498db]`
- Replace `text-red-400 hover:text-red-300` -> `text-[var(--color-error)] hover:opacity-80`

**CreatePostModal.tsx hardcoded colors (D-20):**
- Replace `border-green-500 bg-green-500/10` -> `border-[var(--color-success)] bg-[rgba(46,204,113,0.1)]`
- Replace `text-green-400` -> `text-[var(--color-success)]`
- Replace `border-blue-500 bg-blue-500/10` -> `border-[#3498db] bg-[rgba(52,152,219,0.1)]`
- Replace `bg-green-500/20 text-green-400` -> `bg-[rgba(46,204,113,0.2)] text-[var(--color-success)]`
- Replace `bg-blue-500/20 text-blue-400` -> `bg-[rgba(52,152,219,0.2)] text-[#3498db]`

**ProposalCard.tsx hardcoded colors (D-20):**
- Replace status color map:
  - `accepted`: `'bg-[rgba(46,204,113,0.2)] text-[var(--color-success)]'`
  - `rejected`: `'bg-[rgba(231,76,60,0.2)] text-[var(--color-error)]'`
  - `countered`: `'bg-[rgba(52,152,219,0.2)] text-[#3498db]'`
  - `completed`: `'bg-[rgba(46,204,113,0.2)] text-[var(--color-success)]'`
  - `cancelled`: `'bg-[var(--color-surface-light)] text-[var(--color-on-surface-muted)]'` (was bg-zinc-500/20)
- Replace fairness score colors:
  - `text-green-400` -> `text-[var(--color-success)]`
  - `text-red-400` -> `text-[var(--color-error)]`

**ProposalDetailModal.tsx hardcoded colors (D-20):**
- Same status color map replacement as ProposalCard
- Fairness score: `text-green-400` -> `text-[var(--color-success)]`, `text-red-400` -> `text-[var(--color-error)]`
- Delete/reject button: `text-red-400` -> `text-[var(--color-error)]`

**CreateProposalModal.tsx (D-20):** Read file first, replace any hardcoded color references with token vars.
  </action>
  <verify>
    <automated>grep -c "EmptyState" apps/web/src/app/\(app\)/market/page.tsx && grep -c "EmptyState" apps/web/src/app/\(app\)/proposals/page.tsx && grep -c "Badge" apps/web/src/app/\(app\)/proposals/page.tsx && ! grep -q "bg-green-500" apps/web/src/components/trading/PostCard.tsx && echo "no-green-pass" && ! grep -q "bg-zinc-500" apps/web/src/components/trading/ProposalCard.tsx && echo "no-zinc-pass"</automated>
  </verify>
  <acceptance_criteria>
    - market/page.tsx imports and uses `Skeleton` and `EmptyState`
    - market/page.tsx destructures `loading` from usePostStore
    - market/page.tsx EmptyState has title "No posts yet" and ctaLabel "Create Post"
    - proposals/page.tsx imports `Badge` and uses `<Badge variant="premium">` for pending count
    - proposals/page.tsx has skeleton loading state and EmptyState with "No active proposals"
    - PostCard.tsx contains NO `bg-green-500` or `bg-blue-500` -- replaced with rgba + token vars
    - PostCard.tsx contains `var(--color-success)` for offering label
    - PostDetailModal.tsx contains NO `text-red-400` -- uses `var(--color-error)`
    - CreatePostModal.tsx contains NO `border-green-500` -- uses `var(--color-success)`
    - ProposalCard.tsx contains NO `bg-zinc-500` -- uses `var(--color-surface-light)`
    - ProposalCard.tsx contains `var(--color-error)` for rejected status
    - ProposalDetailModal.tsx contains `var(--color-error)` instead of `text-red-400`
  </acceptance_criteria>
  <done>Market and Proposals pages fully refreshed with skeleton loading, empty states, gradient accents on premium posts, and all hardcoded colors replaced with token CSS vars</done>
</task>

</tasks>

<verification>
- All 4 pages show skeleton loading states when store `loading` is true
- All 4 pages show EmptyState component when data is empty
- No `bg-green-500`, `bg-blue-500`, `bg-red-500`, `bg-zinc-500`, `text-green-400`, `text-blue-400`, `text-red-400`, `text-zinc-400` in any modified trading or cards component
- Proposals page uses Badge component for pending count
- Market premium posts have gold accent border
</verification>

<success_criteria>
- Cards page: skeleton grid (12 items) while loading, EmptyState with "No cards found" when empty
- Collection page: skeleton grid (8 items) while loading, EmptyState with "Your collection is empty" + "Browse Cards" CTA
- Market page: skeleton list (6 items) while loading, EmptyState with "No posts yet" + "Create Post" CTA, premium post accent
- Proposals page: skeleton list (4 items) while loading, EmptyState with "No active proposals" + "Go to Market" CTA, Badge for pending count
- Zero hardcoded Tailwind color classes (green-500, blue-500, red-400/500, zinc-400/500, gray-400/500) in any modified file
</success_criteria>

<output>
After completion, create `.planning/phases/18-web-companion-sync/18-03-SUMMARY.md`
</output>
