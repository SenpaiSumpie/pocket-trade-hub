---
phase: 19-premium-touches-and-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/src/components/SplashOverlay.tsx
  - apps/mobile/app/_layout.tsx
autonomous: true
requirements: [POL-01, POL-05]

must_haves:
  truths:
    - "App launch shows a branded splash animation before the main screen appears"
    - "Logo fades and scales in with a spring animation (0.8 to 1.0)"
    - "App name 'Pocket Trade Hub' fades in below the logo"
    - "A gold shimmer sweeps across both logo and app name"
    - "Total overlay duration is approximately 1.5 seconds"
    - "Reduced-motion users see instant logo display then quick fade to main screen"
  artifacts:
    - path: "apps/mobile/src/components/SplashOverlay.tsx"
      provides: "Animated splash overlay component with logo spring, app name fade, gold shimmer"
      min_lines: 80
    - path: "apps/mobile/app/_layout.tsx"
      provides: "Modified layout mounting SplashOverlay and deferring hideAsync"
  key_links:
    - from: "apps/mobile/app/_layout.tsx"
      to: "apps/mobile/src/components/SplashOverlay.tsx"
      via: "Mounts SplashOverlay when isHydrated, overlay calls SplashScreen.hideAsync()"
      pattern: "SplashOverlay"
    - from: "apps/mobile/src/components/SplashOverlay.tsx"
      to: "apps/mobile/src/constants/springs.ts"
      via: "Imports SPRING_CARD_APPEAR, TIMING_FADE_IN, TIMING_SHIMMER presets"
      pattern: "SPRING_CARD_APPEAR|TIMING_FADE_IN|TIMING_SHIMMER"
---

<objective>
Create a branded splash animation overlay that plays after app load, before revealing the main screen.

Purpose: Per D-01 through D-04, the native static splash transitions seamlessly into a React-rendered animated overlay showing logo spring-in, gold shimmer sweep, and app name fade-in. This creates a premium first impression. Per D-18, reduced-motion users see instant display.

Output: SplashOverlay.tsx component + modified _layout.tsx lifecycle
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-premium-touches-and-polish/19-CONTEXT.md
@.planning/phases/19-premium-touches-and-polish/19-RESEARCH.md
@.planning/phases/19-premium-touches-and-polish/19-UI-SPEC.md

<interfaces>
<!-- Current _layout.tsx splash lifecycle (lines 108-116): -->
```typescript
useEffect(() => {
  if (isHydrated) {
    SplashScreen.hideAsync().catch(() => {});
  }
}, [isHydrated]);

if (!isHydrated) {
  return null;
}
```

<!-- Spring presets from springs.ts: -->
```typescript
export const SPRING_CARD_APPEAR = { damping: 12, stiffness: 120, mass: 1 };
export const TIMING_FADE_IN = { duration: 200, easing: Easing.out(Easing.quad) };
export const TIMING_SHIMMER = { duration: 1200, easing: Easing.linear };
```

<!-- Shimmer pattern from Shimmer.tsx uses react-native-svg LinearGradient -->
<!-- Splash icon asset: apps/mobile/assets/images/splash-icon.png -->
<!-- Splash background: #0f0f1a (from app.json) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SplashOverlay component with animated logo, shimmer, and app name</name>
  <files>apps/mobile/src/components/SplashOverlay.tsx</files>
  <read_first>
    apps/mobile/src/constants/springs.ts
    apps/mobile/src/hooks/useShimmer.ts
    apps/mobile/src/components/animation/Shimmer.tsx
    apps/mobile/app/_layout.tsx
    apps/mobile/assets/images/splash-icon.png
  </read_first>
  <action>
Create `apps/mobile/src/components/SplashOverlay.tsx`:

**Props:** `{ onComplete: () => void }`

**Component structure:**
- Full-screen absolute overlay with `backgroundColor: '#0f0f1a'` (matches app.json splash)
- `zIndex: 9999` to cover everything
- Logo image centered vertically (slightly above center: `translateY: -24`), using `require('@/assets/images/splash-icon.png')`
- App name "Pocket Trade Hub" centered 16px below logo bottom edge
- Gold shimmer sweep overlay covering both logo and app name area

**Animation sequence (non-reduced-motion):**
1. On mount: call `SplashScreen.hideAsync()` immediately (native splash hides, React overlay visible)
2. Logo scale: shared value `logoScale` starts at 0.8, springs to 1.0 using `SPRING_CARD_APPEAR` ({ damping: 12, stiffness: 120, mass: 1 })
3. Logo opacity: shared value `logoOpacity` starts at 0, animates to 1 with `TIMING_FADE_IN` (200ms)
4. After logo reaches ~1.0 (use `setTimeout` 400ms after mount), app name fades in: `nameOpacity` 0 -> 1 with `TIMING_FADE_IN`
5. At ~600ms: gold shimmer sweep starts. Use `useShimmer(containerWidth)` pattern but with gold gradient stops: `rgba(240,192,64,0)` -> `rgba(240,192,64,0.6)` -> `rgba(240,192,64,0)`. Create an inline SVG LinearGradient (don't modify Shimmer.tsx). The shimmer sweeps once (not infinite) over ~1200ms using `withTiming` to go from `-containerWidth` to `containerWidth`.
6. At ~1500ms from mount: overlay fades out. `overlayOpacity` 1 -> 0 with `TIMING_FADE_IN` (200ms)
7. After fade-out completes (~1700ms): call `onComplete()` prop

**Reduced-motion variant (D-18):**
```typescript
const reducedMotion = useReducedMotion();
```
If `reducedMotion`:
- Call `SplashScreen.hideAsync()` on mount
- Set `logoScale.value = 1`, `logoOpacity.value = 1`, `nameOpacity.value = 1` instantly (no springs/timing)
- No shimmer sweep
- After 100ms delay, fade overlay to 0 opacity over 200ms, then call `onComplete()`

**Animated styles:**
- `logoAnimatedStyle`: `{ transform: [{ scale: logoScale.value }], opacity: logoOpacity.value }`
- `nameAnimatedStyle`: `{ opacity: nameOpacity.value }`
- `overlayAnimatedStyle`: `{ opacity: overlayOpacity.value }`

**Static styles:**
- Overlay: `position: 'absolute', top: 0, left: 0, right: 0, bottom: 0, backgroundColor: '#0f0f1a', zIndex: 9999, justifyContent: 'center', alignItems: 'center'`
- Logo image: `width: 120, height: 120` (approximate, adjust to splash-icon.png aspect)
- App name text: `fontSize: 20, fontWeight: '700', color: '#ffffff', fontFamily: fontFamily.bold, marginTop: 16, textAlign: 'center'`
- Import `fontFamily` from `@/src/constants/theme`

**Gold shimmer implementation:**
Use a View with absolute positioning covering the logo+name area. Inside, an `Animated.View` wrapping an SVG with LinearGradient using gold stops. Animate translateX from `-width` to `width` once. Use `Dimensions.get('window').width` for the shimmer width. The shimmer band width should be 40% of container width (matching Shimmer.tsx pattern).
  </action>
  <verify>
    <automated>grep -c "useReducedMotion" apps/mobile/src/components/SplashOverlay.tsx</automated>
  </verify>
  <acceptance_criteria>
    - apps/mobile/src/components/SplashOverlay.tsx exists
    - File contains `export function SplashOverlay`
    - File contains `onComplete` prop
    - File contains `SplashScreen.hideAsync()` call
    - File contains `useReducedMotion` import and check
    - File contains `SPRING_CARD_APPEAR` import from springs.ts
    - File contains `TIMING_FADE_IN` import from springs.ts
    - File contains `rgba(240,192,64` (gold shimmer color stops)
    - File contains `Pocket Trade Hub` string literal
    - File contains `splash-icon` asset reference
    - File contains `#0f0f1a` background color
    - File contains `zIndex: 9999` or equivalent high z-index
    - Reduced-motion path skips animation and calls onComplete after brief delay
  </acceptance_criteria>
  <done>SplashOverlay component renders animated logo spring-in, gold shimmer sweep, app name fade, then fades out. Reduced-motion shows instant logo then quick fade.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate SplashOverlay into _layout.tsx and update splash lifecycle</name>
  <files>apps/mobile/app/_layout.tsx</files>
  <read_first>
    apps/mobile/app/_layout.tsx
    apps/mobile/src/components/SplashOverlay.tsx
  </read_first>
  <action>
Modify `apps/mobile/app/_layout.tsx` to mount the SplashOverlay and defer hideAsync:

1. Add import: `import { SplashOverlay } from '@/src/components/SplashOverlay';`
2. Add state: `const [splashDone, setSplashDone] = useState(false);`
3. **Remove** the existing `useEffect` that calls `SplashScreen.hideAsync()` when `isHydrated` (lines 108-112). The SplashOverlay now owns that lifecycle.
4. Keep the `if (!isHydrated) { return null; }` guard (line 114-116).
5. Inside the return, add `SplashOverlay` as the last child of `GestureHandlerRootView` (after `ToastOverlay`), conditionally rendered:

```typescript
{!splashDone && (
  <SplashOverlay onComplete={() => setSplashDone(true)} />
)}
```

The flow becomes:
- `SplashScreen.preventAutoHideAsync()` at module level (already exists, keep it)
- App renders null until `isHydrated` (already exists, keep it)
- When `isHydrated`, render the full layout tree including `SplashOverlay`
- `SplashOverlay` calls `SplashScreen.hideAsync()` immediately on mount (native splash disappears, React overlay seamlessly visible with same background color)
- After animation (~1.5s), overlay calls `onComplete` -> `setSplashDone(true)` -> overlay unmounts

Do NOT change anything else in _layout.tsx (keep toastConfig, Notifications setup, Stack structure, etc.).
  </action>
  <verify>
    <automated>grep -c "SplashOverlay" apps/mobile/app/_layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - _layout.tsx contains `import { SplashOverlay } from '@/src/components/SplashOverlay'`
    - _layout.tsx contains `splashDone` state variable
    - _layout.tsx contains `<SplashOverlay onComplete=` JSX
    - _layout.tsx does NOT contain `SplashScreen.hideAsync()` directly (moved to SplashOverlay)
    - _layout.tsx still contains `SplashScreen.preventAutoHideAsync()` at module level
    - _layout.tsx still contains `if (!isHydrated)` guard returning null
    - _layout.tsx still contains `<Toast config={toastConfig} />`
    - _layout.tsx still contains `<ToastOverlay />`
  </acceptance_criteria>
  <done>_layout.tsx mounts SplashOverlay after hydration, overlay owns hideAsync lifecycle, unmounts after animation completes</done>
</task>

</tasks>

<verification>
1. `grep "SplashOverlay" apps/mobile/app/_layout.tsx` returns import + JSX usage
2. `grep "SplashScreen.hideAsync" apps/mobile/src/components/SplashOverlay.tsx` returns match
3. `grep "SplashScreen.hideAsync" apps/mobile/app/_layout.tsx` returns NO match (moved to overlay)
4. `grep "useReducedMotion" apps/mobile/src/components/SplashOverlay.tsx` returns match
5. `grep "SPRING_CARD_APPEAR" apps/mobile/src/components/SplashOverlay.tsx` returns match
6. `grep "Pocket Trade Hub" apps/mobile/src/components/SplashOverlay.tsx` returns match
7. No TypeScript compilation errors: `cd apps/mobile && npx tsc --noEmit 2>&1 | head -20`
</verification>

<success_criteria>
- SplashOverlay component exists with full animation sequence (logo spring, gold shimmer, app name fade, overlay fade-out)
- _layout.tsx mounts overlay after hydration, overlay controls SplashScreen.hideAsync() timing
- Reduced-motion users see instant logo display then quick fade
- No flash between native splash and React overlay (same #0f0f1a background)
</success_criteria>

<output>
After completion, create `.planning/phases/19-premium-touches-and-polish/19-02-SUMMARY.md`
</output>
