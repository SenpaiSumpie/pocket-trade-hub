---
phase: 05-trade-proposals-and-reputation
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - apps/mobile/src/stores/trades.ts
  - apps/mobile/src/hooks/useProposals.ts
  - apps/mobile/src/hooks/useMatchSocket.ts
  - apps/mobile/src/hooks/useNotifications.ts
  - apps/mobile/src/components/trades/ProposalCreationModal.tsx
  - apps/mobile/src/components/trades/ProposalCard.tsx
  - apps/mobile/src/components/trades/ProposalDetailModal.tsx
  - apps/mobile/src/components/trades/FairnessMeter.tsx
  - apps/mobile/src/components/trades/MatchDetailModal.tsx
  - apps/mobile/app/(tabs)/trades.tsx
autonomous: true
requirements:
  - TRADE-01
  - TRADE-02
  - TRADE-03
  - TRADE-04
  - TRADE-06

must_haves:
  truths:
    - "User can tap 'Propose Trade' on a match and see a pre-filled proposal editor"
    - "User can add/remove cards in the proposal editor and see the fairness meter update live"
    - "User can send a proposal and it appears in the outgoing proposals list"
    - "User can view incoming proposals with accept/reject/counter buttons"
    - "User can counter-offer by modifying an incoming proposal's cards"
    - "User can view a proposal thread showing chronological history of offers and counters"
    - "Trades tab shows both Matches and Proposals via segment toggle"
    - "ProposalCard displays partner average rating and trade count next to partner name"
    - "ProposalDetailModal displays partner average rating and trade count in the partner header"
  artifacts:
    - path: "apps/mobile/src/stores/trades.ts"
      provides: "Extended trades store with proposals state, fetch, optimistic updates"
      contains: "proposals"
    - path: "apps/mobile/src/hooks/useProposals.ts"
      provides: "Proposal CRUD hooks (create, accept, reject, counter, list)"
    - path: "apps/mobile/src/components/trades/ProposalCreationModal.tsx"
      provides: "Card picker modal with fairness meter, pre-filled from match"
      min_lines: 80
    - path: "apps/mobile/src/components/trades/ProposalDetailModal.tsx"
      provides: "Proposal thread view with accept/reject/counter actions and partner reputation display"
      min_lines: 60
    - path: "apps/mobile/src/components/trades/FairnessMeter.tsx"
      provides: "Horizontal bar showing Unfair/Fair/Great"
    - path: "apps/mobile/src/components/trades/ProposalCard.tsx"
      provides: "Proposal list item with status badge, partner info with avgRating + tradeCount, card preview"
  key_links:
    - from: "apps/mobile/src/components/trades/MatchDetailModal.tsx"
      to: "apps/mobile/src/components/trades/ProposalCreationModal.tsx"
      via: "'Propose Trade' button opens creation modal with match data"
      pattern: "ProposalCreationModal"
    - from: "apps/mobile/src/components/trades/ProposalCreationModal.tsx"
      to: "apps/mobile/src/hooks/useProposals.ts"
      via: "createProposal call on send"
      pattern: "createProposal"
    - from: "apps/mobile/src/hooks/useProposals.ts"
      to: "/proposals"
      via: "apiFetch POST/GET/PUT"
      pattern: "apiFetch.*proposals"
    - from: "apps/mobile/src/components/trades/FairnessMeter.tsx"
      to: "packages/shared/src/schemas/fairness.ts"
      via: "calculateFairness import for live score"
      pattern: "calculateFairness"
    - from: "apps/mobile/src/components/trades/ProposalCard.tsx"
      to: "GET /users/:id"
      via: "Partner reputation data from API (avgRating, tradeCount)"
      pattern: "avgRating|tradeCount"
---

<objective>
Mobile proposal workflow: creation, listing, detail view, and fairness evaluation.

Purpose: Enables users to create trade proposals from matches, view incoming/outgoing proposals, accept/reject/counter-offer, and see live fairness evaluation. This is the core trade negotiation UI. Per user decision, partner reputation (avgRating + tradeCount) is displayed on proposal views.

Output: Extended trades store, proposal hooks, ProposalCreationModal, ProposalDetailModal, ProposalCard, FairnessMeter, Trades tab segment toggle (Matches/Proposals).
</objective>

<execution_context>
@C:/Users/romec/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/romec/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-trade-proposals-and-reputation/05-CONTEXT.md
@.planning/phases/05-trade-proposals-and-reputation/05-RESEARCH.md
@.planning/phases/05-trade-proposals-and-reputation/05-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 -- shared schemas the executor will import -->
From packages/shared/src/schemas/proposal.ts:
```typescript
export const proposalCardSchema = z.object({ cardId: z.string(), cardName: z.string(), imageUrl: z.string(), rarity: z.string() });
export const proposalStatusValues = ['pending', 'accepted', 'rejected', 'countered', 'completed', 'cancelled'] as const;
export const createProposalSchema = z.object({ matchId: z.string(), senderGives: proposalCardSchema.array(), senderGets: proposalCardSchema.array(), fairnessScore: z.number(), parentId: z.string().optional() });
export const tradeProposalSchema = z.object({ id, matchId, senderId, receiverId, parentId, status, senderGives, senderGets, fairnessScore, createdAt, updatedAt });
export type CreateProposalInput = z.infer<typeof createProposalSchema>;
export type ProposalCard = z.infer<typeof proposalCardSchema>;
export type TradeProposal = z.infer<typeof tradeProposalSchema>;
```

From packages/shared/src/schemas/fairness.ts:
```typescript
export function calculateFairness(givingCards: { rarity: string }[], gettingCards: { rarity: string }[]): { score: number; label: 'Unfair' | 'Fair' | 'Great' };
export const RARITY_WEIGHTS: Record<string, number>;
```

<!-- Plan 01 API -- user profile now includes reputation -->
From Plan 01 API:
```
GET /users/:id -> { ...profile, avgRating: number, tradeCount: number }
```

<!-- Existing mobile patterns to follow -->
From apps/mobile/src/stores/trades.ts (existing pattern):
```typescript
// Zustand store with matches state, setMatches, reset
// Extend with proposals, setProposals, etc.
```

From apps/mobile/src/hooks/useApi.ts:
```typescript
export async function apiFetch<T>(path: string, options?: RequestInit): Promise<T>;
```

From apps/mobile/src/hooks/useMatches.ts:
```typescript
// Pattern: export function useMatches() { return { matches, loading, error, fetchMatches, ... } }
```

From apps/mobile/src/components/trades/MatchDetailModal.tsx:
```typescript
// Has "Propose Trade" button placeholder
// Shows partner header, card pair layout, star rating
// Props: match object, visible, onClose
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trades store extension and proposal hooks</name>
  <files>
    apps/mobile/src/stores/trades.ts,
    apps/mobile/src/hooks/useProposals.ts,
    apps/mobile/src/hooks/useMatchSocket.ts
  </files>
  <action>
    1. Extend apps/mobile/src/stores/trades.ts:
       - Add proposals state: `proposals: TradeProposal[]`, `proposalsLoading: boolean`, `proposalDirection: 'incoming' | 'outgoing' | 'all'`
       - Add actions: `setProposals(proposals)`, `addProposal(proposal)`, `updateProposal(id, updates)`, `setProposalDirection(dir)`
       - Add `activeSegment: 'matches' | 'proposals'` and `setActiveSegment(seg)` for Trades tab toggle
       - Extend existing `reset()` to also clear proposals state
       - Keep all existing matches state/actions unchanged

    2. Create apps/mobile/src/hooks/useProposals.ts:
       - `useProposals()` hook returning: { proposals, loading, fetchProposals, createProposal, acceptProposal, rejectProposal, counterProposal }
       - `fetchProposals(direction?, status?)`: GET /proposals?direction={dir}&status={status}, call setProposals
       - `createProposal(input: CreateProposalInput)`: POST /proposals, add to store optimistically, refetch on success
       - `acceptProposal(id)`: PUT /proposals/{id}/accept, update store optimistically, revert on error
       - `rejectProposal(id)`: PUT /proposals/{id}/reject, same pattern
       - `counterProposal(id, input)`: POST /proposals/{id}/counter, refetch proposals after
       - `getProposalThread(id)`: GET /proposals/{id}, return full thread array
       - Use apiFetch from useApi.ts for all API calls
       - Follow useMatches.ts pattern for loading/error state

    3. Extend apps/mobile/src/hooks/useMatchSocket.ts:
       - Add listeners for: 'new-proposal', 'proposal-accepted', 'proposal-rejected', 'proposal-countered', 'trade-completed'
       - On 'new-proposal': show toast ("New trade proposal from {senderName}"), refetch proposals
       - On 'proposal-accepted'/'rejected'/'countered': show toast, update proposal in store
       - On 'trade-completed': show toast, update proposal status in store
       - Follow existing 'new-match' event pattern for toast config
       - Add proposalNotification custom toast type extending existing matchNotification pattern
  </action>
  <verify>
    <automated>cd C:/Users/romec/OneDrive/Desktop/CodingProjects/pocket-trade-hub && npx tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>
    - Trades store has proposals state alongside matches state with segment toggle
    - useProposals hook provides full CRUD for proposals via API
    - Socket.IO hook listens for all proposal lifecycle events and shows toasts
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: FairnessMeter and ProposalCreationModal</name>
  <files>
    apps/mobile/src/components/trades/FairnessMeter.tsx,
    apps/mobile/src/components/trades/ProposalCreationModal.tsx,
    apps/mobile/src/components/trades/MatchDetailModal.tsx
  </files>
  <action>
    1. Create apps/mobile/src/components/trades/FairnessMeter.tsx:
       - Props: { givingCards: ProposalCard[], gettingCards: ProposalCard[] }
       - Calls calculateFairness() from @pocket-trade-hub/shared to compute score and label
       - Renders horizontal bar track with positioned indicator dot
       - Labels "Unfair" / "Fair" / "Great" at left / center / right
       - Indicator color: green for Great, gold (#f0c040) for Fair, red for Unfair
       - Use React Native Animated for smooth indicator movement when cards change
       - Use project theme constants (colors, spacing, borderRadius)
       - Compact size -- fits in a modal without dominating the view

    2. Create apps/mobile/src/components/trades/ProposalCreationModal.tsx:
       - Props: { visible, onClose, match: TradeMatch, isCounter?: boolean, existingProposal?: TradeProposal }
       - Pre-fill giving/getting cards from match.userGives / match.userGets (converted to ProposalCard format)
       - If isCounter, pre-fill from existingProposal.senderGets (becomes my gives) and existingProposal.senderGives (becomes my gets)
       - Two sections: "You Give" and "You Get" each showing card thumbnails in a horizontal scroll
       - Each card has an X button to remove
       - "Add Card" button in each section: opens a mini card picker filtered to own collection (for giving) or partner's wanted list (for getting). Use a simple search/filter list -- can reuse CardThumbnail component.
       - FairnessMeter displayed between the two sections, updates live as cards are added/removed
       - "Send Proposal" button at bottom, disabled if either side is empty
       - On send: call createProposal (or counterProposal if isCounter) from useProposals hook
       - Show loading state during API call, close modal on success, show error toast on failure
       - Use Modal from react-native with SafeAreaView, dark theme styling

    3. Wire MatchDetailModal.tsx "Propose Trade" button:
       - Import ProposalCreationModal
       - Add state: `showProposalModal: boolean`
       - On "Propose Trade" button press, set showProposalModal=true
       - Pass match data to ProposalCreationModal
       - On ProposalCreationModal close, set showProposalModal=false
  </action>
  <verify>
    <automated>cd C:/Users/romec/OneDrive/Desktop/CodingProjects/pocket-trade-hub && npx tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>
    - FairnessMeter renders a horizontal bar that updates based on card rarity comparison
    - ProposalCreationModal shows pre-filled cards from match, allows add/remove, shows live fairness
    - "Propose Trade" in MatchDetailModal opens ProposalCreationModal with match data
    - Counter-offer mode pre-fills from existing proposal with sides swapped
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 3: ProposalCard, ProposalDetailModal, and Trades tab segment</name>
  <files>
    apps/mobile/src/components/trades/ProposalCard.tsx,
    apps/mobile/src/components/trades/ProposalDetailModal.tsx,
    apps/mobile/app/(tabs)/trades.tsx
  </files>
  <action>
    1. Create apps/mobile/src/components/trades/ProposalCard.tsx:
       - Props: { proposal: TradeProposal, currentUserId: string, onPress: () => void }
       - Shows: partner display name + avatar (determine partner from senderId vs receiverId vs currentUserId)
       - **Partner reputation display (per user decision: "Average rating and trade count displayed on proposal views"):** Next to partner name, show their avgRating as small star icon + number (e.g., "4.5 star-icon") and tradeCount (e.g., "(12 trades)"). If partner has no ratings, show "New trader". Fetch partner profile from GET /users/:id if not already cached, or accept partnerReputation as an optional prop populated by the parent component when fetching proposals. Prefer including reputation data in the proposal list API response (Plan 01 getProposals can join with getUserReputation) -- check 05-01-SUMMARY.md for actual API shape and adapt accordingly.
       - Card thumbnail previews for senderGives/senderGets (first 3 cards each, "+N more" if more)
       - Status badge: colored pill (pending=gold, accepted=green, rejected=red, countered=blue, completed=green)
       - Direction indicator: "Incoming" or "Outgoing" text
       - Timestamp (relative: "2h ago", "3d ago")
       - Follow MatchCard.tsx styling pattern (card shadow, border, padding)
       - Pressable with onPress callback

    2. Create apps/mobile/src/components/trades/ProposalDetailModal.tsx:
       - Props: { visible, onClose, proposalId: string, currentUserId: string }
       - On mount: fetch proposal thread via getProposalThread(proposalId) from useProposals
       - **Partner reputation header (per user decision: "Average rating and trade count displayed on proposal views"):** Display partner avatar, display name, and below the name show avgRating as filled/half/empty stars + tradeCount text (e.g., "4.5 stars - 12 completed trades"). Fetch partner profile from GET /users/:partnerId to get avgRating and tradeCount. If no ratings, show "New trader - no ratings yet".
       - Show full thread as chronological list: each entry shows sender name, cards offered/requested, fairness score, timestamp
       - Highlight the "active" proposal (most recent pending one)
       - Action buttons at bottom depend on state:
         - If pending AND I'm receiver: Accept / Reject / Counter buttons
         - If accepted AND either party: "Mark as Completed" button + partner friend code prominently displayed
         - If completed: "Rate Partner" button (if not yet rated) -- triggers onRatePartner callback prop
         - If rejected/countered: no actions (read-only history)
       - Accept calls acceptProposal, Reject calls rejectProposal
       - Counter opens ProposalCreationModal in counter mode
       - "Mark as Completed" calls completeProposal, then triggers onRatePartner callback
       - Loading states for all actions
       - Friend code display: large text with Ionicons copy icon, tap copies to clipboard (reuse pattern from MatchDetailModal)

    3. Update apps/mobile/app/(tabs)/trades.tsx:
       - Add segment toggle at top: "Matches" | "Proposals" (use Pressable pills similar to MatchSortToggle)
       - Read activeSegment from trades store
       - When "Matches" selected: show existing match list (FlashList of MatchCard)
       - When "Proposals" selected: show FlashList of ProposalCard, pull-to-refresh, empty state
       - Add direction filter for proposals: "All" | "Incoming" | "Outgoing" (small pills below segment)
       - ProposalCard onPress opens ProposalDetailModal
       - Keep existing match functionality unchanged (sort toggle, empty state, etc.)
       - Empty state for proposals: "No proposals yet. Create one from a match!" with Ionicons illustration
  </action>
  <verify>
    <automated>cd C:/Users/romec/OneDrive/Desktop/CodingProjects/pocket-trade-hub && npx tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>
    - ProposalCard shows partner info with avgRating + tradeCount, card previews, status badge, direction
    - ProposalDetailModal shows partner reputation in header, full thread history with accept/reject/counter/complete actions
    - Accepted proposals display partner friend code prominently with copy functionality
    - Trades tab has Matches/Proposals segment toggle with direction filter for proposals
    - Full proposal workflow: view list -> tap -> see detail -> accept/reject/counter/complete
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
- TypeScript compiles: `npx tsc --noEmit --project apps/mobile/tsconfig.json`
- Trades tab renders with segment toggle between Matches and Proposals
- Tapping "Propose Trade" on a match opens ProposalCreationModal with pre-filled cards
- FairnessMeter updates live as cards are added/removed
- Proposal list shows incoming/outgoing with status badges and partner reputation
- ProposalDetailModal shows partner reputation in header and thread history with action buttons
- Socket events trigger toasts for proposal lifecycle changes
</verification>

<success_criteria>
- Complete proposal creation flow from match to sent proposal
- Complete proposal response flow (accept/reject/counter) with real-time feedback
- Proposal list with direction filtering, status badges, and partner reputation (avgRating + tradeCount)
- ProposalDetailModal shows partner reputation in header area
- Thread view showing counter-offer history chronologically
- Friend code prominently displayed on accepted proposals
- Fairness meter works with live updates
- All mobile components use project theme (dark background, gold accent)
</success_criteria>

<output>
After completion, create `.planning/phases/05-trade-proposals-and-reputation/05-02-SUMMARY.md`
</output>
