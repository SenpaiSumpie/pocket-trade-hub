---
phase: 05-trade-proposals-and-reputation
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - apps/mobile/src/stores/notifications.ts
  - apps/mobile/src/hooks/useNotifications.ts
  - apps/mobile/src/components/notifications/NotificationBell.tsx
  - apps/mobile/src/components/notifications/NotificationItem.tsx
  - apps/mobile/app/notifications.tsx
  - apps/mobile/src/components/trades/RatingModal.tsx
  - apps/mobile/src/hooks/useRating.ts
  - apps/mobile/app/(tabs)/_layout.tsx
  - apps/mobile/app/(tabs)/profile.tsx
  - apps/mobile/src/components/trades/MatchCard.tsx
autonomous: true
requirements:
  - TRADE-05
  - REP-01
  - REP-02
  - NOTIF-01
  - NOTIF-02
  - NOTIF-03

must_haves:
  truths:
    - "User sees bell icon with unread badge count in app header on every tab"
    - "Tapping bell opens full-screen notification inbox with all event types"
    - "Tapping a notification marks it as read and navigates to relevant content"
    - "User can tap 'Mark all as read' to clear all unread badges"
    - "Rating modal appears after tapping 'Mark as Completed' with 5-star selector"
    - "User can skip rating or submit 1-5 stars"
    - "User profile shows average rating and completed trade count"
    - "Match cards show partner's average rating and trade count"
  artifacts:
    - path: "apps/mobile/src/stores/notifications.ts"
      provides: "Notification inbox store with unreadCount, notifications list"
      exports: ["useNotificationStore"]
    - path: "apps/mobile/src/hooks/useNotifications.ts"
      provides: "Extended notification hooks with inbox fetching, mark read"
    - path: "apps/mobile/src/components/notifications/NotificationBell.tsx"
      provides: "Bell icon header component with unread badge"
    - path: "apps/mobile/app/notifications.tsx"
      provides: "Full-screen notification inbox route"
      min_lines: 40
    - path: "apps/mobile/src/components/trades/RatingModal.tsx"
      provides: "5-star rating prompt modal"
    - path: "apps/mobile/src/hooks/useRating.ts"
      provides: "Rating submission hook"
  key_links:
    - from: "apps/mobile/src/components/notifications/NotificationBell.tsx"
      to: "apps/mobile/app/notifications.tsx"
      via: "router.push('/notifications') on press"
      pattern: "router\\.push.*notifications"
    - from: "apps/mobile/src/components/notifications/NotificationBell.tsx"
      to: "apps/mobile/src/stores/notifications.ts"
      via: "useNotificationStore for unreadCount"
      pattern: "useNotificationStore"
    - from: "apps/mobile/app/notifications.tsx"
      to: "apps/mobile/src/hooks/useNotifications.ts"
      via: "fetchNotifications, markRead, markAllRead"
      pattern: "useNotifications"
    - from: "apps/mobile/src/components/trades/RatingModal.tsx"
      to: "apps/mobile/src/hooks/useRating.ts"
      via: "submitRating call"
      pattern: "submitRating"
    - from: "apps/mobile/app/(tabs)/_layout.tsx"
      to: "apps/mobile/src/components/notifications/NotificationBell.tsx"
      via: "headerRight in screenOptions"
      pattern: "NotificationBell"
---

<objective>
Notification inbox, rating system, and reputation display across the app.

Purpose: Gives users a persistent notification inbox accessible from any tab via bell icon, a 5-star rating flow after trade completion, and reputation display (avg rating + trade count) on profiles and match cards.

Output: Notification store + hooks, NotificationBell header component, notification inbox screen, RatingModal, rating hook, reputation on profile and match cards.
</objective>

<execution_context>
@C:/Users/romec/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/romec/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-trade-proposals-and-reputation/05-CONTEXT.md
@.planning/phases/05-trade-proposals-and-reputation/05-RESEARCH.md
@.planning/phases/05-trade-proposals-and-reputation/05-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 -- API contracts -->
API Routes (from Plan 01):
```
GET  /notifications              -> { notifications: Notification[], hasMore: boolean }
GET  /notifications/unread-count -> { count: number }
PUT  /notifications/:id/read     -> { success: true }
PUT  /notifications/read-all     -> { success: true }
POST /proposals/:id/rate         -> { id: string } (body: { stars: 1-5 })
GET  /users/:id                  -> { ...profile, avgRating: number, tradeCount: number }
```

From packages/shared/src/schemas/notification.ts:
```typescript
export const notificationTypeValues = ['proposal_received', 'proposal_accepted', 'proposal_rejected', 'proposal_countered', 'trade_completed', 'new_match', 'rating_received', 'system'] as const;
export const notificationSchema = z.object({ id: z.string(), userId: z.string(), type: z.enum(notificationTypeValues), title: z.string(), body: z.string(), data: z.any().optional(), read: z.boolean(), createdAt: z.string() });
export type Notification = z.infer<typeof notificationSchema>;
```

From packages/shared/src/schemas/rating.ts:
```typescript
export const createRatingSchema = z.object({ stars: z.number().int().min(1).max(5) });
export type CreateRatingInput = z.infer<typeof createRatingSchema>;
```

<!-- Existing mobile patterns -->
From apps/mobile/app/(tabs)/_layout.tsx:
```typescript
// Tab layout with headerStyle, headerTintColor, headerShadowVisible
// Each Tabs.Screen can have options={{ headerRight: () => <Component /> }}
```

From apps/mobile/src/hooks/useApi.ts:
```typescript
export async function apiFetch<T>(path: string, options?: RequestInit): Promise<T>;
```

From apps/mobile/src/components/trades/MatchCard.tsx:
```typescript
// Shows partner avatar, display name, card pair previews, star rating
// Currently has partnerTradeCount placeholder (hardcoded 0)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Notification store, hooks, bell icon, and inbox screen</name>
  <files>
    apps/mobile/src/stores/notifications.ts,
    apps/mobile/src/hooks/useNotifications.ts,
    apps/mobile/src/components/notifications/NotificationBell.tsx,
    apps/mobile/src/components/notifications/NotificationItem.tsx,
    apps/mobile/app/notifications.tsx,
    apps/mobile/app/(tabs)/_layout.tsx,
    apps/mobile/src/hooks/useMatchSocket.ts
  </files>
  <action>
    1. Create apps/mobile/src/stores/notifications.ts (new Zustand store):
       - State: `notifications: Notification[]`, `unreadCount: number`, `loading: boolean`, `hasMore: boolean`
       - Actions: `setNotifications(notifs, hasMore)`, `addNotification(notif)`, `markRead(id)`, `markAllRead()`, `setUnreadCount(count)`, `reset()`
       - Follow existing trades store pattern (create with zustand, reset on logout)

    2. Extend apps/mobile/src/hooks/useNotifications.ts (currently handles push token registration):
       - Keep existing push token registration logic
       - Add: `fetchNotifications(cursor?)`: GET /notifications?cursor={cursor}, update store
       - Add: `fetchUnreadCount()`: GET /notifications/unread-count, update store
       - Add: `markNotificationRead(id)`: PUT /notifications/{id}/read, update store optimistically
       - Add: `markAllNotificationsRead()`: PUT /notifications/read-all, update store optimistically
       - Export these alongside existing useNotificationSetup

    3. Create apps/mobile/src/components/notifications/NotificationBell.tsx:
       - Read unreadCount from useNotificationStore
       - Render Ionicons "notifications-outline" (size 24, color from theme)
       - If unreadCount > 0: show red badge circle with count (99+ cap)
       - onPress: router.push('/notifications')
       - Wrapped in TouchableOpacity with padding for hit target
       - Badge positioned absolute top-right of icon

    4. Create apps/mobile/src/components/notifications/NotificationItem.tsx:
       - Props: { notification: Notification, onPress: (notif) => void }
       - Icon based on type: proposal_received -> "swap-horizontal", proposal_accepted -> "checkmark-circle", proposal_rejected -> "close-circle", trade_completed -> "trophy", new_match -> "flash", rating_received -> "star", system -> "megaphone"
       - Title + body text, relative timestamp ("2h ago")
       - Unread indicator: left blue dot or bolder text when read=false
       - Pressable wrapper calling onPress

    5. Create apps/mobile/app/notifications.tsx (full-screen route, NOT a tab):
       - Header: "Notifications" title, "Mark all as read" button in headerRight (only if unreadCount > 0)
       - FlashList of NotificationItem components
       - Pull-to-refresh triggers fetchNotifications
       - Infinite scroll: onEndReached loads next page if hasMore
       - onPress handler: markNotificationRead(id), then deep-link based on notification.data:
         - If data.proposalId: navigate to trades tab (or open proposal detail -- store proposalId for later)
         - If data.matchId: navigate to trades tab
         - Default: just mark as read
       - Empty state: Ionicons "notifications-off-outline" with "No notifications yet" text
       - Dark theme styling matching rest of app

    6. Update apps/mobile/app/(tabs)/_layout.tsx:
       - Import NotificationBell
       - Add `headerRight: () => <NotificationBell />` to Tabs screenOptions (appears on all tabs)
       - Fetch unread count on login: in the existing useEffect that fetches matches, also call fetchUnreadCount
       - Import and call useNotificationStore reset on logout (extend existing reset pattern)

    7. Extend apps/mobile/src/hooks/useMatchSocket.ts:
       - Add listener for 'notification-new' event
       - On 'notification-new': increment unreadCount in notification store, add notification to store
       - This handles real-time badge updates when new notifications arrive
  </action>
  <verify>
    <automated>cd C:/Users/romec/OneDrive/Desktop/CodingProjects/pocket-trade-hub && npx tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>
    - Bell icon visible in header on all tabs with unread badge count
    - Tapping bell navigates to /notifications full-screen inbox
    - Notification list renders with type-specific icons, relative timestamps, read/unread styling
    - Pull-to-refresh and infinite scroll work
    - "Mark all as read" clears all unread indicators
    - Tapping notification marks it read and deep-links to relevant content
    - Real-time badge updates via Socket.IO notification-new event
    - Store resets on logout
  </done>
</task>

<task type="auto">
  <name>Task 2: Rating modal, rating hook, and reputation display on profiles and match cards</name>
  <files>
    apps/mobile/src/components/trades/RatingModal.tsx,
    apps/mobile/src/hooks/useRating.ts,
    apps/mobile/app/(tabs)/profile.tsx,
    apps/mobile/src/components/trades/MatchCard.tsx
  </files>
  <action>
    1. Create apps/mobile/src/hooks/useRating.ts:
       - `submitRating(proposalId: string, stars: number)`: POST /proposals/{proposalId}/rate with { stars }
       - `checkIfRated(proposalId: string)`: Track locally which proposals user has rated (simple Set in store or check server)
       - Returns { submitRating, submitting, rated }

    2. Create apps/mobile/src/components/trades/RatingModal.tsx:
       - Props: { visible, onClose, proposalId: string, partnerName: string }
       - Shows: "Rate your trade with {partnerName}" heading
       - 5 star icons in a row (Ionicons "star" filled / "star-outline" unfilled)
       - Stars are pressable: tapping star N selects rating 1-N (highlight stars 1 through N in gold #f0c040)
       - Stars also support sliding/dragging across for smooth selection
       - "Submit" button (disabled until a star is selected), "Skip" button
       - On submit: call submitRating(proposalId, selectedStars) from useRating hook
       - Show loading during submission, close on success with success toast
       - On skip: close modal without submitting
       - Per user decision: appears immediately after tapping "Mark as Completed" -- the caller (ProposalDetailModal from Plan 02) will show this modal after completeProposal succeeds via an onRatePartner callback
       - Export the component so ProposalDetailModal (Plan 02) can import and use it
       - Dark theme styling, modal overlay

    3. Update apps/mobile/app/(tabs)/profile.tsx:
       - Fetch user profile which now includes avgRating and tradeCount (from Plan 01 API changes)
       - Display below display name / friend code area:
         - Star rating: render avgRating as filled/half/empty stars (use Ionicons "star"/"star-half"/"star-outline")
         - Trade count: "{tradeCount} completed trades" text
         - If no ratings yet: show "No ratings yet" text instead of stars
       - Also update the public profile view (viewing another user) with the same rating display

    4. Update apps/mobile/src/components/trades/MatchCard.tsx:
       - The match API response from Plan 01 now includes partner avgRating and tradeCount
       - Replace the hardcoded partnerTradeCount=0 with actual tradeCount from match data
       - Add small star rating display next to partner name (e.g., "4.5 star-icon (12 trades)")
       - If partner has no ratings, show "New trader" text
       - Keep existing card pair preview, star rating (match quality stars), and other layout
  </action>
  <verify>
    <automated>cd C:/Users/romec/OneDrive/Desktop/CodingProjects/pocket-trade-hub && npx tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>
    - RatingModal shows 5 interactive stars with submit/skip options
    - Rating modal can be triggered by ProposalDetailModal (Plan 02) after trade completion
    - User profile displays avgRating as stars and tradeCount
    - Match cards show partner reputation (rating + trade count) instead of placeholder
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
- TypeScript compiles: `npx tsc --noEmit --project apps/mobile/tsconfig.json`
- Bell icon renders in header on all tabs
- Notification inbox opens and shows notifications with type-specific icons
- Rating modal renders 5 interactive stars
- Profile screen shows avgRating and tradeCount
- Match cards show partner reputation data
</verification>

<success_criteria>
- Bell icon with live unread badge count accessible from every tab
- Full notification inbox with paginated list, mark read, mark all read
- Deep linking from notifications to relevant proposals/matches
- 5-star rating flow triggered after trade completion (skippable)
- Reputation visible on user profiles and match cards
- All components follow project dark theme with gold accent
</success_criteria>

<output>
After completion, create `.planning/phases/05-trade-proposals-and-reputation/05-03-SUMMARY.md`
</output>
