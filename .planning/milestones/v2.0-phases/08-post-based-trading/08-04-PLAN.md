---
phase: 08-post-based-trading
plan: 04
type: execute
wave: 3
depends_on: ["08-02", "08-03"]
files_modified:
  - apps/mobile/app/(tabs)/trades.tsx
  - apps/mobile/src/stores/trades.ts
  - apps/mobile/src/hooks/useMatchSocket.ts
  - apps/mobile/src/hooks/useProposals.ts
  - apps/mobile/src/components/trades/MyPostCard.tsx
  - apps/mobile/src/components/trades/MyPostDetailModal.tsx
  - apps/mobile/src/components/trades/ProposalCard.tsx
  - apps/mobile/src/components/trades/ProposalCreationModal.tsx
  - apps/mobile/src/components/trades/ProposalDetailModal.tsx
autonomous: false
requirements:
  - TRAD-04
  - TRAD-05

must_haves:
  truths:
    - "Trades tab shows My Posts and Proposals segments (not Matches)"
    - "User can see their own posts with status and proposal counts"
    - "User can close or delete their own posts from the Trades tab"
    - "User can send a proposal from a post without a match record"
    - "Real-time post-match and post-closed events update the UI"
    - "Proposal flow works end-to-end from post detail to completion"
  artifacts:
    - path: "apps/mobile/app/(tabs)/trades.tsx"
      provides: "Refactored Trades screen with My Posts + Proposals segments"
      min_lines: 60
    - path: "apps/mobile/src/hooks/useMatchSocket.ts"
      provides: "Socket hook with post events (post-match, post-closed)"
      min_lines: 20
    - path: "apps/mobile/src/components/trades/MyPostCard.tsx"
      provides: "Card for user's own post in Trades tab"
      min_lines: 30
    - path: "apps/mobile/src/components/trades/ProposalCreationModal.tsx"
      provides: "Adapted proposal creation supporting postId"
      min_lines: 40
  key_links:
    - from: "apps/mobile/src/hooks/useMatchSocket.ts"
      to: "apps/mobile/src/stores/posts.ts"
      via: "Socket events update posts store"
      pattern: "post-match|post-closed"
    - from: "apps/mobile/src/hooks/useProposals.ts"
      to: "/proposals API"
      via: "POST /proposals with postId"
      pattern: "postId"
    - from: "apps/mobile/src/components/trades/ProposalCreationModal.tsx"
      to: "apps/mobile/src/hooks/useProposals.ts"
      via: "createProposal with postId"
      pattern: "createProposal.*postId"
---

<objective>
Refactor the Trades tab for post-based trading and wire up real-time events for the complete trading flow.

Purpose: Users manage their posts and proposals in the Trades tab. Real-time Socket.IO events keep the UI updated when posts are matched or auto-closed. The proposal flow is adapted to work from posts.
Output: Refactored Trades tab (My Posts + Proposals), real-time post events, working end-to-end proposal flow from posts.
</objective>

<execution_context>
@C:/Users/romec/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/romec/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-post-based-trading/08-CONTEXT.md
@.planning/phases/08-post-based-trading/08-RESEARCH.md
@.planning/phases/08-post-based-trading/08-01-SUMMARY.md
@.planning/phases/08-post-based-trading/08-02-SUMMARY.md
@.planning/phases/08-post-based-trading/08-03-SUMMARY.md

<interfaces>
<!-- From Plan 01 -->
TradePost, PostCard, CreatePostInput types from @pocket-trade-hub/shared

<!-- From Plan 02 -->
Updated proposal schemas: matchId optional, postId optional
Socket events: 'post-match' { postId, matchedPostIds }, 'post-closed' { postId }
Auto-close on trade completion

<!-- From Plan 03 -->
usePostsStore: stores/posts.ts with marketPosts, myPosts, actions
usePosts hook: createPost, closePost, deletePost, fetchMyPosts
useMarketplace hook: fetchPosts, filters

<!-- Existing mobile patterns to modify -->

From apps/mobile/src/hooks/useMatchSocket.ts:
```typescript
// Current events handled: new-match, match-update, new-proposal, proposal-accepted, etc.
// Pattern: socket.on('event', handler) with store updates
```

From apps/mobile/src/hooks/useProposals.ts:
```typescript
// createProposal currently requires matchId
// Need to support postId as alternative
```

From apps/mobile/src/stores/trades.ts:
```typescript
// Current segments: 'matches' | 'proposals'
// Need to change to: 'posts' | 'proposals'
// Remove match-related state
```

From apps/mobile/src/components/trades/ProposalCreationModal.tsx:
```typescript
// Currently receives matchId and pre-populates from match data
// Need to support receiving postId and pre-populating from post data
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trades store refactor, socket events, and proposal hook adaptation</name>
  <files>
    apps/mobile/src/stores/trades.ts,
    apps/mobile/src/hooks/useMatchSocket.ts,
    apps/mobile/src/hooks/useProposals.ts
  </files>
  <action>
    1. Refactor apps/mobile/src/stores/trades.ts:
       - Remove match-related state: matches, unseenCount, sortBy, isLoading, lastRefreshed
       - Remove match actions: setMatches, addMatch, clearUnseen, setSortBy, setLoading, markSeen
       - Change ActiveSegment type from 'matches' | 'proposals' to 'posts' | 'proposals'
       - Default activeSegment to 'posts'
       - Keep all proposal state and actions unchanged
       - Update TradeMatch import removal (no longer needed from shared)
       - Keep the store export name as useTradesStore for consistency

    2. Refactor apps/mobile/src/hooks/useMatchSocket.ts:
       - Remove match-related socket event handlers (new-match, match-update)
       - Add post-related socket event handlers:
         a. 'post-match': When received, show toast "New match for your post!" and trigger a refresh of myPosts via usePosts hook or direct store action. Optionally show a badge update.
         b. 'post-closed': When received, update the post status in both usePostsStore (myPosts and marketPosts) to reflect auto_closed status. Show toast "A post was auto-closed because the card was traded."
       - Keep all proposal socket events unchanged (new-proposal, proposal-accepted, proposal-rejected, proposal-countered, proposal-cancelled, trade-completed)
       - Rename the hook to useTradeSocket (or keep useMatchSocket for minimal diff -- Claude's discretion, prefer minimal rename to avoid breaking imports in other files)

    3. Update apps/mobile/src/hooks/useProposals.ts:
       - Update createProposal to accept either matchId or postId:
         - Change the function signature to accept { postId?, matchId?, receiverId, senderGives, senderGets, fairnessScore, parentId? }
         - Send postId in the API request body when creating from a post
       - Ensure the hook works for both post-based and legacy match-based proposals
       - Update any TypeScript types that reference matchId as required
  </action>
  <verify>
    <automated>cd apps/mobile && npx expo export --platform web --output-dir /tmp/expo-check-08-04a 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Trades store has posts+proposals segments (no match state)
    - Socket handles post-match and post-closed events with store updates and toasts
    - Proposal socket events still work (new-proposal, accepted, rejected, etc.)
    - useProposals.createProposal supports postId parameter
    - App compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Trades tab UI refactor and My Post components</name>
  <files>
    apps/mobile/app/(tabs)/trades.tsx,
    apps/mobile/src/components/trades/MyPostCard.tsx,
    apps/mobile/src/components/trades/MyPostDetailModal.tsx,
    apps/mobile/src/components/trades/ProposalCard.tsx,
    apps/mobile/src/components/trades/ProposalCreationModal.tsx,
    apps/mobile/src/components/trades/ProposalDetailModal.tsx
  </files>
  <action>
    1. Create MyPostCard.tsx:
       - Displays user's own post with: type badge (Offering/Seeking), card image, card name, language, status badge (active=green, closed=gray, auto_closed=orange), creation date.
       - Show count of proposals received on this post (if available from API).
       - onPress opens MyPostDetailModal.

    2. Create MyPostDetailModal.tsx:
       - Full detail view of user's own post.
       - Actions: "Close Post" button (calls closePost from usePosts), "Delete Post" button (with confirmation alert, calls deletePost).
       - If post has proposals, show a list/link to view them (filtered proposals for this postId).
       - If post is already closed/auto_closed, disable close button, show status explanation.

    3. Refactor apps/mobile/app/(tabs)/trades.tsx:
       - Replace the segment control: change from "Matches | Proposals" to "My Posts | Proposals".
       - "My Posts" segment: Fetch via usePosts.fetchMyPosts(), render FlashList of MyPostCard components. Pull-to-refresh. Empty state: "You don't have any posts yet. Go to Market to create one!"
       - "Proposals" segment: Keep existing proposal list rendering largely intact. Update to work without match dependency.
       - Remove all match-related code: match fetching, MatchCard imports, match detail modal references.
       - Add a FAB or header button to navigate to Market tab or open PostCreationModal directly.

    4. Update ProposalCreationModal.tsx:
       - Support receiving postId as a prop (in addition to or instead of matchId).
       - When opened from a post: receiverId = post.userId, and pre-populate senderGives or senderGets based on post type (Offering post -> user wants the card -> senderGets pre-filled; Seeking post -> user has the card -> senderGives pre-filled).
       - User then fills the other side of the trade (what they offer in return or what they want in return).
       - Pass postId to createProposal hook call.
       - Keep backward compatibility if opened from a match context (for any legacy flows).

    5. Update ProposalCard.tsx:
       - Show post reference instead of match reference when postId is present.
       - Display the post type and card info if available.
       - Gracefully handle proposals with either matchId or postId.

    6. Update ProposalDetailModal.tsx:
       - Show post context when proposal has postId: link to the original post or show post card info.
       - Keep all existing proposal actions (accept, reject, counter, complete, rate).
       - Handle case where referenced post may be closed/auto_closed (show notice).
  </action>
  <verify>
    <automated>cd apps/mobile && npx expo export --platform web --output-dir /tmp/expo-check-08-04b 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Trades tab shows "My Posts | Proposals" segments
    - My Posts segment renders user's posts with status badges
    - MyPostDetailModal allows closing and deleting posts
    - ProposalCreationModal works from post context with pre-populated cards
    - ProposalCard and ProposalDetailModal handle postId-based proposals
    - All match-related code removed from trades screen
    - App compiles without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-end post-based trading verification</name>
  <files>none</files>
  <action>
    Human verifies the complete post-based trading flow works end-to-end across all 4 plans.
  </action>
  <verify>Human confirms all verification steps pass</verify>
  <done>Full trading flow verified: create posts, browse marketplace, send proposals from posts, complete trades, auto-close posts</done>
  <what-built>Complete post-based trading system: Market tab with browsing/filtering/creation, Trades tab with My Posts + Proposals, real-time matching notifications, proposal flow from posts, and auto-close on trade completion.</what-built>
  <how-to-verify>
    1. Start the API server: `cd apps/api && npm run dev`
    2. Start the mobile app: `cd apps/mobile && npx expo start`
    3. Open the app and verify:
       a. Bottom nav shows 5 tabs: Home | Cards | Market | Trades | Profile
       b. Market tab shows empty state or existing posts
       c. Tap create button -> select Offering -> pick a card from collection -> confirm -> post appears
       d. Tap create button -> select Seeking -> pick a card from wanted list -> confirm -> post appears
       e. Verify card language is visible on posts
       f. Use filters: toggle Offering/Seeking, search by card name
       g. Go to Trades tab -> see "My Posts" segment with created posts
       h. Tap a post -> see detail with Close/Delete actions
       i. Create a second user account, create a complementary post (Seeking what first user is Offering)
       j. Verify notification appears for matching post
       k. From Market tab, tap the complementary post -> Send Proposal -> verify proposal creation works
       l. Accept the proposal and complete the trade -> verify the Offering post is auto-closed
    4. Run API tests: `cd apps/api && npx jest --no-coverage`
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
```bash
cd apps/api && npx jest --no-coverage
cd apps/mobile && npx expo export --platform web --output-dir /tmp/expo-check-08-04-final 2>&1 | tail -5
```
All API tests pass. Mobile app compiles. End-to-end flow works: create posts -> match -> propose -> complete -> auto-close.
</verification>

<success_criteria>
- Trades tab shows My Posts + Proposals (no matches)
- My Posts segment lists user's posts with status badges and management actions
- ProposalCreationModal works from post context with pre-populated card data
- Socket.IO handles post-match and post-closed events with UI updates
- Proposals can be created, accepted, rejected, countered, completed from posts
- Auto-closed posts reflect correctly in UI via real-time events
- Full end-to-end flow verified by human tester
</success_criteria>

<output>
After completion, create `.planning/phases/08-post-based-trading/08-04-SUMMARY.md`
</output>
