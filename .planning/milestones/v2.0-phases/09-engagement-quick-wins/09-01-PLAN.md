---
phase: 09-engagement-quick-wins
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/schemas/pull-rates.ts
  - packages/shared/src/index.ts
  - packages/shared/src/__tests__/pull-rates.test.ts
  - apps/mobile/src/components/cards/LuckCalculator.tsx
  - apps/mobile/src/components/cards/CardDetailModal.tsx
autonomous: true
requirements:
  - INTL-05

must_haves:
  truths:
    - "User sees a 'Calculate odds' button on card detail modal"
    - "User sees pull rate per pack, expected packs to pull, and cost estimate for any card"
    - "User sees a cumulative probability curve chart"
    - "Probability math is correct: per-card rate = slot rate / cards of same rarity in pack"
  artifacts:
    - path: "packages/shared/src/schemas/pull-rates.ts"
      provides: "Slot-based pull rate constants and probability math functions"
      exports: ["SLOT_RATES", "GOD_PACK_RATE", "GOD_PACK_SLOT_RATES", "probabilityInNPacks", "packsForProbability"]
    - path: "packages/shared/src/__tests__/pull-rates.test.ts"
      provides: "Unit tests for probability math"
      contains: "probabilityInNPacks"
    - path: "apps/mobile/src/components/cards/LuckCalculator.tsx"
      provides: "Bottom sheet UI with probability stats and SVG curve"
      min_lines: 80
  key_links:
    - from: "apps/mobile/src/components/cards/CardDetailModal.tsx"
      to: "apps/mobile/src/components/cards/LuckCalculator.tsx"
      via: "Calculate odds button opens LuckCalculator"
      pattern: "LuckCalculator"
    - from: "apps/mobile/src/components/cards/LuckCalculator.tsx"
      to: "packages/shared/src/schemas/pull-rates.ts"
      via: "imports probability functions"
      pattern: "probabilityInNPacks|packsForProbability"
---

<objective>
Implement a luck calculator that shows pack opening probabilities for any specific card.

Purpose: Answers the question every Pokemon TCG Pocket player asks -- "how many packs do I need?" -- driving daily app engagement and card detail views.
Output: Pull rate constants in shared package with unit tests, LuckCalculator bottom sheet component, "Calculate odds" button on CardDetailModal.
</objective>

<execution_context>
@C:/Users/romec/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/romec/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-engagement-quick-wins/09-CONTEXT.md
@.planning/phases/09-engagement-quick-wins/09-RESEARCH.md

<interfaces>
<!-- Key types and contracts from existing codebase -->

From packages/shared/src/schemas/card.ts:
```typescript
export const rarityValues = ['diamond1', 'diamond2', 'diamond3', 'diamond4', 'star1', 'star2', 'star3', 'crown'];
export type CardLanguage = typeof cardLanguageValues[number];
export const cardSchema = z.object({ ... });
export type Card = z.infer<typeof cardSchema>;
```

From apps/api/src/db/schema.ts:
```typescript
export const rarityEnum = pgEnum('rarity', ['diamond1', 'diamond2', 'diamond3', 'diamond4', 'star1', 'star2', 'star3', 'crown']);
export const cards = pgTable('cards', { ... }); // has rarity, setId columns
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pull rate constants and probability math with tests</name>
  <files>packages/shared/src/schemas/pull-rates.ts, packages/shared/src/__tests__/pull-rates.test.ts, packages/shared/src/index.ts</files>
  <behavior>
    - Test: SLOT_RATES.slot_4 values sum to approximately 1.0
    - Test: SLOT_RATES.slot_5 values sum to approximately 1.0
    - Test: probabilityInNPacks returns 0 for 0 packs
    - Test: probabilityInNPacks returns value approaching 1.0 for very high pack count
    - Test: probabilityInNPacks for diamond1 rarity returns 1.0 (guaranteed slots 1-3)
    - Test: packsForProbability returns correct expected packs for 50% probability on a known rarity
    - Test: packsForProbability returns Infinity for diamond1 (not in slot 4 or 5 rates, always guaranteed)
    - Test: Per-card rate correctly divides by cardsOfSameRarityInPack
  </behavior>
  <action>
    Create `packages/shared/src/schemas/pull-rates.ts` with:
    1. SLOT_RATES constant with slot_1_3 (diamond1: 1.0), slot_4 (diamond2 through crown rates from RESEARCH.md), slot_5 (same rarities, higher rates from RESEARCH.md)
    2. GOD_PACK_RATE = 0.0005
    3. GOD_PACK_SLOT_RATES constant
    4. `probabilityInNPacks(cardRarity, cardsOfSameRarityInPack, numPacks)` -- cumulative probability: 1 - (1-p)^n where p = (slot4Rate + slot5Rate) / cardsOfSameRarityInPack
    5. `packsForProbability(cardRarity, cardsOfSameRarityInPack, targetProbability)` -- inverse: ceil(log(1-target) / log(1-p))
    6. Export all from packages/shared/src/index.ts

    IMPORTANT: diamond1 cards are guaranteed in slots 1-3 and do NOT appear in slot 4/5 rates. For diamond1, per-pack probability should be computed differently (always pulled), so probabilityInNPacks should return a special case. However, since users won't typically calculate odds for guaranteed cards, handle gracefully by returning the slot4+slot5 rate (which is 0), resulting in 0% probability via the formula. The UI can show "Guaranteed in every pack" for diamond1 rarity.

    Write tests FIRST in `packages/shared/src/__tests__/pull-rates.test.ts`, then implement to pass.
  </action>
  <verify>
    <automated>cd apps/mobile/../../packages/shared && npx jest --testPathPattern="pull-rates" -x --passWithNoTests 2>/dev/null || cd ../../packages/shared && npx jest --testPathPattern="pull-rates" -x</automated>
  </verify>
  <done>Pull rate constants defined, probability functions exported, all unit tests pass, slot rates sum to ~1.0 per slot</done>
</task>

<task type="auto">
  <name>Task 2: LuckCalculator bottom sheet and CardDetailModal integration</name>
  <files>apps/mobile/src/components/cards/LuckCalculator.tsx, apps/mobile/src/components/cards/CardDetailModal.tsx</files>
  <action>
    Create `apps/mobile/src/components/cards/LuckCalculator.tsx` as a bottom sheet component that receives card rarity and setId as props:

    1. **Props:** `{ visible: boolean; onClose: () => void; cardRarity: string; cardName: string; setId: string; }`
    2. **Data fetching:** Query the API or use local card data to count how many cards share the same rarity in the same set (cardsOfSameRarityInPack). If API call is impractical, accept `cardsOfSameRarityInPack` as a prop computed by the parent.
    3. **Stats display section:**
       - Pull rate per pack: `((slot4Rate + slot5Rate) / cardsOfSameRarityInPack * 100).toFixed(3)%`
       - Expected packs for 50% chance: use `packsForProbability(rarity, count, 0.5)`
       - Expected packs for 90% chance: use `packsForProbability(rarity, count, 0.9)`
       - Cost estimate: expected packs * 2 (each pack costs 2 pack points in Pokemon TCG Pocket)
    4. **Cumulative probability curve:** Use react-native-svg to draw an SVG Path showing probability (y-axis, 0-100%) vs packs opened (x-axis, 0 to packsFor99%). Compute ~20 data points using probabilityInNPacks and connect with a smooth curve. Use gold accent color (#f0c040) for the curve line. Add horizontal dashed lines at 50% and 90%.
    5. **Special case:** If rarity is diamond1, show "Guaranteed in every pack" message instead of probability stats.
    6. **Styling:** Dark background consistent with app theme, gold accent (#f0c040) for highlight text and chart line, white text for labels.
    7. **Bottom sheet:** Use a Modal or existing bottom sheet pattern from the project (check if there's a BottomSheet component used elsewhere, e.g., PostDetailModal pattern).

    Modify `apps/mobile/src/components/cards/CardDetailModal.tsx`:
    1. Add a "Calculate odds" button (TouchableOpacity) below existing card info, styled with gold accent border
    2. Add state for LuckCalculator visibility
    3. Pass card.rarity, card.name, card.setId (and cardsOfSameRarityInPack if computed here) to LuckCalculator
    4. Import LuckCalculator component

    Use RarityBadge component for rarity display in the calculator if appropriate.
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>CardDetailModal shows "Calculate odds" button, tapping it opens LuckCalculator bottom sheet with pull rate stats and SVG probability curve for the viewed card's rarity. Diamond1 cards show "Guaranteed" message.</done>
</task>

</tasks>

<verification>
- Pull rate math unit tests pass
- TypeScript compilation succeeds with no errors
- LuckCalculator imports from shared package correctly
- CardDetailModal renders the "Calculate odds" button
</verification>

<success_criteria>
User can tap "Calculate odds" on any card detail view and see per-card pull rate, expected packs for 50%/90% probability, cost estimate, and a cumulative probability curve chart. Free for all users.
</success_criteria>

<output>
After completion, create `.planning/phases/09-engagement-quick-wins/09-01-SUMMARY.md`
</output>
