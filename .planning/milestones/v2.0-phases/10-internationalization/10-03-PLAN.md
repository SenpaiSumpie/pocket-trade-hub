---
phase: 10-internationalization
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - apps/mobile/src/i18n/locales/de.json
  - apps/mobile/src/i18n/locales/es.json
  - apps/mobile/src/i18n/locales/fr.json
  - apps/mobile/src/i18n/locales/it.json
  - apps/mobile/src/i18n/locales/ja.json
  - apps/mobile/src/i18n/locales/ko.json
  - apps/mobile/src/i18n/locales/pt.json
  - apps/mobile/src/i18n/locales/zh.json
  - apps/mobile/src/i18n/locales/th.json
  - apps/mobile/src/i18n/index.ts
  - apps/api/src/i18n/locales/de.json
  - apps/api/src/i18n/locales/es.json
  - apps/api/src/i18n/locales/fr.json
  - apps/api/src/i18n/locales/it.json
  - apps/api/src/i18n/locales/ja.json
  - apps/api/src/i18n/locales/ko.json
  - apps/api/src/i18n/locales/pt.json
  - apps/api/src/i18n/locales/zh.json
  - apps/api/src/i18n/locales/th.json
  - apps/api/src/i18n/index.ts
  - apps/api/src/routes/auth.ts
  - apps/api/src/routes/users.ts
  - apps/api/src/routes/posts.ts
  - apps/api/src/routes/proposals.ts
  - apps/api/src/routes/collection.ts
  - apps/api/src/routes/wanted.ts
  - apps/api/src/routes/promo.ts
  - apps/api/src/routes/premium.ts
  - apps/api/src/services/notification.service.ts
  - apps/api/src/services/match.service.ts
  - apps/api/src/services/post-match.service.ts
  - apps/api/src/services/proposal.service.ts
  - apps/api/src/services/rating.service.ts
  - apps/api/src/services/card-alert.service.ts
autonomous: true
requirements:
  - PLAT-03

must_haves:
  truths:
    - "API error messages return in the user's preferred language"
    - "Push notifications are sent in the user's preferred language"
    - "All 9 non-English translation files exist for mobile with complete key coverage"
    - "All 9 non-English translation files exist for server with complete key coverage including email template keys"
    - "Email template translation keys exist in all 10 languages"
  artifacts:
    - path: "apps/mobile/src/i18n/locales/de.json"
      provides: "German mobile translations"
      min_lines: 200
    - path: "apps/mobile/src/i18n/locales/ja.json"
      provides: "Japanese mobile translations"
      min_lines: 200
    - path: "apps/mobile/src/i18n/locales/th.json"
      provides: "Thai mobile translations"
      min_lines: 200
    - path: "apps/api/src/i18n/locales/de.json"
      provides: "German server translations (errors, notifications, emails)"
      min_lines: 30
    - path: "apps/api/src/i18n/index.ts"
      provides: "Server i18n with all 10 languages loaded"
      contains: "resources"
  key_links:
    - from: "apps/api/src/routes/auth.ts"
      to: "apps/api/src/i18n/index.ts"
      via: "t() calls for error messages with user language"
      pattern: "t\\("
    - from: "apps/api/src/services/notification.service.ts"
      to: "apps/api/src/i18n/index.ts"
      via: "t() calls for notification title/body"
      pattern: "t\\("
    - from: "apps/mobile/src/i18n/index.ts"
      to: "apps/mobile/src/i18n/locales/*.json"
      via: "resource imports for all 10 languages"
      pattern: "import.*from.*locales"
---

<objective>
Generate all 9 non-English translation files, wire server-side i18n into API routes and notification services, and ensure email template keys are translated.

Purpose: Without translation files, the i18n infrastructure from Plan 01 and the t() calls from Plan 02 only produce English. This plan generates the actual translations and makes the server speak the user's language for errors, notifications, and email templates.

Output: 18 translation JSON files (9 mobile + 9 server), server routes using translated errors, notification services using translated messages, email template keys in all languages.
</objective>

<execution_context>
@C:/Users/romec/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/romec/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-internationalization/10-CONTEXT.md
@.planning/phases/10-internationalization/10-RESEARCH.md
@.planning/phases/10-internationalization/10-01-SUMMARY.md

@apps/mobile/src/i18n/locales/en.json
@apps/api/src/i18n/locales/en.json
@apps/api/src/i18n/index.ts
@apps/mobile/src/i18n/index.ts

<interfaces>
<!-- From Plan 01 -->

From apps/api/src/i18n/index.ts:
```typescript
export function t(key: string, lng: string, options?: Record<string, unknown>): string;
export async function initServerI18n(): Promise<void>;
```

From apps/api/src/db/schema.ts:
```typescript
// users table now has:
uiLanguage: varchar('ui_language', { length: 5 }).default('en')
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate 9 non-English mobile translation files and register in mobile i18n config</name>
  <files>
    apps/mobile/src/i18n/locales/de.json,
    apps/mobile/src/i18n/locales/es.json,
    apps/mobile/src/i18n/locales/fr.json,
    apps/mobile/src/i18n/locales/it.json,
    apps/mobile/src/i18n/locales/ja.json,
    apps/mobile/src/i18n/locales/ko.json,
    apps/mobile/src/i18n/locales/pt.json,
    apps/mobile/src/i18n/locales/zh.json,
    apps/mobile/src/i18n/locales/th.json,
    apps/mobile/src/i18n/index.ts
  </files>
  <action>
1. For each of the 9 non-English languages (de, es, fr, it, ja, ko, pt, zh, th), create a mobile translation file that is a complete translation of en.json:
   - Maintain the EXACT same key structure as en.json (same nesting, same keys)
   - Translate every value to the target language
   - Preserve interpolation variables exactly as-is: {{count}}, {{name}}, {{setName}}, etc.
   - Preserve pluralization suffixes: keys ending in _one, _other must exist with translated values
   - For JA, KO, ZH, TH: no grammatical plural, so _one and _other can have the same translated value
   - Keep universal terms as-is: "Pokemon", "TCG"
   - Quality: AI-translated, functional, not professional-grade

2. Update apps/mobile/src/i18n/index.ts:
   - Add imports for all 9 locale files
   - Add all 9 to the resources object

Translation guidance:
- DE: Formal "Sie" form. German words tend to be longer.
- ES: Latin American neutral Spanish.
- FR: Standard French.
- IT: Standard Italian.
- JA: Polite desu/masu form. Katakana for loanwords.
- KO: Polite speech level.
- PT: Brazilian Portuguese.
- ZH: Simplified Chinese.
- TH: Neutral tone, no polite particles in UI labels.
  </action>
  <verify>
    <automated>cd apps/mobile && node -e "const en=require('./src/i18n/locales/en.json'); const langs=['de','es','fr','it','ja','ko','pt','zh','th']; let ok=true; for(const l of langs){ const f=require('./src/i18n/locales/'+l+'.json'); const ek=JSON.stringify(Object.keys(en).sort()); const lk=JSON.stringify(Object.keys(f).sort()); if(ek!==lk){console.error(l+' top-level keys mismatch');ok=false} } if(ok) console.log('All 9 mobile locale files match en.json top-level keys'); else process.exit(1)"</automated>
  </verify>
  <done>
    - 9 mobile translation files exist with same key structure as en.json
    - Mobile i18n config loads all 10 languages
    - Interpolation variables preserved in all translations
    - Pluralization keys (_one, _other) present in all files
  </done>
</task>

<task type="auto">
  <name>Task 2: Generate 9 non-English server translation files (including email template keys), register in server config, and wire i18n into API routes and services</name>
  <files>
    apps/api/src/i18n/locales/de.json,
    apps/api/src/i18n/locales/es.json,
    apps/api/src/i18n/locales/fr.json,
    apps/api/src/i18n/locales/it.json,
    apps/api/src/i18n/locales/ja.json,
    apps/api/src/i18n/locales/ko.json,
    apps/api/src/i18n/locales/pt.json,
    apps/api/src/i18n/locales/zh.json,
    apps/api/src/i18n/locales/th.json,
    apps/api/src/i18n/index.ts,
    apps/api/src/routes/auth.ts,
    apps/api/src/routes/users.ts,
    apps/api/src/routes/posts.ts,
    apps/api/src/routes/proposals.ts,
    apps/api/src/routes/collection.ts,
    apps/api/src/routes/wanted.ts,
    apps/api/src/routes/promo.ts,
    apps/api/src/routes/premium.ts,
    apps/api/src/services/notification.service.ts,
    apps/api/src/services/match.service.ts,
    apps/api/src/services/post-match.service.ts,
    apps/api/src/services/proposal.service.ts,
    apps/api/src/services/rating.service.ts,
    apps/api/src/services/card-alert.service.ts
  </files>
  <action>
1. For each of the 9 non-English languages, create a server translation file translating the server en.json:
   - Same key structure as server en.json (errors, notifications, AND emails sections)
   - All error, notification, AND email template strings translated
   - Preserve interpolation variables ({{variable}})
   - The `emails` section MUST be translated for all languages -- this is a locked decision from CONTEXT.md (email template i18n). Email keys include:
     - emails.passwordReset.subject, greeting, body, buttonText, footer, expiryNote
     - emails.welcome.subject, greeting, body, buttonText, footer
   - Translation guidance same as Task 1 (DE formal Sie, JA polite desu/masu, etc.)

2. Update apps/api/src/i18n/index.ts:
   - Add imports for all 9 server locale files
   - Add all 9 to the server resources object

3. For API route files, replace hardcoded error strings with t() calls:
   - Import `{ t }` from '../i18n' (or '../../i18n' depending on depth)
   - For authenticated routes: get user's language from request.user or query the user's uiLanguage from DB
   - For unauthenticated routes (login, signup, password reset): use Accept-Language header to determine language, with 'en' fallback. Parse with: `const lang = (request.headers['accept-language'] || 'en').split(',')[0].split('-')[0]` then validate against supported languages.
   - Replace patterns like `reply.code(404).send({ error: 'User not found' })` with `reply.code(404).send({ error: t('errors.userNotFound', lang) })`
   - Key routes to update:
     - auth.ts: 'Email already registered', 'Invalid email or password', 'Invalid or expired token', 'Invalid refresh token'
     - users.ts: 'User not found', 'Validation failed'
     - posts.ts: 'Post not found', 'Limit reached' messages
     - proposals.ts: 'Proposal not found' and status messages
     - collection.ts, wanted.ts: error messages
     - promo.ts: 'Invalid code', 'Code already redeemed', 'Code expired'
     - premium.ts: error messages

4. For notification service files, replace hardcoded notification title/body strings:
   - notification.service.ts: sendNewSetNotification needs to fetch user's uiLanguage per push token recipient. Change the function to accept db parameter and look up user language per token. Use t('notifications.newSetTitle', lang) and t('notifications.newSetBody', lang, { setName, cardCount }).
   - match.service.ts: Replace hardcoded 'New match found!' / 'High-priority match!' with t() calls. The function already has access to userId -- query user's uiLanguage.
   - post-match.service.ts: Replace 'Someone wants your card!' and other hardcoded strings with t() calls.
   - proposal.service.ts: Replace proposal notification strings with t() calls.
   - rating.service.ts: Replace 'New rating received' and body string with t() calls.
   - card-alert.service.ts: Replace 'Card Alert' title and body with t() calls.

5. For notification services that send to specific users, the pattern is:
   - Query `users.uiLanguage` for the target user (often already have userId)
   - Pass language to t() call
   - For bulk notifications (sendNewSetNotification), join pushTokens with users to get per-user language

6. Initialize server i18n on startup: In the main server entry point (likely apps/api/src/index.ts or server.ts), call initServerI18n() during server startup before routes are registered.

IMPORTANT: For authenticated routes, the simplest approach is to add a Fastify preHandler or decorator that reads the user's uiLanguage and attaches it to the request. This avoids querying the DB for language in every route handler. Add a `request.userLanguage` property set from the JWT claims or a lightweight DB lookup.
  </action>
  <verify>
    <automated>cd apps/api && pnpm test -- --no-coverage -x 2>&1 | tail -20</automated>
  </verify>
  <done>
    - 9 server translation files exist with same key structure as server en.json (errors + notifications + emails)
    - Email template keys (passwordReset.*, welcome.*) translated in all 9 non-English languages
    - Server i18n config loads all 10 languages
    - All API error messages use t() with user's language preference
    - All push notification title/body strings use t() with recipient's language
    - Unauthenticated routes use Accept-Language header for language detection
    - Server i18n initializes on startup
    - Existing API tests pass (error message assertions may need updating to match en.json keys)
  </done>
</task>

</tasks>

<verification>
- `cd apps/api && pnpm test -- --no-coverage -x` passes
- `cd apps/mobile && npx tsc --noEmit` passes
- All 10 locale files exist in both apps/mobile/src/i18n/locales/ and apps/api/src/i18n/locales/
- Each locale file has the same top-level key structure as en.json
- Server locale files all contain `emails` section with passwordReset and welcome keys
</verification>

<success_criteria>
- 9 non-English translation files for mobile, each matching en.json key structure
- 9 non-English translation files for server, each matching server en.json key structure (including emails section)
- i18n configs load all 10 languages on both mobile and server
- API routes return errors in user's preferred language
- Push notifications sent in user's preferred language
- Email template translation keys ready in all 10 languages for when email sending is implemented
- Server i18n initializes on startup
</success_criteria>

<output>
After completion, create `.planning/phases/10-internationalization/10-03-SUMMARY.md`
</output>
