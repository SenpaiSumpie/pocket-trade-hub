---
phase: 11-intelligence
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/api/__tests__/services/suggest.service.test.ts
  - apps/api/__tests__/services/meta-scraper.service.test.ts
  - apps/api/__tests__/services/tierlist.service.test.ts
  - apps/api/__tests__/routes/meta.route.test.ts
  - apps/api/__tests__/routes/tierlist.route.test.ts
autonomous: true
requirements:
  - INTL-01
  - TRAD-07
  - INTL-02
  - INTL-03
  - INTL-04

must_haves:
  truths:
    - "Test stubs exist for all Intelligence phase services and routes"
    - "Jest test runner discovers all 5 new test files"
    - "All tests are pending/skipped (not failing)"
  artifacts:
    - path: "apps/api/__tests__/services/suggest.service.test.ts"
      provides: "Test stubs for suggestion computation (INTL-01, TRAD-07)"
    - path: "apps/api/__tests__/services/meta-scraper.service.test.ts"
      provides: "Test stubs for meta scraper parsing (INTL-02)"
    - path: "apps/api/__tests__/services/tierlist.service.test.ts"
      provides: "Test stubs for tier list CRUD, voting, official generation (INTL-03, INTL-04)"
    - path: "apps/api/__tests__/routes/meta.route.test.ts"
      provides: "Test stubs for meta routes with premium gating (INTL-02)"
    - path: "apps/api/__tests__/routes/tierlist.route.test.ts"
      provides: "Test stubs for tier list routes (INTL-03, INTL-04)"
  key_links: []
---

<objective>
Create Wave 0 test stubs for all Intelligence phase services and routes per VALIDATION.md Nyquist requirements.

Purpose: Establish test scaffolds so that Plans 01-02 can run Jest verification against real test files during execution, maintaining feedback sampling continuity.
Output: 5 test stub files with pending/skipped tests describing expected behaviors.
</objective>

<execution_context>
@C:/Users/romec/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/romec/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-intelligence/11-CONTEXT.md
@.planning/phases/11-intelligence/11-VALIDATION.md
@apps/api/__tests__/setup.ts
@apps/api/__tests__/services/analytics.service.test.ts
@apps/api/jest.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test stubs for suggestion, meta-scraper, and tierlist services</name>
  <files>apps/api/__tests__/services/suggest.service.test.ts, apps/api/__tests__/services/meta-scraper.service.test.ts, apps/api/__tests__/services/tierlist.service.test.ts</files>
  <action>
Follow the existing test file pattern from analytics.service.test.ts (imports from setup.ts, uses testDb, buildTestApp).

1. Create apps/api/__tests__/services/suggest.service.test.ts:
   - describe('suggest.service')
   - it.todo('computes suggestions from user wanted list and collection')
   - it.todo('returns empty suggestions when user has < 3 collection items')
   - it.todo('caches suggestions in Redis with 1-hour TTL')
   - it.todo('returns cached suggestions on subsequent calls')
   - it.todo('invalidates cache when refresh=true')
   - it.todo('limits suggestions to top 5 by score')
   - it.todo('generates template reasoning text from data signals')
   - it.todo('hydrates card details (name, rarity, imageUrl) for give and get cards')

2. Create apps/api/__tests__/services/meta-scraper.service.test.ts:
   - describe('meta-scraper.service')
   - it.todo('parses Limitless TCG HTML into structured deck data')
   - it.todo('extracts deck name, play count, usage %, win rate from table cells')
   - it.todo('returns empty array when scrape fails')
   - it.todo('returns empty array when HTML has no deck rows')

3. Create apps/api/__tests__/services/tierlist.service.test.ts:
   - describe('tierlist.service')
   - it.todo('creates a new tier list with valid tiers')
   - it.todo('rejects tier list with no decks in any tier')
   - it.todo('returns paginated tier lists sorted by most_liked')
   - it.todo('returns paginated tier lists sorted by newest')
   - it.todo('includes userVoted boolean when userId provided')
   - it.todo('allows owner to delete their tier list')
   - it.todo('rejects deletion by non-owner')
   - it.todo('toggles vote: adds vote if not voted, removes if already voted')
   - it.todo('atomically updates upvoteCount on vote toggle')
   - it.todo('generates official tier list from meta data with correct tier distribution')

Each file should import { buildTestApp, cleanDb, closeDb, testDb } from '../setup' and include standard beforeAll/afterAll/afterEach hooks (buildTestApp, cleanDb, closeDb) even though tests are pending -- this validates the import paths compile.
  </action>
  <verify>
    <automated>cd apps/api && npx jest --testPathPattern="(suggest|meta-scraper|tierlist)\\.service" --no-coverage 2>&1 | tail -10</automated>
  </verify>
  <done>3 service test files exist with pending test stubs. Jest discovers all files and reports them as todo/pending (0 failures).</done>
</task>

<task type="auto">
  <name>Task 2: Create test stubs for meta and tierlist routes</name>
  <files>apps/api/__tests__/routes/meta.route.test.ts, apps/api/__tests__/routes/tierlist.route.test.ts</files>
  <action>
Follow existing route test pattern from premium.route.test.ts or cards.route.test.ts.

1. Create apps/api/__tests__/routes/meta.route.test.ts:
   - describe('GET /meta/decks')
   - it.todo('returns deck list for authenticated user')
   - it.todo('returns limited data for free user (no matchups, no tournamentResults, top 3 cards only)')
   - it.todo('returns full data for premium user')
   - it.todo('returns 401 for unauthenticated request')
   - describe('GET /meta/decks/:id')
   - it.todo('returns single deck detail')
   - it.todo('returns 404 for non-existent deck')
   - it.todo('applies premium gating to detail fields')

2. Create apps/api/__tests__/routes/tierlist.route.test.ts:
   - describe('GET /tierlists')
   - it.todo('returns paginated tier lists')
   - it.todo('sorts by most_liked when requested')
   - it.todo('sorts by newest when requested')
   - describe('POST /tierlists')
   - it.todo('creates tier list for authenticated user')
   - it.todo('validates title is required')
   - it.todo('validates at least one deck in tiers')
   - describe('DELETE /tierlists/:id')
   - it.todo('allows owner to delete')
   - it.todo('returns 403 for non-owner')
   - it.todo('returns 404 for non-existent tier list')
   - describe('POST /tierlists/:id/vote')
   - it.todo('adds vote for first-time voter')
   - it.todo('removes vote for repeat voter')
   - it.todo('returns updated upvoteCount and userVoted')

Import { buildTestApp, cleanDb, closeDb, testDb } from '../setup' and include standard hooks.
  </action>
  <verify>
    <automated>cd apps/api && npx jest --testPathPattern="(meta|tierlist)\\.route" --no-coverage 2>&1 | tail -10</automated>
  </verify>
  <done>2 route test files exist with pending test stubs. Jest discovers all files and reports them as todo/pending (0 failures).</done>
</task>

</tasks>

<verification>
- `cd apps/api && npx jest --no-coverage 2>&1 | tail -15` shows all 5 new test files discovered with pending tests
- No test failures introduced
- Existing test suite remains green
</verification>

<success_criteria>
- 5 new test stub files in apps/api/__tests__/
- All tests are pending (it.todo) -- no red tests
- Jest discovers all files without errors
- Import paths and setup hooks compile correctly
</success_criteria>

<output>
After completion, create `.planning/phases/11-intelligence/11-00-SUMMARY.md`
</output>
