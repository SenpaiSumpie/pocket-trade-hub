---
phase: 12-web-app-companion
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - apps/web/package.json
  - apps/web/tsconfig.json
  - apps/web/next.config.ts
  - apps/web/postcss.config.mjs
  - apps/web/src/app/globals.css
  - apps/web/src/app/layout.tsx
  - apps/web/src/app/page.tsx
  - apps/web/src/app/not-found.tsx
  - apps/web/src/app/error.tsx
  - apps/web/src/lib/api.ts
  - apps/web/src/lib/constants.ts
  - apps/web/src/lib/socket.ts
  - apps/web/src/i18n/index.ts
  - apps/web/src/stores/auth.ts
  - turbo.json
autonomous: true
requirements:
  - PLAT-01

must_haves:
  truths:
    - "Web app starts and renders in a browser at localhost:3001"
    - "Dark theme with gold accent matches mobile app branding"
    - "apiFetch wrapper sends credentials: include and handles 401 refresh"
    - "i18n initialized with 10 locale files"
  artifacts:
    - path: "apps/web/package.json"
      provides: "Next.js project configuration"
      contains: "next"
    - path: "apps/web/src/lib/api.ts"
      provides: "Cookie-credentialed API fetch wrapper"
      contains: "credentials"
    - path: "apps/web/src/stores/auth.ts"
      provides: "Auth Zustand store"
      contains: "useAuthStore"
    - path: "apps/web/src/i18n/index.ts"
      provides: "i18n initialization"
      contains: "i18next"
  key_links:
    - from: "apps/web/src/lib/api.ts"
      to: "apps/api"
      via: "fetch with credentials: include"
      pattern: "credentials.*include"
---

<objective>
Next.js project scaffold with Tailwind dark theme, core libraries (apiFetch, socket, i18n, auth store), and project configuration.

Purpose: Creates the foundational project structure, theme, and core utilities that all subsequent web plans depend on.
Output: Next.js app that boots at localhost:3001 with dark theme, apiFetch wrapper, auth store, i18n, and socket client ready.
</objective>

<execution_context>
@C:/Users/romec/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/romec/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-web-app-companion/12-CONTEXT.md
@.planning/phases/12-web-app-companion/12-RESEARCH.md
@.planning/phases/12-web-app-companion/12-01-SUMMARY.md

@apps/api/package.json
@turbo.json
@pnpm-workspace.yaml

<interfaces>
From apps/web/vitest.config.ts (created in Plan 01):
```typescript
// Test infrastructure already configured -- vitest runs with `pnpm test`
```

From apps/web/__tests__/lib/api.test.ts (stubs from Plan 01):
```typescript
// TODO stubs for apiFetch -- implement the actual tests after creating api.ts
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project scaffold, config, and theme</name>
  <files>
    apps/web/package.json,
    apps/web/tsconfig.json,
    apps/web/next.config.ts,
    apps/web/postcss.config.mjs,
    apps/web/src/app/globals.css,
    apps/web/src/app/layout.tsx,
    apps/web/src/app/page.tsx,
    apps/web/src/app/not-found.tsx,
    apps/web/src/app/error.tsx,
    turbo.json
  </files>
  <action>
    1. Update apps/web/package.json (already partially created in Plan 01 for vitest) -- add:
       - scripts: dev (next dev -p 3001), build (next build), start (next start) -- keep existing test script
       - Dependencies: next@^15.2, react@^19.1, react-dom@^19.1, zustand@^5.0, socket.io-client@^4.8, i18next@^25.8, react-i18next@^16.5, zod@^3.24, @pocket-trade-hub/shared (workspace:*), lucide-react
       - DevDeps: typescript@^5.8, @types/react@^19.1, tailwindcss@^4.0, @tailwindcss/postcss, postcss, autoprefixer
       - Run `cd apps/web && pnpm install`

    2. tsconfig.json: Extend from base if exists, or create with target ES2022, module ESNext, moduleResolution bundler, jsx preserve, paths for @/* -> src/*

    3. next.config.ts: Enable transpilePackages: ['@pocket-trade-hub/shared'].

    4. postcss.config.mjs: `export default { plugins: { '@tailwindcss/postcss': {} } }`

    5. globals.css with Tailwind v4 CSS-first config:
       ```css
       @import "tailwindcss";
       @theme {
         --color-bg: #0a0a0a;
         --color-surface: #18181b;
         --color-surface-hover: #27272a;
         --color-border: #3f3f46;
         --color-text: #fafafa;
         --color-text-muted: #a1a1aa;
         --color-gold: #f0c040;
         --color-gold-hover: #d4a830;
       }
       ```

    6. Root layout.tsx: Import globals.css, set <html lang="en" className="dark">, <body className="bg-bg text-text min-h-screen">

    7. Root page.tsx: Redirect to /cards (middleware handles auth check)

    8. not-found.tsx: Simple 404 page with dark theme, "Page not found" message, link back to /cards

    9. error.tsx: "use client" error boundary with dark theme, error message, retry button

    10. Update turbo.json: Add web-specific task config (build outputs .next/**).
  </action>
  <verify>
    <automated>cd apps/web && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <done>Next.js project builds. Dark theme CSS configured. Root layout renders. Error/404 pages exist. Turbo pipeline updated.</done>
</task>

<task type="auto">
  <name>Task 2: Core lib utilities, auth store, and i18n</name>
  <files>
    apps/web/src/lib/api.ts,
    apps/web/src/lib/constants.ts,
    apps/web/src/lib/socket.ts,
    apps/web/src/i18n/index.ts,
    apps/web/src/stores/auth.ts
  </files>
  <action>
    1. lib/constants.ts: API_URL from NEXT_PUBLIC_API_URL env var (default http://localhost:3000), theme color tokens for JS usage.

    2. lib/api.ts: apiFetch<T> wrapper per research Pattern 1. Uses API_URL from constants. credentials: 'include'. On 401, tries /auth/refresh (POST with credentials: include), on success retries original request, on failure redirects to /login via window.location. Content-Type: application/json. After creating, implement the todo test stubs from Plan 01 in __tests__/lib/api.test.ts -- mock fetch globally and verify credentials: include, 401 refresh retry, and redirect on refresh failure.

    3. lib/socket.ts: "use client" module. Creates socket.io-client instance with autoConnect: false, withCredentials: true. Export socket instance and connectSocket/disconnectSocket pair.

    4. i18n/index.ts: Initialize i18next with react-i18next. Copy locale files from apps/mobile/src/i18n/locales/ into apps/web/src/i18n/locales/ (all 10 locale JSON files). Use getBrowserLanguage() to detect initial language. Fallback to en.

    5. stores/auth.ts: "use client" Zustand store with: user (null | User type), isLoggedIn, isHydrated. Actions: login(email, password) via apiFetch POST /auth/login, signup(email, password, displayName) via apiFetch POST /auth/signup, logout() via apiFetch POST /auth/logout then clear state and redirect, hydrate() via apiFetch GET /users/me to populate user on app load.

    6. Run both build and tests to verify everything compiles and apiFetch tests pass.
  </action>
  <verify>
    <automated>cd apps/web && pnpm build 2>&1 | tail -10 && pnpm test 2>&1 | tail -10</automated>
  </verify>
  <done>apiFetch sends credentials: include and handles 401 refresh. Auth store provides login/signup/logout/hydrate. Socket client configured. i18n initialized with 10 locales. apiFetch tests pass.</done>
</task>

</tasks>

<verification>
- `cd apps/web && pnpm build` -- Next.js build succeeds
- `cd apps/web && pnpm test` -- apiFetch tests pass, stubs registered
- `cd apps/web && pnpm dev` starts without errors on port 3001
</verification>

<success_criteria>
- Next.js project builds and starts at localhost:3001
- Dark theme with gold accent renders correctly
- apiFetch wrapper sends credentials: include (verified by tests)
- Auth store provides login/signup/logout/hydrate actions
- i18n initialized with 10 locale files
- Socket.IO client configured with withCredentials
</success_criteria>

<output>
After completion, create `.planning/phases/12-web-app-companion/12-02-SUMMARY.md`
</output>
